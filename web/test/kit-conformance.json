[
  {
    "law": "palette",
    "where": "shared/web/google-sign-in.tsx",
    "what": "#",
    "why": "Google's own mark. Its four colours are fixed by Google's brand terms for the sign-in button, so a G recoloured to our tokens is a terms violation rather than a theming win — and a mark a person recognises by its colours stops being recognisable when it flips with the palette. The app already carries this exact argument for this exact file in PALETTE_LITERAL_OK (R32, shared/rules/registry.ts); this entry is the same ruling said to the kit's own law, which reads the four fill= attributes where R32 reads the hex literals. Both rot-check, so the day the mark leaves this file both entries go red together."
  },
  {
    "law": "borders",
    "where": "web/components/knowledge/knowledge-upload-dialog.tsx",
    "what": "border",
    "why": "A drop target, which is the case RULES.md §2.7 blesses in its own sentence — form fields and selection controls are the two places a hairline is allowed at all — and the kit makes the identical call for the identical shape in its own components/file-upload/file-upload.tsx. The dashed edge is also doing work no fill and no inset ring can do: dashed is the one stroke style that reads as PROVISIONAL, which is exactly what an empty drop zone is saying, and the same element restates it in the primary tone while a file is being dragged over it. THE HONEST FOOTNOTE: this app already imports the kit's own FileUpload in eight other places, so this hand-rolled zone is a duplicate of a kit part and adopting it would delete this entry outright. That was weighed and not done tonight — the swap changes what a person sees in a dialog, and CONTROL-SWAP-LANES.md's standing ruling on this very file is that an unsure conversion is left alone and reported."
  },
  {
    "law": "images",
    "where": "shared/web/attachment-preview.tsx",
    "what": "fit=\"contain\"",
    "why": "The one picture in this app that IS the content rather than a mark standing for it. Every other picture the fill-not-fit ruling governs is a logo, a face or a brand lockup with the record's name written beside it, where a crop costs the edges of an identity the word already carries. This is a FILE somebody attached to a ticket — a screenshot of the thing that is broken — and the well is 16/9 while an attachment is not, so a portrait screenshot cropped to it shows a band from the middle and hides the error message at the top, which is the reason the file was attached. Nothing on screen tells the reader that happened, because a crop looks exactly like a picture that was always that shape. The app already carries this exact argument for this exact file in OBJECT_FIT_OK (R60, web/test/an-image-fills.test.ts); this entry is the same ruling said to the kit's own law, which arrived with v1.2.75 and reads the same two spellings R60 reads. Both rot-check, so the day this preview stops containing, both entries go red together and both get deleted. Flagged for the client rather than assumed: her 2026-09-09 ruling was made over marks in select components and filters, and this is the one site in the app it does not obviously describe. Delete both the day she says it does."
  }
]
