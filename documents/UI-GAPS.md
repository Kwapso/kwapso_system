# UI gaps, what the library is missing (the flag list)

Rule: the Kwapso System never invents UI. When a needed component doesn't exist in the
library, we build a clearly-marked placeholder in `web/components/temp/`, list it
HERE, and the library absorbs it later — the placeholder is deleted the day the
real component lands, so the temp folder never becomes a second component set.

**What changed on 2026-08-22 — and again on 2026-08-25 — and it changes what this
list IS.** The library was
the npm package `@kwapso/ui`, built and released from a separate repository the
owner deployed. It now lives in this one, at `shared/ui/`, imported as
`@shared/ui/…` — and since 2026-08-25 it is **pinned**: `github.com/Kwapso/kwapso-ui-ux`
at the tag in `shared/ui/VERSION.json`, with a hand-edit under `shared/ui/`
turning the build red (`web/test/vendored-kit.test.ts`). So a row on this list
used to be a *request*: a written change
proposal, handed over, then a wait on somebody else's release schedule, which is
why almost every entry below is phrased as a flag and why several sat open for
months. For three days it was work this repo could do in the same commit; today a
gap is closed **upstream in `Kwapso/kwapso-ui-ux`** — built there, tagged, pulled with
`scripts/sync-design.mjs` — and the swap-and-delete lands here. A gap is still a
to-do with an owner, not a wait on a stranger; the loop just runs through the kit
repo.

Three things did NOT change, and they are the reason this file still exists.
(1) The layering: a primitive is generic, app-agnostic lego, and a control that
only makes sense in kwapso still belongs in `web/components/`, so "which side of
the line is this?" is still the first question every row answers. (2) The
discipline of writing the gap down before working around it — a host workaround
that names the library change it stands in for can be removed; an unexplained one
becomes permanent. (3) The rule about editing in place: `shared/ui/`
is a pinned dependency, so nothing is ever hand-edited under it — a kit change is
made upstream in `github.com/Kwapso/kwapso-ui-ux`, tagged, and pulled. (The retired
lineage, `@swift-struck/ui` → `@kwapso/ui`, is history other products still stand
on; nothing here is pushed back to it either.) See `shared/ui/README.md`.

A note on how to read the rows below, because the status words were written under
the old arrangement and are left as they were rather than reworded one by one.
Entries marked **SHIPPED in the library** with a version number are history: that
is what upstream released while this app was still installing the package, and
the vendored copy carries it. Entries marked **flag for the library**,
**waiting on library** or **nice-to-have** are the OPEN ones, and every one of
them is now a task in this repo rather than a request to another. Nobody is
waiting for anybody. Entries marked **app-side** or **CLOSED app-side** never
concerned the library at all.

## The flag list

### Two things to know before you act on an open row

**1 · The row NUMBER is the handle, and every number below is now unique.** Rows
run **1–34**. Five numbers used to be shared by two or three unrelated rows —
"gap 22" named three different gaps — so a brief, a commit message or a lane
assignment that cited one was ambiguous. The collided rows were renumbered to
**30–34** (calendar-view, `choice`, collection sorting, sorting the remaining
collections, `record-detail`'s avatar) and every other number kept its original
row, so an older citation of 1–29 still lands where it did. In a document this
long the row number is most of the navigation there is; do not reuse one.

**2 · Every open row's evidence PREDATES the current pin. Re-check before you
build.** The `RE-CHECKED …` stamps below say the date and the kit tag the
evidence was gathered at — they are **not** a claim about today. Today's pin is
in `shared/ui/VERSION.json`; the open rows were last checked against **v1.2.24**
and **v1.2.27**, which are many tags behind it. A "still genuinely missing"
verdict gathered against an older kit is not evidence that the kit still lacks
the part, and acting on a stale row means building or keeping a workaround for
something the kit may already ship — the exact cost R39 and R46 exist to avoid.
Before you touch a row: read the tag in `shared/ui/VERSION.json`, run
`node scripts/kit-coverage.mjs` for the current roster, look under
`shared/ui/components/` for the part, and re-stamp the row with what you find.

| # | Missing component | Placeholder here | What the library version needs | Status |
|---|---|---|---|---|
| 1 | `code-input` (primitive), one-time-code boxes | `shared/web/code-input.tsx` (MOVED 2026-08-10. See note under the table) | Configurable length, auto-advance, backspace, paste-spread, numeric keypad on mobile, `one-time-code` autofill, disabled state. **RE-CHECKED 2026-09-02 against the pinned kit (v1.2.24): still genuinely missing** — there is no `components/code-input/`, and no one-time-code control under any other name (nothing in the kit's parts autofills `one-time-code`; the roster is whatever `node scripts/kit-coverage.mjs` reports at the current pin), so `shared/web/code-input.tsx` stands and this row stays open as written. | waiting on library |
| 2 | ~~`auth-card` (collection), full sign-in card~~ |, | Config-driven sign-in card: two-step email→code flow, provider row, `?error=` message strip. | **SHIPPED in the library, adopted 2026-08-29** — `compositions/screens/sign-in-system.tsx` (`LoginRoute`) already carried the provider row and the error strip this row asked for. `web/app/login/page.tsx` now renders `web/components/auth-card.tsx`, which wires `LoginRoute` to the app's own `shared/web/use-email-sign-in.ts` state machine and `shared/web/google-sign-in.tsx`; the temp file is deleted and `web/components/temp/` no longer holds it. One follow-up for whoever owns brand assets, not code: `media` is passed `null` because no kwapso photography exists yet — the kit's own default (`AuthPhotograph`) is generic stock, wrong for this brand — so the photo panel is empty until real photography is commissioned. A second, real finding for the library itself: `LoginRoute` hardcodes `emailLabel="Work email"` when it calls the lower `SignIn` template (`sign-in-system.tsx` line 249) instead of forwarding an `emailLabel` prop the way it forwards every other visible string — `SignIn` itself takes `emailLabel?: string`, `LoginRouteProps` just never exposes it, so an app cannot translate that one field label. Flagged for the library rather than worked around app-side (there's nothing app-side to override). |
| 3 | ~~`permission-matrix` (collection)~~ |, | The roles access-rights grid. | **SHIPPED in the library (2026-06-13)**, live at `@shared/ui/components/permission-matrix/permission-matrix`; integrated in the Member roles detail under `/t/<teamId>/roles/<id>` (host-composed). Temp removed before it was ever needed. |
| 4 | Collection card-surface config (`data-table`, `permission-matrix`) | (overridden app-side via `className`) | The user wants NO card backgrounds. `permission-matrix` flattens cleanly with `className="bg-transparent"`, but its sticky module column keeps a `bg-card` fill (needed so scrolled cells stay opaque), and `data-table`'s frame lives on an inner div `className` can't reach. Proper fix: a `surface: "card" \| "none"` option on the collection config that also swaps the sticky fill to `bg-background`. | **nice-to-have, flag for the library** |
| 5 | ~~`dropdown-menu` (+ `glass` popovers) translucent~~ |, | Menus over page content were see-through (the `glass` 72%-opaque surface, no `bg-popover`). | **SHIPPED in the library (2026-06-18, `c31a35c`)**, `dropdown-menu`/`popover`/`hover-card` now render on an opaque `bg-popover` surface (light + dark). App-side inline stopgaps removed. |
| 6 | ~~Selectable list-row (collection)~~ |, | The role list needed a selected/active highlight; the `list` collection was hover-only. | **SHIPPED in the library (2026-06-18, `c31a35c`)**, `list` now has an opt-in `selectedId`/`onSelect` with an accessible teal accent (`aria-current`/`data-[selected]`) that reads in both themes and keeps the leading icon legible. The engine's role screens use it; the hand-built rows are gone (the old `roles-panel.tsx` host file was itself retired in the M3 engine migration). |
| 7 | ~~Collection **search + filters** (config + UI)~~ |, | The collection system had `searchable` + the `selectRows` engine but no user-facing filter UI. | **SHIPPED**, the library search/filter bar landed and the app turned search + facets ON across every collection via the recipes (2026-06-30, `listCollection` + `withDataDrivenCollection`: search/filters hide when a list is empty or a facet has no options). FTS5 server-side search remains future. See SEARCH.md Status. |
| 8 | ~~Role-detail **Overview/Activity** around the permission matrix~~ |, | The role detail was the last record without the standard tabs (the reviewed R2 exception). | **CLOSED app-side (2026-07-06)**, `role-detail.tsx` now composes TabsView: Permissions (matrix, main) + Overview (audit block) + Activity (generic feed); joined `RECORD_DETAIL_COMPONENTS`, `RECORD_DETAIL_EXCEPTIONS` is empty. |
| 9 | ~~AgentChat **step-row label wrap**~~ |, | Step labels truncated on phones. | **SHIPPED in the library (0.3.0, 2026-07-06)**, `break-words`, status chip first-line; host CSS override removed. |
| 10 | ~~Collection header **compact one-row mobile layout**~~ |, | Count + search + filter stacked on phones. | **SHIPPED in the library (0.3.0, 2026-07-06)**, `<640px`: one row, stretching search + funnel popover, count folded into the placeholder ("Search 36 people…"); desktop byte-for-byte unchanged. |
| 11 | ~~Safe **XLSX → CSV** in the import wizard~~ |, | SheetJS npm carries a HIGH advisory, so it was never bundled. | **CLOSED app-side (2026-07-07)**. The Kwapso System ships its OWN zero-dependency reader (`web/lib/xlsx-to-csv.ts`): hand-parsed ZIP directory + browser-native `DecompressionStream` + DOMParser; first sheet, strings/numbers/booleans/inline; no eval, no formulas run, no deps. `.xlsx` drops work in the wizard AND the chat; legacy `.xls` still asks for a Save-As. Real-fixture tests. |
| 12 | ~~`list` **surface="none" row-group rounding**~~ |, | A flat list's full-bleed hover/selected row showed square corners inside a rounded host card. | **SHIPPED in the library (0.4.0, 2026-07-07)**, the flat list rounds + clips its own row-group (`rounded-xl` + the existing overflow-hidden); host override removed from `CollectionCard`. |
| 13 | ~~AgentChat **composer-native attach**~~ |, | The paperclip belonged INSIDE the composer (Intercom-style), not a host strip. | **SHIPPED in the library (0.4.0, 2026-07-07)**, `onAttachFiles` / `attachAccept` / `attachments` / `onRemoveAttachment` on `AgentChat`; the Kwapso System's host strip replaced with the composer-native slot (panel-wide drop kept). |
| 14 | ~~`web-embed` **default sandbox pairs `allow-scripts` with `allow-same-origin`**~~ |, (host now pre-checks the URL with `safeSrc`) | `WebEmbed`'s default is `"allow-scripts allow-same-origin allow-popups allow-forms allow-presentation"`. Those first two TOGETHER are the one combination the HTML spec calls out: a framed page can script its way to `frameElement` and remove its own `sandbox` attribute, so the frame is effectively unsandboxed. It's the right default for a YouTube/maps embed and the wrong one for an arbitrary link a member typed. Proper fix: an explicit `trust: "embed" \| "unknown"` (or default WITHOUT `allow-same-origin`, opted in per embed), so the untrusted case is the easy one. The Kwapso System's side is done, `learning-detail.tsx` refuses anything that isn't http/https before the URL reaches the frame. | **SHIPPED in the library (v1.2.24, 2026-09-02)** — verified against the vendored source rather than the tag note: `shared/ui/components/web-embed/web-embed.tsx` now declares `const DEFAULT_SANDBOX = "allow-scripts"` and nothing else — no same-origin, and no forms, popups or presentation either — so the pair this row was written about is gone from the default. **What shipped is not what this row asked for, and the shape that shipped is the better one.** The ask was a `trust: "embed" \| "unknown"` union; the kit shipped a boolean `allowSameOrigin`, default `false`, with `SAME_ORIGIN_SANDBOX` composed FROM the default so the two can never disagree about what "the default plus one token" is. The dangerous pair is then one greppable word at the call site instead of a trust label whose meaning has to be looked up, and a call site that needs something else again passes its own `sandbox` string, which replaces the default wholesale — tested `!== undefined`, so `sandbox=""` (the tightest sandbox there is) is a real thing to ask for, and `allowSameOrigin` is ignored beside it. **The host half of this row is stale rather than done:** `learning-detail.tsx` no longer exists, the app draws no embed at all today (`components/web-embed` is a reasoned R46 exemption in `shared/rules/registry.ts` saying exactly that — the only `<iframe>` in the repo is a sanitiser test fixture), so nothing here passes `allowSameOrigin` and nothing had to change to take the safer default. |
| 15 | `screen-renderer` **doesn't pass the frame's `serverSide` seam through** | `web/components/paged-find.tsx` (host-composed, not a temp fork) | `CollectionFrame` already has the right seam, `serverSide` + `onQueryChange`, "the frame stops filtering in memory and emits the query so an app can refetch". `ScreenRenderer` renders that frame from a recipe and passes neither, so a recipe-driven screen cannot use it. That is a real gap, because on a PAGED collection (R14) in-memory search is a lie: it answers about page one under a badge counting everything. The Kwapso System's side is done for all three controls now: the recipe turns the frame's box off, declares no facets and gets no sort picker, and the host draws its own find bar over the door (SEARCH.md layer 2) carrying the search, the filters (`web/lib/collection-filters.ts`) and the order. The facets were the last of the three and were still the frame's until 18 Aug 2026, filtering the knowledge base by "From a meeting" answered TWO over a base holding 170, because page one had two of them on it. Proper fix: `ScreenRenderer` accepts `serverSide` + `onQueryChange` and forwards them to its `CollectionFrame`, so a paged recipe declares its search/filters once and the host answers them from the door. **RE-CHECKED 2026-09-02 against the pinned kit (v1.2.24): the gap is still real and the OWNER has moved.** Both halves are engine code in this repo now — `shared/web/screen-engine/screen-renderer.tsx` names neither `serverSide` nor `onQueryChange` anywhere, while the frame it renders (`shared/web/screen-engine/collection-frame.tsx`) takes both — so this is a task here, not a wait on the kit. The kit's own `components/screen-renderer/screen-renderer.tsx` is a different component and is not what draws these recipes, which also means `npm run kit:drift` (whose probe for this row reads that kit file for `serverSide`) can never see this row close. | **flag for the library** |
| 16 | `screen-renderer` **maps a row to a title and a subtitle, with no leading slot** | none, the mark is simply absent on an engine list (`web/lib/type-marks.ts` carries the read side, ready for the day it lands) | UI-CONVENTIONS §5 (amended 17 Aug 2026) allows a TYPE MARK, one glyph in the slot a lucide icon would take, and CHECKLIST 11.8 asks for one on every collection. The library `List` already HAS the slot, `item.leading` (`registry/collections/list/list.tsx:19,94`), and the host uses it directly on the sprints overview, where the mark shows today. But `ScreenRenderer`'s `renderList` builds each row as `{ id, title, subtitle }` and passes no `leading`, so a recipe-driven collection cannot carry one, which is every ticket, story and account list plus the sprints **All** tab. The workaround would be to put the glyph inside the title string, and that is the ONE shape §5 refuses (a pictograph inside a sentence), so nothing is done host-side and the mark is simply absent there, the WORD still carries the meaning on its own. Proper fix, one line: `renderList` reads an optional `leading` column off the row (as it already reads `fields[0]` for the title) and passes it to `List`. **Widened 19 Aug 2026: the missing slot costs a PICTURE, not only a glyph.** `accounts.list` carries `logoUrl` on every row, `apps.list` carries one, `members.list` carries `imageUrl`, `brand.list` carries the asset's own file — four recipe lists whose rows arrive from the door with a picture on them and render as two lines of text, while the host-composed lists over the SAME records (`AppTiles`, the apps panel, the team panel) draw it. One `leading` column closes both halves at once, and the host already has the component to put in it (`shared/web/record-mark.tsx`). **RE-READ 19 Aug 2026 against the INSTALLED library (0.9.1), and the reading is now MACHINE-CHECKED** (`web/test/type-mark-slots.test.ts`): the slot is still missing, and the half of this entry that says the fix is one line — `ListItem.leading`, `TabsTrigger.icon`, `CollectionConfig.emptyText`, `StatItem` — is asserted too, because that is the half that goes quietly false. A flag in prose rots in BOTH directions: the library ships the slot and nobody here notices, or the entry describes a function that has moved on. The day it lands the check turns RED and names the screen to feed, so this row can only shrink. | **SHIPPED in the library (2026-08-19, v0.11.0, `leading`)** — and WIRED here in the same commit. The check that held this row open now asserts the slot is FED rather than missing (`web/test/type-mark-slots.test.ts`), so this row can only be reopened by the wiring going away. |
| 17 | `notes` **takes no `id` and forwards no ARIA, so its `Field` label points at nothing** | none — the visible label still sits above the editor, so a sighted person reads it; a screen-reader user hears an unlabelled edit box | The library `Notes` renders a toolbar plus a `contentEditable` div and accepts only `defaultValue` / `onChange` / `placeholder` / `className`. Every long-text field on both front doors is now this editor inside a `Field` (R4's shared FormShell), and `Field` wires its `<Label htmlFor>` to an id that no element in `Notes` carries — so the association is dangling on ~15 fields. There is no host workaround: wrapping it in a `<div id>` does not help, because a `<label for>` only binds to a LABELABLE element and a div is not one. Proper fix, small: `Notes` accepts `id` and `aria-label` (or `aria-labelledby`) and puts them on the editable div, which should also carry `role="textbox"` and `aria-multiline`. A `disabled` prop would close the other half of the same gap — every form here passes `disabled={busy}` to its other inputs and cannot to this one, so the editor stays typable while a save is in flight. **RE-CHECKED 2026-09-02 against the pinned kit (v1.2.24), and this row is filed against the wrong component.** The kit's `components/notes/notes.tsx` is not an editor at all — it is a read-only stack of remark rows (`NoteItem[]`: author, initials, timestamp, highlight), so there is nothing there to give an `id` to and the drift probe that reads that file for `id?: string` is watching a component that will never grow one. The EDITOR moved app-side with the kit swap and lives at `shared/web/notes-editor/notes-editor.tsx`, where the defect is exactly as this row describes it and unchanged: the component takes `defaultValue` / `onChange` / `placeholder` / `className`, its `contentEditable` div carries no `id`, no `role="textbox"`, no `aria-multiline` and no `aria-label`, and there is still no `disabled`. Same bug, same ~15 fields, this repo's to fix — a `<label for>` still cannot bind to it. | **app-side, not started (re-filed 2026-09-02; was "flag for the library")** |
| 18 | `tabs` (`TabsView`) **resolves `icon` as a lucide NAME, so a type MARK cannot sit in a tab** | none — the tab shows its WORD alone | `TabsConfig.tabs[].icon` is typed `string` and rendered `<DynamicIcon name={t.icon as IconName} fallback={() => null} />` (`registry/primitives/tabs/tabs.tsx:144,215`), so anything that is not a lucide name draws nothing at all. The Tickets strip has a tab per TICKET TYPE, and a type is exactly the thing UI-CONVENTIONS §5 gives a mark to — the same glyph the ticket's own header band already shows. Putting it in the LABEL is the one shape §5 refuses (a pictograph inside a sentence), so the tabs go without. Proper fix, the same one-liner shape as #16: `icon` also accepts a `React.ReactNode`, and `TabsView` passes a node straight through to the trigger it already has a slot for (`TabsTrigger.icon` is `React.ReactNode` **already**, line 90 — only the config-driven wrapper narrows it to a name). **RE-READ 19 Aug 2026 against the INSTALLED library (0.9.1), and the reading is now MACHINE-CHECKED** (`web/test/type-mark-slots.test.ts`): the slot is still missing, and the half of this entry that says the fix is one line — `ListItem.leading`, `TabsTrigger.icon`, `CollectionConfig.emptyText`, `StatItem` — is asserted too, because that is the half that goes quietly false. A flag in prose rots in BOTH directions: the library ships the slot and nobody here notices, or the entry describes a function that has moved on. The day it lands the check turns RED and names the screen to feed, so this row can only shrink. | **SHIPPED in the library (2026-08-19, v0.11.0, `TabItem.icon`)** — and WIRED here in the same commit. The check that held this row open now asserts the slot is FED rather than missing (`web/test/type-mark-slots.test.ts`), so this row can only be reopened by the wiring going away. |
| 19 | `collection-frame` / `screen-renderer` **empty state is a bare string** | none — the host's OWN empty states carry a glyph (`screen-bits.tsx`, `pulse.tsx`); a RECIPE-driven one cannot | `CollectionConfig.emptyText` is a `string`, so an engine-rendered list with no rows shows one grey sentence in the middle of a card. Every empty state the host composes itself now leads with an `aria-hidden` lucide glyph, because a lone line of grey text reads as a screen that FAILED rather than one with nothing on it yet — and that is precisely the screen a brand-new team sees on every page. The recipe cannot say it. Proper fix: an optional `emptyIcon` (a lucide name, like the tab config) beside `emptyText`, defaulting to none so nothing changes for a caller that does not set one. **RE-READ 19 Aug 2026 against the INSTALLED library (0.9.1), and the reading is now MACHINE-CHECKED** (`web/test/type-mark-slots.test.ts`): the slot is still missing, and the half of this entry that says the fix is one line — `ListItem.leading`, `TabsTrigger.icon`, `CollectionConfig.emptyText`, `StatItem` — is asserted too, because that is the half that goes quietly false. A flag in prose rots in BOTH directions: the library ships the slot and nobody here notices, or the entry describes a function that has moved on. The day it lands the check turns RED and names the screen to feed, so this row can only shrink. | **SHIPPED in the library (2026-08-19, v0.12.0, `emptyIcon`)** — and WIRED here in the same commit: `listCollection` resolves the section's own `CONCEPT_ICON`, so the empty state and the nav rail can never name different concepts, and all fourteen list recipes feed one. The check that held this row open now asserts every recipe FEEDS the slot rather than that the slot is missing, and names the bare ones — so this row can only be reopened by a recipe losing its glyph. |
| 20 | `stat-grid` **has no glyph slot on a card** | none — the label carries the meaning alone | `StatItem` is `{ id, label, value, delta, trend }` (`structures/stat-grid/stat-grid.tsx`), and the only graphic on a card is the trend arrow. So the "big numbers" band on Home is the one main surface in the app with no icon vocabulary on it: the four cards are Open tickets, Work in hand, Admin due, Hours this week, each of which is a CONCEPT that already owns an icon in `CONCEPT_ICON`. Writing a glyph into `label` would be emoji in copy, which §5 forbids outright. Proper fix: an optional `icon` on `StatItem`, rendered `aria-hidden` in a leading slot beside the label — the same shape `list`'s `leading` already has. **RE-READ 19 Aug 2026 against the INSTALLED library (0.9.1), and the reading is now MACHINE-CHECKED** (`web/test/type-mark-slots.test.ts`): the slot is still missing, and the half of this entry that says the fix is one line — `ListItem.leading`, `TabsTrigger.icon`, `CollectionConfig.emptyText`, `StatItem` — is asserted too, because that is the half that goes quietly false. A flag in prose rots in BOTH directions: the library ships the slot and nobody here notices, or the entry describes a function that has moved on. The day it lands the check turns RED and names the screen to feed, so this row can only shrink. | **SHIPPED in the library (2026-08-19, v0.11.0, `StatItem.icon`)** — and WIRED here in the same commit. The check that held this row open now asserts the slot is FED rather than missing (`web/test/type-mark-slots.test.ts`), so this row can only be reopened by the wiring going away. |
| 21 | ~~A **flow / graph** shape, boxes joined by labelled arrows~~ | `web/components/agent-blocks.tsx` (`FlowBlock`, host-composed from a `div` and two lucide glyphs, not a temp fork) | The assistant and the knowledge base can now DRAW an answer, and one of the shapes they need is a process that BRANCHES, loops back, or has more than one way through, which is what the product's own process maps are (app → process → step). The library's nearest neighbours are both wrong for it and were checked before this was written: `StatusStepper` is a LIFECYCLE with a "you are here" stage, and the `RunSteps` collection is steps that RAN, with outcomes, so both would state something the model never said. `steps` covers the linear case already. What is missing is nodes + edges. The host version lays it out DOWN the page (nodes in the given order, the arrow joining two consecutive ones becomes the connector, every other arrow is drawn under its source naming the box it goes to) because the narrow host is ~400px and a box-and-line canvas at that width is either overlapping labels or something you drag sideways. A library version wants the same vertical behaviour as its narrow mode and could earn a real canvas above a breakpoint. | **SHIPPED in the library (v1.2.24, verified 2026-09-02) — and adopted here on 2026-08-25** — `shared/ui/components/flowchart/flowchart.tsx` is the kit's collection view 23, "nodes and arrows, branches where a decision splits it", and it was read against this row's ask rather than assumed to match it: a `FlowStep[]` taken top to bottom (`node` \| `decision` \| `branch`), a fork rail with a per-branch `chain` under each head, a `continues` flag that draws the WHOLE fork rejoining the trunk, `loopTo` for a way back (a dashed lane measured off the DOM after layout), the rotated decision node, and a vertical trunk rather than a canvas — nodes + edges, branching, loops, laid out down the page, which is the four things this row named. `web/components/process-flowchart.tsx` draws the product's own process maps with it (`app → process → step`, rendered by `web/components/process/steps-panel.tsx`); it replaced a hand-rolled SVG on 2026-08-25 and has fed the branch chains since `branchOf` landed on 26 Aug. **`FlowBlock` in `web/components/agent-blocks.tsx` is NOT dead code, and is deliberately not deleted here.** It is the renderer for the assistant's `flow` block kind in `BLOCK_RENDERERS`, which R9's mapped type over the block union makes compulsory, and it still says three things the kit's component does not: an ARBITRARY edge list (every edge drawn exactly once, including a jump that is not a tree edge — the kit takes a rank-and-fork TREE, so a graph that is not one cannot be expressed as `FlowStep[]` without dropping edges); a LABEL on an ordinary connector (the kit's `condition` is the line above a BRANCH node and is drawn nowhere else, and `loopTo` draws a line with no words on it); and an all-vertical layout with no side-by-side branch row — the kit lays branches out in columns and scrolls sideways below `branchMinWidth`, which is the one behaviour a ~400px chat panel cannot have. Whether the assistant's `flow` blocks should be re-modelled onto `FlowStep[]` — losing arbitrary edges, gaining the kit's drawing — is a real question and a separate one; what this row asked for is shipped. |
| 22 | `DialogContent` / `Sheet` **draw one shape each, so a screen that needs four presentations cannot use either** | `shared/web/screen-engine/screen-renderer.tsx` builds its own on the kit's own Radix primitive, pinned in `UI_PACKAGE_EXEMPT` (R39) | `ScreenLayer` renders every engine screen as a layer in one of four presentations — `responsive` (a bottom sheet on a phone, a centred card on a desktop), `overlay`, `sheet` and `fullscreen` — and the kit exposes neither: `DialogContent` is centred only, and `Sheet` takes a fixed `side` with no responsive switch and no fullscreen. So the ONE screen renderer behind every recipe-driven screen in the app is also the one file that reaches past the kit to `@radix-ui/react-dialog`. It uses the very primitive the kit's own Dialog and Sheet are built on, so the two can never disagree about focus trapping or scroll locking — but the SHAPE is decided app-side, which is the thing R39 exists to end. Proper fix, and it is one prop: `presentation?: "responsive" | "overlay" | "sheet" | "fullscreen"` on the kit's `DialogContent`, defaulting to today's centred behaviour so no existing caller changes. The day it ships, R39's pin turns RED because the import is gone, which is how this row closes itself. | **flag for the kit** |
| 30 | `calendar-view` **draws records nobody can open, and hides some behind dead text** | `web/components/record-calendar.tsx` (`RecordCalendar`, host-composed from `Button` + `List` + `ToggleGroup` + `Dialog`, not a temp fork) | `CalendarView` renders each event as a plain `<div key={idx}>` and exposes **no** click prop of any kind — its whole config is `dateField` / `titleField` / `accentField` / `weekStartsOn` / `maxPerDay`, and the only `onClick`s in the file are Today / previous / next. So a task on the grid is a PICTURE of a task: no keyboard focus, no pointer target, no way to its detail screen. `+N more` is a second plain `<div>`, which is worse than silence — it names six records and offers no gesture that reaches any of them. There is no host workaround: `titleField` is read as `String(ev[field])`, so a node cannot be passed in, and delegating a click from a wrapper would mean identifying the record from the DOM. Reported by the owner, 18 Aug 2026 ("all detail screens should be clickable and accessible if there are records displaying on the calendar"). **UPDATE, 30 Aug 2026 — THE BLOCKING HALF IS FIXED UPSTREAM.** Checked against the kit as vendored today (`shared/ui/VERSION.json` tag `v1.2.9`, synced 29 Aug): `CalendarViewProps` now carries `onSelectDay`, `onSelectEvent` and `onSelectItem`, every enabled cell/chip/row becomes a real `<button>` when its handler is given, and `CalendarEvent.label` / `CalendarAgendaItem.title` are typed `React.ReactNode` rather than forced through `String(ev[field])` — the exact gap this row asked for. **NOT DONE: rebuilding `record-calendar.tsx` on it.** That file is 452 lines and backs three screens (`tasks-screen.tsx`, `meetings-screen.tsx`, `sprints-screen.tsx`); swapping its internals for the kit's now-clickable `CalendarView` is a real, bounded task but not a same-sitting one, and a rebuild attempted without dedicated testing time risks three screens at once for one census tick. Recorded here so the next pass that opens this file finds an accurate gap, not a stale one: **the fix landed, the adoption is the open work now, not a wait on the library.** The two props still missing — `onMonthChange?: (month: Date) => void` (so a host over a PAGED collection can say "this month isn't loaded yet," R14) and a `mode?: "month" \| "agenda"` toggle — are real but no longer BLOCKING; `record-calendar.tsx` already carries its own agenda-mode toggle by hand and could keep doing so on top of the kit's grid. **DONE, 31 Aug 2026 — THE ADOPTION LANDED.** `record-calendar.tsx` is now composed from the kit's `CalendarView` (month grid) and `Agenda` (day list) directly, both reached for real (R46). One residual gap the rebuild found rather than closed: the kit's own "+N more" (`formatMoreEvents`) is still text with no click of any kind, so the host still cannot lean on it — it composes the overflow as one more `CalendarEvent` per day instead, whose click opens the day dialog. That line stays open for the library; everything else in this row is closed. | **closed app-side, 31 Aug 2026 — the "+N more" click-through is still the library's to add** |

| 31 | `choice` **is the library's searchable picker and it cannot ask a door** | `web/components/record-picker.tsx` (host-composed from `Popover` + `Command`, not a temp fork) | The library ALREADY ships the searchable combobox: `Choice` (`registry/primitives/choice/choice.tsx`), `display: "dropdown"` + `searchable: true`, built from `Popover` + `Command` (cmdk) with a `modal` prop for living inside a dialog. It is the right control for every picker in this app over a BOUNDED list, and nothing here beats it. What it cannot do is ask a SERVER: it owns its search text in local state (`const [query, setQuery]`), never emits it, and hands it to cmdk's own in-memory filter — so on a `GROWING_COLLECTIONS` list (R14) it would search the loaded page and answer "no options found" about everything past the cursor, confidently. That is precisely the defect this repo already hit on the accounts picker and the one `PagedFind` exists for on list screens. So the host composed the same two primitives with the search wired to the door instead. Proper fix, the same shape `CollectionFrame` already has (see #15): an optional `onSearch?: (query: string) => void` + `serverSide?: boolean` on `ChoiceConfig`, which turns cmdk's filter off (`<Command shouldFilter={false}>`) and emits the debounced query, plus a `loading?: boolean` and an `emptyText` that can differ while a search is in flight (saying "nothing matched" before the door has answered is the same lie in a smaller box). With that, `record-picker.tsx` collapses onto `Choice` and this row closes. NOT BLOCKING: the composition works today and uses only shipped primitives. **RE-CHECKED 2026-09-02 against the pinned kit (v1.2.24): the gap is still open and the component this row names is no longer the one it describes.** The kit's `components/choice/choice.tsx` is now the selection ROW — a mark (`Checkbox` / `RadioGroupItem` / `Switch`) with the words beside it and the whole row clickable — with no `display`, no `searchable`, no cmdk inside it and therefore nothing to hand an `onSearch` to; the drift probe that reads that file for `onSearch?:` is watching the wrong component. What the kit ships for this shape is the raw pair, `components/command` + `components/popover`, which is precisely what `web/components/record-picker.tsx` already composes with its search wired to the door. So the ask survives — a searchable picker that can ask a SERVER rather than filtering the loaded page — but it is now "the kit has no server-searchable combobox at all", not "`Choice` cannot emit its query". | **flag for the library** |

| 32 | `collection` **sorts by the rendered TEXT, and a `data-table` header sorts NOTHING** | `shared/web/format.ts` `formatDateSortable` (the values that get compared are spelled so lexical order is real order) + `web/components/paged-find.tsx` (a door-side sort control beside the search box) + `web/components/record-table.tsx` (the table itself, host-composed from `CollectionFrame` + the `Table` primitives, not a temp fork) | TWO gaps in one row, because they are one control. **(a) The comparison.** `selectRows` step 4 is `typeof av === "number" && typeof bv === "number" ? av - bv : String(av).localeCompare(String(bv))` (`lib/collection.ts:105`), so a column holding a formatted date ("14 Apr 2025"), a duration, money as text or a padded number sorts ALPHABETICALLY — April before January — and does it silently, because the rows move and the control looks like it worked. Proper fix: a per-column `sortType` (`"text" \| "number" \| "date"`), or an optional `sortKey?: (row) => string \| number` on `DataTableColumn` so a column can DISPLAY one thing and COMPARE another. `DataTable` already knows each column's `type` (`"text" \| "number" \| "badge" \| "date"`) and simply does not use it for the compare, so most of the answer is already declared. Host side is done for the three columns that were wrong, at the cost of showing "2026-04-14" where a warm date belongs. **(b) The coverage — and the sentence above it was WRONG, which is the point.** This row originally said `DataTable` "feeds that choice into the frame's in-memory pipeline", so a header ordered the loaded page. It does not. **`DataTable`'s header sorts nothing at all**, and did not on either table: it keeps the choice in its own state and passes it down as `CollectionFrame`'s `config.sortBy`, and the frame seeds its sort from that prop ONCE at mount (`collection-frame.tsx:83`, `React.useState(config.sortBy)`) and orders by its own state ever after. So the header wrote to a value nobody reads, on every column, while its ARROW — the header's own state — moved correctly. Reported on the deployed build within hours of this row being written. `screen-renderer` hard-codes `sortable: true` on every column of a `display: "table"` recipe (`screen-renderer.tsx:542`), so there is no host workaround through the engine: the affordance cannot be turned off from here and cannot be made to work from here. **The change, exactly:** the frame keeps the prop and the state in step (`React.useEffect(() => { setSortBy(config.sortBy); setSortDir(config.sortDir) }, [config.sortBy, config.sortDir])`, the same two lines `SearchInput` already runs for its `value`) — that alone makes every existing `DataTable` header work. Then, for the paged case, `DataTable` accepts `serverSide` + `onSortChange` and `ScreenRenderer` forwards them (#15's sentence, said about sorting), so a header can ask the door instead of the array. Host side is done and does not wait on either: `web/components/record-table.tsx` composes `CollectionFrame` with headers the host owns, ordering a bounded collection in the browser and a paged one at its door. **NOT BLOCKING**, and it retires the moment the frame syncs — `web/test/table-header-sorts.test.tsx` watches the library for exactly that line and goes red when it changes. **RECONCILED 2026-09-02 against the pinned kit (v1.2.24), and the three halves have come apart — read them one at a time, because `npm run kit:drift` reports this row as one thing and closes it on the first half alone.** **(b) THE COVERAGE IS SHIPPED.** `shared/ui/components/data-table/data-table.tsx` now takes controlled `sortKey` / `sortDirection` / `onSortChange` (a column's `key` IS the key handed back), falls back to `defaultSortKey` / `defaultSortDirection` when the caller controls neither, and drives the header button and the `SortControl` above the table off that one state — "sort is one state with two doors", in the file's own words. So a header CAN drive a door now, which is exactly the seam this row asked for. **(a) THE COMPARISON IS NOT SHIPPED, AND THE ASK HAS MOVED.** `DataTableColumn` is `key` / `header` / `cell` / `sortable` / `align` / `width` / `headClassName` / `cellClassName` / `headerLabel` — there is no `sortType` and no `sortKey?: (row) => string \| number`. **`sortKey?: string` on the COMPONENT is the controlled-sort key (which column is currently active), not the per-column comparator this row asked for**, and reading the drift tool's `onSortChange?:` hit as both halves is how this row gets closed while half of it is still true. It is also no longer a compare the kit could do: this `DataTable` orders nothing at all — it reports the chosen key and direction and renders `rows` exactly as handed to it — so a comparator on a column would have no compare step in the kit to live in. The comparison this row is about now runs app-side, in the engine: `shared/web/screen-engine/collection.ts` `selectRows` step 4 is still `typeof av === "number" && typeof bv === "number" ? av - bv : String(av).localeCompare(String(bv))`, the same line one directory over, so a formatted date, a duration or money as text still sorts alphabetically there. The remaining work is a per-column sort type on the engine's own `CollectionConfig`, not a prop request to the kit. (The host table already carries the better comparator for its own columns: `web/components/record-table.tsx`'s `compare` is numeric-aware with blanks last, in both directions.) **THE FRAME HALF IS STILL TRUE, and its one-line fix no longer is.** `shared/web/screen-engine/collection-frame.tsx` still seeds its order ONCE at mount and never re-reads the prop; the `React.useState(config.sortBy)` this row named became `useRemembered(slot, () => ({ …, sortBy: config.sortBy, sortDir: config.sortDir }))` on 27 Aug, which is the same seam under a different name. But the prescribed `React.useEffect(prop → state)` would now be wrong on its own: that state is the NAV MEMORY's, so a blind sync overwrites the order she chose before she left the screen. Whatever lands has to reconcile the two — a genuine config change wins, a return to a remembered question does not — which is why this half keeps no one-line prescription any more. Host side is unchanged and still does not wait on any of it; `web/test/table-header-sorts.test.tsx` is green as of this pass, INCLUDING its rot-check that the frame still seeds once, so the register and the test agree. (One stale sentence left alone deliberately: `record-table.tsx`'s header comment still describes the OLD library's mechanism — a `DataTable` pushing its choice down as `CollectionFrame`'s `config.sortBy` — which the kit's `DataTable` no longer does. The workaround it explains is unaffected; the paragraph wants a re-write the day the frame is fixed and that file collapses.) | **(b) SHIPPED in the library (v1.2.24, 2026-09-02)** · **(a) still open, and app-side now** — a per-column sort type belongs on the engine's `CollectionConfig`/`selectRows`, not on `DataTableColumn`, because the kit's table does no comparing · **the frame's prop-to-state sync still open, app-side** |
| 23 | The kit's **`AuthShell` cannot be imported under Next at all**, so the client portal's front door is drawn by hand from it | `web-portal/components/portal-shell.tsx` draws the auth shell class-for-class off the kit's `AuthShell`, with `web-portal/components/auth-artwork.tsx` beside it; the reason and a deletion date are in the file header | Not a styling gap — a MODULE-GRAPH one. `AuthShell` imports `AuthPhotograph` from `compositions/templates/sign-in` at module scope, and that file does not compile under Next (`sign-in.tsx(208,7): Type 'StaticImageData' is not assignable to type 'string | Blob | undefined'`), because Vite evaluates an asset import to a URL string and Next to a `StaticImageData` object. TypeScript reports it on every importer whether the photograph renders or not, so passing your own artwork through `media` does not save you: four compositions (`screens/sign-in`, `screens/splash`, `templates/rail`, `templates/sign-in`) are UNIMPORTABLE rather than merely unstyled. The portal lane's answer is the right one for a blocked dependency and the wrong one to leave permanent: it read the ARRANGEMENT off the composition and drew the same thing, which is duplication with an expiry. **An expiry nobody holds is not an expiry, which is why this row exists and the header comment is not enough** — the comment is read by whoever opens the file, who already knows; the row is read by whoever closes the upstream fix, who has to act. UPSTREAM FIX BUILT AND PROVEN: `Kwapso/kwapso-ui-ux` branch `fix/assets-resolve-to-urls` (df87091), one seam `lib/asset-url.ts`, green under Vite AND under Next; awaiting the design lead. **WHEN THAT MERGES AND A TAG IS CUT: `portal-shell.tsx`'s hand-drawn auth shell and `auth-artwork.tsx` are DELETED and replaced by the kit's `AuthShell`.** Not revisited — deleted. **UPDATE, 2026-08-29 — THE MERGE ALREADY HAPPENED.** Checked empirically (a throwaway probe import + `tsc --noEmit`, not a read of the tag number) against the copy currently vendored in `shared/ui/`: `templates/sign-in.tsx` and `templates/rail.tsx` (both, via `components/brand/brand.tsx`) import `assetUrl` from `lib/asset-url.ts` and compile cleanly under this app's Next toolchain. `screens/sign-in` and `screens/splash` were not re-probed but sit on the same fix. **`portal-shell.tsx`'s hand-drawn shell and `auth-artwork.tsx` are candidates for the deletion this row already asked for** — nobody has done it yet, and this row is what says so. Whether the kit's `SignIn` (row 2, `auth-card`) now also satisfies that row's full feature list (legal links are not obviously present) was NOT checked past compiling — a separate pass should confirm before deleting anything, but the compile blocker itself is gone. **UPDATE, 2026-08-29 (later the same day) — HALF THE DELETION IS DONE.** `web-portal/components/auth-artwork.tsx` is deleted; `portal-shell.tsx`'s `PortalDoor` now renders the kit's own `Logotype` (`components/brand/brand.tsx`) and `AuthPhotograph` (`compositions/templates/sign-in.tsx`) directly, verified live at phone/tablet/desktop and both themes — same photograph (the client's own placeholder, not a kit stock image; confirmed by reading the asset path, not assumed), same cut-swap behaviour, now from the kit's token rule instead of a hand-copied one. `npm run check` green. **What is NOT done**: `PortalDoor` itself is still hand-drawn rather than `<AuthShell>{children}</AuthShell>` — `AuthShell` (`screens/sign-in.tsx`) has no `children` slot, its body is one of ch27.17-19's four fixed registers, none of which is a login form, and `COMPOSITION-MISMATCHES.md`'s `screens/sign-in.tsx` entry has the full reasoning. The compile blocker this row tracked is gone for the brand/photograph half; the shell-structure question is a separate, already-recorded finding, not still open work. | **flag for the kit — fix on a branch, awaiting merge** → **fix has landed; brand/photograph half deleted 2026-08-29; shell-structure half recorded as a mismatch, not a re-check**
| 33 | Sorting the collections that are neither a recipe list nor a paged list SCREEN | none — they simply have no control | Two sets, named rather than left silent (SEARCH.md § *The third question* has the whole census). **(a) The ~30 record PANELS** (`work-panels.tsx`, `account-detail-panels.tsx`, `contact-panels.tsx`, `time-panel.tsx`, the rate cards, attachments, certificates): each is a hand-built `<ul>` of rows inside one record, and five of them PAGE (a story's work logs, an app's tickets/meetings/maps, an account's stories). A sort on those has to go to the door like every other paged one, which means their doors need the same `SortMenu` treatment and their panels need a control — a real piece of work, and worth doing when somebody asks for it rather than because the list exists. The bounded ones are short enough that a control would be furniture. **(b) The CLIENT PORTAL**, which has no recipe engine at all — every collection there is hand-composed, and only one of them wants ordering: the tickets list, which pages and is newest-first. `/api/content/help` already takes `sort`/`dir`, so the portal's half is a control and a query parameter, not a door. Neither is blocked on the library. | **app-side, not started** |
| 24 | A collection that GROUPS its rows under a heading | `web/components/contacts-by-company.tsx` (`ContactsByCompany`, host-composed from `ScreenRenderer` + a `<section>` per group, not a temp fork) | The accounts screen's **Contacts** tab shows every contact grouped under the company each one sits at (the owner, 18 Aug 2026). No library collection groups: `List` renders a flat array and exposes no section/header seam, `CollectionFrame` searches, filters and sorts an array but never partitions one, and `ScreenRenderer`'s `renderList` maps rows straight onto whichever collection the recipe names. The only grouped shape in the library is `kanban`, and it is the wrong one — a kanban column is a STATE a card moves between, with drag between columns as its whole point, and a contact does not move from Bergman to Delaval by being dragged. So the host renders one `ScreenRenderer` per group over the SAME recipe, which keeps a contact row identical to the row the other two tabs draw (same fields, same click-through, same gating) and leaves only the arrangement to us — the same composition `apps-screen.tsx` already makes to group apps by stage and `selectable-screen.tsx` to group dropdown values by type, which is three screens on one pattern and the reason it is written down here. **The change, exactly:** an optional `groupBy` on `CollectionConfig` — a row field, or a `(row) => string` — plus an optional `groupLabel?: (key: string) => React.ReactNode`, with `CollectionFrame` partitioning AFTER its own search/filter/sort so a grouped list still narrows as one collection; groups render in the order the labels sort unless the caller passes `groupOrder`. Nothing changes for a caller that sets none of them. **NOT BLOCKING:** the composition works today and is built only from shipped primitives. **One thing a library version still could not fix:** on a PAGED collection (R14) the grouping can only see the rows loaded, so a group is complete only once the cursor is spent — that is a door question (an ordering by the group key), not a library one, and `contacts-by-company.tsx`'s header says which of the two arrangements this screen took and why. **RE-CHECKED 2026-09-02 against the pinned kit (v1.2.24): still open, and half of it is ours.** No kit part groups: `components/list/list.tsx` renders a flat `ListRow[]` with no section or header seam, and the kit's `components/collection-frame/collection-frame.tsx` is a different LAYER from the one this row is about (eyebrow / heading / count / tabs / slots — it never partitions, filters or sorts an array, so the drift probe reading it for `groupBy` is reading a file that could not hold one). The `CollectionConfig` this row proposes putting `groupBy` on is the ENGINE's, `shared/web/screen-engine/config.ts`, and the partitioning would go in `shared/web/screen-engine/collection-frame.tsx` after its own search/filter/sort — app-side work, unblocked, whether or not the kit ever grows a grouped list. | **flag for the library** |
| 25 | The library **composes English of its own, and offers no seam to translate it** | `shared/web/field.tsx` (a host wrapper that translates `config.label` / `config.helpText` before handing the config to the library `Field` — composition, not a fork) + R33's import ban, which is what makes the wrapper unavoidable | This app ships in every language `LANGUAGES` declares, keyed by the English sentence (`shared/i18n.ts`), and every string the LIBRARY writes for itself is outside that catalogue and therefore English forever. Four of them, all in code a host cannot reach: `validateField` (`lib/config.ts:156-161`) returns `` `${label} is required.` ``, `Must be at least N characters.`, `Must be at most N characters.` and `` `${label} is not in the expected format.` ``; `defaultCollectionConfig.searchPlaceholder` is `"Search…"`; and the `Form` collection defaults `submitLabel: "Submit"`. None is a bug in one language. **What the library version needs, exactly:** an optional `translate?: (english: string) => string` on the config-driven entry points — or one `UIStringsProvider` at the root, which is smaller and reaches everything at once — defaulting to identity, so nothing changes for a caller that passes none. The composed messages then read `t("{field} is required.", { field })` rather than concatenating a translated label onto an English tail, because a translator has to be able to reorder those. **Host side is done for the part a host CAN own:** the field's own words are translated on the way in, so every label and help text in both front doors is in the reader's language today. **NOT BLOCKING:** `validateField` is unused here (each form states its own readiness), and the two defaults are overridden at every call site. | **SHIPPED in the library (2026-08-19, v0.11.0, `avatarShape`)** — and WIRED here in the same commit. The check that held this row open now asserts the slot is FED rather than missing (`web/test/type-mark-slots.test.ts`), so this row can only be reopened by the wiring going away. |

| 34 | `record-detail` **draws a circular avatar on every record, whether or not the record is a person or has a picture at all** | none — the circle is drawn by the library, above anything the host passes | `RecordDetail` renders the avatar on `hasAvatar = Boolean(avatarSrc \|\| avatarFallback)` and `ScreenRenderer` always supplies `avatarFallback={initials(title)}`, whose own floor is `"?"` — so the flag is true for every recipe detail there will ever be. Three of the five in this app have no image concept whatever (`invites.detail`, `brand.detail`, `purposes.detail`), and each opens with two letters of its own title in a circle: an invite is an email address, a brand asset is a file, a meeting purpose is a sentence, and none of the three is a face. It is also the wrong SHAPE for the two that do carry a picture — this app's rule (`shared/web/record-mark.tsx`) is that a person is a circle and a company, an app or an asset is a rounded square shown `object-contain`, because a wordmark cropped to a circle is a wordmark nobody can read. There is no host workaround: `RecordDetailConfig` exposes no way to suppress the avatar or to change its shape, and the fallback is computed inside `screen-renderer` from the title. **The change, exactly:** `avatarFallback` becomes optional and `ScreenRenderer` passes it only when the recipe's `header` declares an `avatar`, so a detail with no picture concept draws none; plus an optional `avatarShape?: "circle" \| "square"` on `RecordDetailConfig` (defaulting to `circle`, so nothing changes for a caller that omits it). With both, the five recipe details render the same mark as the eleven bespoke ones and this row closes. | **SHIPPED in the library (2026-08-19, v0.11.0, `avatarShape`)** — and WIRED here in the same commit. The check that held this row open now asserts the slot is FED rather than missing (`web/test/type-mark-slots.test.ts`), so this row can only be reopened by the wiring going away. |

| 26 | ~~`stopwatch` has no label slot for what it is timing, so it cannot tell two running clocks apart~~ |, | `web/components/timer-bar.tsx`'s `TimerBar` hand-drew its own near-identical pill for exactly one reason — no way to say WHICH record a running clock belonged to, and `.plans/BUILD-1` §5 puts one in the header of every screen while the app lets more than one run at once. | **SHIPPED in the library (v1.2.7, `leading`)** — and WIRED here in the same commit. `TimerBar` now renders the real `Stopwatch` per timer (`elapsed` controlled in ms off the same wall-clock arithmetic as before, `onRunningChange` wired to the existing stop door, `leading` carrying the clickable record name that used to need a whole hand-drawn pill to hold). The upstream fix also closed a second bug in passing: `StopwatchProps` had advertised `children` in its type while the render silently discarded them — `children` is now omitted from the interface so the type matches what the component does. |
| 27 | ~~`filter-bar`'s **add-a-filter chip is not the same box as a `SelectTrigger`**, so the three toolbar pills are not one family~~ | `shared/web/screen-engine/filter-bar.tsx` + the toolbar hosts, via a `[&_[data-slot=filter-bar-add]]:` hook (a documented seam the kit publishes, not a fork) | Client ruling, 2026-09-02: "the filter button-pill it's still differnet than the other 2. fix and uniform it". Height, radius and resting fill already matched. Three properties did not, measured off the kit's own source: inline padding 12 (`px-3`) against `SelectTrigger`'s 18 (`--space-4h`); type step 12 (`--text-badge`, leading 1) against 14 (`--text-sm`, leading 1.45); weight inherited (300/400) against 500. A fourth was ours and is already deleted rather than corrected — an `--accent` hover override that was right when the kit drew that slot `bg-transparent` and read as dimming once the slot became an opaque pill. **The fix belongs in `CHIP_ADD`**: the chip already borrows `SelectTrigger`'s height and fill, and should borrow these three the same way, so the pill is born matching instead of being corrected at four call sites. `web/test/filter-row-is-the-kits.test.tsx` derives the assertion from the kit's source at BOTH ends and goes red the day it adopts them, which is the signal to delete the override here. | **SHIPPED in the library (v1.2.27, 2026-09-02), ADOPTED HERE 2026-09-03.** `CHIP_ADD` now carries `SelectTrigger`'s inline padding (18), type step (14/`--text-sm`) and weight (500) itself, so the pill is born matching instead of being corrected at four call sites. Checked before it moved, rather than assumed: the kit's own v1.2.20 entry names the four properties it carried onto the chips and these three are not among them, and CH11's small step rules a CHIP, not the "+ filter" slot standing in a toolbar — a gap, not a decision. The app's `FILTER_PILL_MATCHES_THE_OTHER_TWO` override is deleted, along with a stale `--accent` hover that dated from when the kit drew that slot transparent and read as dimming once it became an opaque pill. |
| 28 | ~~`filter-bar` **ships no compact facet, and the absence cost a real feature**~~ | `shared/web/screen-engine/filter-bar.tsx`'s own `SelectFacet`, composed from the kit's `Select` (not a fork — the kit has no part to fork) | The kit offers `SearchableFacet`, which is by design always-expanded (a heading, a search field, then every option as a checkbox row), and `RangeFacet`, which is numbers only. A facet that should read as one short field saying "Any client" — which is what the client's own artifact draws and what she approved on 2026-09-02 — exists nowhere, so it was composed here out of `Select`. `FacetLabel` is private to the kit's file, so its two classes are duplicated here too. **The cost is concrete and it is a regression the client has not yet ruled on:** `web/components/wave-finder.tsx` filters 131 clients, and `SearchableFacet` carried a per-facet search box that the compact field does not, so those 131 are now a plain scroll. What both surfaces want is one part — a compact trigger over a *searchable* list — which would also close the duplication and hand Waves its search back. Sibling of #17b, which asks for the same shape one layer up (a picker that can ask a door). | **SHIPPED in the library (v1.2.27, 2026-09-02), ADOPTED HERE 2026-09-03.** `CompactFacet` is a trigger over an optionally searchable list, and `FacetLabel` is exported rather than hand-copied. The app's local `SelectFacet` and its `ANY_VALUE` sentinel (~75 lines) are deleted. **Waves has its search back** — `searchable` turns on above eight options, the kit's own threshold, so the 131-client facet is searchable again instead of a plain scroll. |
| 29 | ~~`collection-frame` **has no slot between its toolbar and its rows**, so a panel that must push the collection down has nowhere to render~~ | `shared/web/screen-engine/filter-bar.tsx`'s `FilterPanelProvider` / `FilterPanelOutlet` + a `createPortal` (not a fork; the frame cannot be hand-edited under R39) | Client ruling, 2026-09-02: "the expanded toolbar shoudl not be an overlay, but literaly expand the space". Every host that owns its own pill track solved this by wrapping the track and the panel in one column — ordinary flow, no portal. The one host that cannot is the `useKitPanel` branch, because `KitCollectionFrame` renders its toolbar and its rows with nothing between them and no slot to reach; so the app wraps the whole frame in a context provider and portals the panel into the top of the frame's BODY, which lands it in the right place but through machinery a slot would make unnecessary. **The fix is a node rendered between the toolbar and the body.** While there: the frame wraps its own `filters` slot in a `flex min-w-0 flex-wrap items-center gap-2` div carrying no `position`, so anything a host puts in that slot has nothing to anchor against — that wrapper should either be `relative` or go. | **SHIPPED in the library (v1.2.27, 2026-09-02), ADOPTED HERE 2026-09-03.** The frame takes a `toolbarPanel` node between its toolbar and its body, so the app's `PanelSlot` context, `FilterPanelProvider`, `FilterPanelOutlet`, `FilterPanelColumn` and their `createPortal` (~62 lines) are gone. `FilterBar` became the `useFilterBar` hook returning the pill and the panel as two values, which is what let each be placed directly instead of one component reaching across the tree. The `filters` wrapper deliberately did NOT gain `relative`: that would publish an anchor for the overlay shape the client refused, in a file no call site can edit. |
### How to close a row

Closing one of these, now that the kit is pinned: build the component upstream in
`Kwapso/kwapso-ui-ux` (under `components/`, or `foundations/` for a token, glyph or motion utility), tag it, pull the tag with
`scripts/sync-design.mjs`, then swap the host import to `@shared/ui/…`, delete the
temp file, and update this table in the same commit here. There is no npm install
step — the instruction here used to be "re-run
`npm install github:Kwapso/kwapso_ui`", and that command no longer exists; a
hand-build under `shared/ui/` no longer exists either, the hash guard refuses it.

**One placeholder is not in `web/components/temp/`, and it needs saying.**
`code-input` moved to `shared/web/code-input.tsx` when the client portal shipped:
both front ends sign in with it, and a file two workspaces import cannot live
inside one of them. So it now sits in the most permanent folder in the repo with
none of the folder's "this is a stand-in" signal, the only thing marking it as
temporary is the comment at the top of the file and this row. It is still #1 on
this list, and it still gets deleted the day the library ships the primitive.
`auth-card` (#2) shipped 2026-08-29 and is out of `web/components/temp/`
entirely — see row 2.

## Composition candidates (built from primitives here; could graduate to the library)

These work today (composed from existing primitives. NOT blocking), but the
pattern repeats across apps, so they're good candidates to become library
collections later. Not urgent.

| Pattern | Lives here | Why it could move to the library |
|---|---|---|
| `app-bar`, top bar with a team switcher + profile menu | `web/components/app-shell.tsx` | Every multi-tenant app wants the same switcher + profile pattern; config-driven (brand, switcher items, menu items) it'd be reused everywhere |
| `pwa-install-prompt`, installable-app pop-up (native `beforeinstallprompt` + iOS "Share → Add to Home Screen" walkthrough, suppress-if-installed, cooldown) | `web/components/install-prompt.tsx` (+ `web/lib/pwa.ts`) | Every PWA on this base wants the same install nudge; config-driven (copy, trigger cadence) it'd drop into any app. Built from the library `Sheet` + `Button` today. |
