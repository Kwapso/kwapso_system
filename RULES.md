# RULES.md, the Laws of the Base (machine-checked)

These are the **laws** every part of the Kwapso System (and anything built on top of it) must
obey. They are not style suggestions, each one is enforced by a real test, so a
change that breaks a law turns the build **red**. This is how an agreed rule
actually sticks instead of quietly slipping over time.

The single source of truth is **`shared/rules/registry.ts`** (the laws as data).
This document is the human-readable twin; a check (`registry-integrity`, in
`web/test/rules.test.ts`) asserts this table lists **exactly** the law ids in the
registry, so the doc and the code can never drift. **You cannot add a law without
its check, and you cannot add a check without its law.**

Deny-lists (the reviewed exceptions for each law) live as DATA in the registry, so
every exception is a visible, conscious line. Never a silent bypass.

## The client portal, and the exceptions it earns

`web-portal/` is the second front end (the client portal; `web/` is the agency app).
It obeys the same laws with three recorded differences, and they are written here
because an exception nobody can find is a bypass with better manners:

- **R2 on the portal. `PORTAL_ACTIVITY_EXEMPT`** (in the registry). Every record
  detail in the base carries Overview + Activity, because a record's history is what
  makes a shared workspace trustworthy. On the client's side that same feed is a
  disclosure: its rows name the staff who moved a ticket or edited an account, and the
  portal shows work status but never who inside the agency is doing it (SCOPE ch.06).
  Two components are listed, the ticket screen and the company screen, each with the
  sentence for why. The activity door is not on the portal gateway's surface at all,
  so this is not a hidden tab; it is a door that was never opened.
  `web-portal/test/rules.test.ts` fails if a listed component grows an activity feed,
  and if a listed component no longer exists.
- **The account fence. `PORTAL_VISIBLE_READS`** (in the registry). Every read a
  client can reach names the fence its lib function must carry.
  `web-portal/test/portal-fence.test.ts` walks the portal gateway's own door table
  through to the function behind each door and demands that function touch the
  caller's stamp. FUNCTION level, not file level, because this list once said a file
  was fenced while one of its readers was not.
- **`workers/portal-gateway/` has no `gating-seam` and no `publish-seam` suite**, a
  reasoned exemption, not an oversight. That worker owns no writes of its own and no
  tables: every `/api` request it accepts is forwarded, unchanged, to a door that is
  already gated (R10) and already publishes (R1) in the worker that owns it. There is
  nothing there for either seam test to read. What it *does* own is which doors exist
  at all, and that is checked instead by `workers/portal-gateway/test/portal-door.test.ts`,
  which derives the agency's whole `/api` surface off `web/lib/api/` and asserts every
  door the portal does not name returns a 404. A door-list worker is held to a
  door-list check.

| ID | Dimension | Law (plain English) | Check (test id) | Status |
|----|-----------|---------------------|-----------------|--------|
| R1 | arch | Every mutation route publishes a live change ping (so screens stay live). | `publish-seam` (per-worker tests: tenancy, content, data-ops; auth's two user-channel publishes and mcp's caller-private token rows are the reviewed, untested exceptions. CACHING rule 5) | enforced |
| R2 | ui | **Every record-detail screen exposes Overview + Activity tabs**, through the library `TabsView` and the one `<ActivityPanel>` (which is itself a pageable `ActivityFeed`, R14). **And WHICH screens those are is DERIVED, never hand-listed.** A component under `web/components` named `*-detail.tsx`, or one that renders an `<ActivityPanel>` — two independent signals, because a record detail can arrive under another name, and because neither signal is one of the two obligations, so nothing here is circular: a file caught by NAME is held to both with neither assumed. Comments are stripped before the match (CONVENTIONS.md), the census carries a blindness tripwire, and `RECORD_DETAIL_NOT` is the reasoned residue, rot-checked so it can only shrink. *Earned by:* the registry holding an INCLUSION list, `RECORD_DETAIL_COMPONENTS`, so R2 and R8 walked exactly the screens somebody had remembered to type into it. It opened twice. `app-detail` and `process-detail` were added on 17 Aug 2026 after a tester found faults on screens no law had ever read; `sprint-detail` and `story-detail` were found missing on 18 Aug, having shipped tabs and an Activity panel that neither law had ever looked at. Nothing was red either time, because a screen no law walks looks exactly like a screen that passes. A law that enumerates its subject from a hand-kept list has a hole by construction. | `record-detail-tabs` | enforced |
| R3 | ui | Collection tab strips use the library TabsView (icon + count badge), no hand-rolled button toggles. | `no-handrolled-toggles` | enforced |
| R4 | ui | Every form/dialog renders through the shared FormShell (title+subtitle · separator · fields · separator · action). | `forms-use-formshell` | enforced |
| R5 | arch | Record activity is read through ONE generic (table, id) path, any module's history, no per-module read SQL. | `generic-activity-path` | enforced |
| R6 | ui | Product terms live in ONE glossary (clear, brief, no over-explaining), the app speaks one dictionary. | `glossary-wellformed` | enforced |
| R7 | ui | Every form dialog persists its draft per session (useFormDraft), unsaved input survives navigating away (CACHING.md §11). | `forms-persist-drafts` | enforced |
| R8 | ui | **Every tab that reveals a collection carries that collection's count, on BOTH tab surfaces.** A team section tab (`placement:'tab'`) declares a `countCacheKey`; a **record-detail** tab is badged from the block it reveals, recipe details through the `withTabCounts` seam (which collection is DERIVED from the tab's own block: `activity` → its source, `list` → its module), bespoke details in their own tabs config. A tab that shows no collection (Overview, the article body, the permission grid) says so once, as a reviewed `RECORD_TAB_COUNT_EXCEPTIONS` entry. R8 owns WHICH collection a tab's badge describes (derived, never hand-listed). The NUMBER is owned by R16 (an exact server total through `formatCount`); where the two disagree, R16 prevails. *Earned by:* every record in the app shipping an Activity tab with no count at all, the check walked the team strip, and the record tabs were built somewhere else. | `tab-counts-derived` | enforced |
| R9 | arch | The agent knows what the app can do, its system prompt carries a capability brief GENERATED from the import/export catalog (+ the glossary), so the UI and the agent can never disagree about a capability. And it knows what the app REFUSES: a vocabulary-gated write states its call ORDER (create the dropdown value first, write the rows second, one turn) on BOTH surfaces the model reads, the tool's own description and the system rule wall. **And it knows what the app can DRAW.** The assistant may emit VISUAL BLOCKS, a fenced ` ```kwapso:<kind> ` region the app renders as a real component instead of a paragraph of figures, and the set of kinds is declared ONCE (`shared/agent-blocks.ts`), so three surfaces are derived from one file and none is hand-listed: the PROMPT (scraped back out of the generated brief), the PARSER, and the RENDERER (read off disk in `web/`, because the model's promise is worth only what the browser paints; `tsc` refuses it too, the renderer map is a mapped type over the catalogue's own union). Every example the prompt teaches is proved by RUNNING the parser over it, not by reading it, the lesson R22 paid for, since an example that drifted from its validator would teach the model, in the prompt, to emit exactly what the app refuses. The wire format is a FENCE so a model that has never heard of it degrades to a code block instead of garbage, and the JSON is treated as untrusted model output (R20's posture off the request path): a bad kind, bad JSON, a non-finite number or a row count over the cap renders as inert text, never as markup. | `agent-app-parity` (workers/data-ops/test/agent-parity.test.ts) | enforced |
| R10 | arch | Every state-changing route opens with a permission gate (requireRight / gated / requireAnyImportRight / adminGuard), unless it's a reviewed identity-gated write (teamless onboarding, own-pointer, ownership) that gates on whoAmI. No ungated door ships. | `gating-seam` (per-worker tests: tenancy, content, data-ops **and mcp**, the security counterpart to `publish-seam`. The mcp suite is the EXTERNAL machine surface's own: it asserts every non-GET route there opens with token or user verification, because a door reached by a machine is reached by a stranger) | enforced |
| R11 | arch | Every external `fetch()` (a bare global fetch to the internet. D1 REST door, email sender, AI model call) carries an `AbortSignal` timeout, so a hung socket can't stall a worker. Service-binding `X.fetch()` calls are Cloudflare-bounded and exempt. | `fetch-timeout` (source-scan in `web/test/rules.test.ts`) | enforced |
| R12 | arch | Every cron / `scheduled` handler records its failures to the error store (`recordWorkerError`), unattended work has no user watching, so a swallowed background failure would vanish from the 90-day error log. (A user-facing catch that shows a friendly message should record too, a convention; see the agent's model-call catch.) | `cron-records` (source-scan in `web/test/rules.test.ts`) | enforced |
| R13 | arch | Shipping the code ships the capability: every module is an import TargetDef or a reviewed `CATALOG_EXEMPT` entry. AND the core catalogue reconciles itself against the code on READ (INSERT-only, `ON CONFLICT DO NOTHING`: a target the owner switched OFF stays off; only a never-existed row is created; the picker filters `is_active` in memory, never in SQL). *Earned by:* staging importing two modules that production, running byte-identical code, could not, rows are data, and no deploy carries data. | `catalog-coverage` (`workers/data-ops/test/catalog-coverage.test.ts`) | enforced |
| R14 | arch | **No unbounded list endpoint, and no capped GROWING one.** Every exported `list*`/`search*` function backing a collection applies a HARD CAP (`LIMIT n`, said in a comment; the caps live in `shared/workers/limits.ts`), but a collection that GROWS with ordinary use (`GROWING_COLLECTIONS` in the registry: support tickets, the activity feed) must **PAGE** instead, and page by KEY not offset: an opaque cursor, an exact total and `hasMore` through the one `pagedJson` seam (`shared/workers/paging.ts`), with a client that can actually reach page two — **its OWN pager, named in the registry (`pagerFile`/`pagerKey`) and proved in the file that holds it**. A cap is an honest refusal to answer; paging is an answer. **And the SEARCH BOX on a paged list is the same sentence:** the library frame searches the array it holds, so on a paged screen it answers about page one, so a paged recipe leaves the frame's box off (`searchable: false`) and the door answers `?q=` instead, with its exact total taken over the SAME `WHERE` (SEARCH.md layer 2 · `web/components/paged-find.tsx`). *Earned by:* one unbounded read stalling a worker under a 24,000-row catalogue, then the same catalogue proving a 1,000-row ceiling is just a slower refusal, and then, once every growing list paged correctly, a manager on staging typing a client's name into six of them and being told, six times, that it was not there. *Amended 2026-08-18:* the client half was asserted by a BRANCH (`!c.listRecipe`) that matched three collections while checking one collection's control, so for the team feed and the list of time it was a constant — deleting either pager left the build green. One branch cannot stand for three collections. | `bounded-lists` (source-scan in `web/test/rules.test.ts`) + `paged-search` (`web/test/paged-search.test.ts`) | enforced |
| R15 | arch | **No deaf publishers:** every resource string any worker publishes must reach a listener (`TEAM_RESOURCES` / `SIMPLE_INVALIDATIONS` in `web/lib/live-resources.ts`, or the portal's own `PORTAL_LISTENERS`) or a reasoned `DEAF_EXEMPT` entry, the publisher set DERIVED by scanning `publishChange` calls, never hand-listed. *Earned by:* a manager screen staling because its worker pinged a resource nothing listened to. **A half was retired, on purpose:** R15 also used to require every paged screen to subscribe via `useLiveRefetch`. That clause detected paged screens by matching `/search?` or `usePagedList` in `web/components`, **zero files matched**, so its offender list was permanently empty and it protected a hook with **no call sites**. The need was real when written and then went away: paging moved to opaque **cursors over the shared store**, so a paged list's rows now live in a cache key (`accounts:<team>`, `help:<team>`, `activity:record:…`) with its cursor in a sidecar, exactly the caches the row-level registry patches and the portal's listener map invalidates. No screen holds page state *outside* those caches any more, which was the hook's whole premise. So the clause and `web/lib/use-live-refetch.ts` were **retired** rather than re-detected: a law kept alive by a filter matching nothing buys confidence without paying for it. | `live-collections` (source-scan in `web/test/rules.test.ts`) | enforced |
| R16 | ui | Every screen showing a collection shows its count, exactly once. The NUMBER: a server COUNT(*) through the ONE `formatCount` seam (`shared/web/format-count.ts`, floored abbreviation at every magnitude, zero/loading render nothing, a paginated screen still badges the WHOLE collection), **counted EXACTLY up to `TOTAL_COUNT_CAP` (1,000,000) and reported as "at least" beyond it**, through the one bounded seam `shared/workers/count.ts`, for every `GROWING_COLLECTIONS` total. A total that stopped early SAYS SO in the same object: `pagedJson` DERIVES `totalCapped` from the total itself, so no door can forget it and no door can disagree with the seam that stopped counting. A door that answers with `json` rather than `pagedJson` owes that flag itself (`summariseWorkLogs`). And a **stat tile is a badge with a label**: every `value:` inside a `<StatGrid>` is a figure, held to the same seam as a `badge:`. A number that feeds a DECISION rather than a display, billable seconds, an export's completeness, stays EXACT and does not come through the capped path. The PLACE: a tab badge where the screen has a counted tab, else a `CollectionHeading`. The ARBITRATION: a React context (`CountedTabs`/`CountedAbove`), a counted tab WINS and the heading stands down, decided per-permission at render, never by a prop. R16 owns the number; R8 owns which collection a tab describes. *Earned by:* a 24,011-product catalogue advertising "1000" (a capped list's length), and the same "24k" shown twice on one screen. *Amended 2026-08-14:* an unbounded `COUNT(*)` over a growing table was the one read in the product with no ceiling at all, and the exactness it bought above a thousand was precision the ladder threw away (24,011 and 24,499 both render "24k"). *Amended 2026-08-18:* the check read `badge:` props only, so one panel shipped BOTH original failures inside a stat grid — `String(summary.total)` beside a tab strip rendering `formatCount` of the same collection, and `people.length` off a `LIMIT 50` read presented as "People on it". | `counted-collections` (source-scan in `web/test/rules.test.ts` + `format-count.test.ts` + `count-seam.test.ts`) | enforced |
| R17 | arch | State transitions are idempotent: every deactivate/reactivate UPDATE carries the current-status predicate (`AND deactivated_at IS [NOT] NULL`; status moves: `AND status <> ?`), reads the changed-row count back, and when ZERO rows moved writes no activity row and publishes no change. *Earned by:* a double-clicked Deactivate writing two "deactivated" rows 2.0s apart into one record's history, history says what happened, not how many times a button was pressed. | `idempotent-transitions` (source-scan in `web/test/rules.test.ts`) | enforced |
| R18 | arch | A cross-module read carries the caller's module rights: the team activity feed subtracts denied modules through ONE shared clause (`activityVisibilityClause`, any count over the feed must reuse it), and every `relatedTable` a worker writes resolves through `ACTIVITY_GATE_MAP` or a pinned, reasoned `ACTIVITY_TABLE_EXEMPT` entry. *Earned by:* a member with a single read right seeing every module's before/after ("changed BIG-0000001 price from 4,500 to 3,900") through the one ungated feed. | `activity-gate-coverage` (source-scan in `web/test/rules.test.ts`) | enforced |
| R19 | ai | Agent/MCP filter parity: any tool sitting on a screen's list/search door EXPOSES and FORWARDS every filter that door parses, the required set is DERIVED from the door's own parameter parsing, never hand-listed. *Earned by:* the assistant falling back to free text and answering a DIFFERENT question, 3,465 descriptions that mentioned the words instead of the 134 records carrying the value. | `agent-filter-parity` (`workers/mcp/test/filter-parity.test.ts`) | enforced |
| R20 | arch | **Input is validated at the boundary, and now it is SCANNED.** Every field a worker reads off a request body must sit in a **checking position**: the first argument of a validator from `shared/workers/validate.ts` (`requireText` / `optionalText` / `queryText` / `requireIdList`, plus `parseUploadDataUrl`, the same seam for bytes), the operand of `typeof`, inside `Array.isArray()` or `Number()`, a strict comparison against a literal, or the needle of an allow-list `.includes()`. Nothing else: **a truthiness guard is not a type check** (`if (!body.id)` lets `{}`, `[]`, `123` and a 10 MB string straight through), and a cast is not a check at all. A body may not be **destructured at the read**, that scatters untrusted values into bare locals nothing can follow back to the boundary. A door that genuinely cannot validate is a reasoned `RAW_BODY_EXEMPT` line in the registry, and the list is a **ratchet**: a listed line that is no longer an offender turns the build red, so it can only shrink. *Earned by:* `POST /api/auth/email/start` with `{"email": 123}`, an unauthenticated **500** that crashed *before* the send throttle and wrote an `error_logs` row into the **global core database** on every request. And earned twice over by how long it hid: this law lived for months as a sentence in CLAUDE.md claiming to be "locked by `workers/content/test/validate.test.ts`", which locks the helpers' behaviour and the **query** half and excludes `workers/auth` outright. Every other law had a scanner; the one about never trusting a request body had prose. | `validated-bodies` (source-scan in `web/test/rules.test.ts`) | enforced |
| R21 | arch | **A door on the agency's own material refuses a client login, at the door.** Every route reachable at the AGENCY origin that a caller holding only the **Client role's** rights can pass, including every door gated by nothing but membership, must do one of four things: refuse a portal caller (`refusePortalCaller`), resolve the caller's account fence (`accountScope`), be a door the **client portal itself opens** (`PORTAL_DOORS`), or be a reasoned `CLIENT_REACHABLE_EXEMPT` line whose reason is always the same shape, *the door answers about the caller themselves*. Nothing is hand-listed: the Client role's rights are read out of the seed, the routes out of each worker's own `ROUTES` table, the gate out of the handler source (following route-local helpers, so a refusal one frame down still counts), the portal's surface out of `PORTAL_DOORS`. *Earned twice.* The client portal's gateway forwards a NAMED allow-list and leaves the agency's own doors out, saying why; the agency gateway forwards **by prefix**, and a client login is an ordinary team member holding an ordinary role, so every door the portal withheld was served to the same person at the other hostname. First the learning library, the module has since been purged, the lesson has not, and the dropdown vocabulary. Then, because the enumeration that followed listed *what the accounts module owns* instead of *what a client can reach*, the ticket **stakeholder** list, which names the agency's staff admins with their email addresses, and answered on a POST as well as a GET. **Enumerate by what a client can reach, never by what a module owns.** | `client-reachable-doors` (source-scan in `web/test/rules.test.ts`) | enforced |
| R22 | ai | **Agent/MCP body-field parity**. R19's sentence about the OTHER HALF of the request. A tool sitting on a WRITE door exposes and forwards every field that door reads off the request **body**. The required set is DERIVED from the door's own `body.<field>` reads, its handler plus any helper in the same file it calls, one level deep (R20 is what makes them legible: a body is never destructured at the read). That one level is R19's blind spot closed in the same breath: while the scan read the handler alone, a door that factored its parsing into a helper beside it dropped out of the census entirely and its tool's obligations silently became none, a law that stops looking when you tidy up is a law that rewards tidying up. The FORWARDING half is proved by **running** the tool's `buildBody` on a filled-in call rather than reading its source, so a builder that delegates to a helper is judged by what the door actually receives, not by which words happen to appear in it. A field deliberately left off is a named line in `NARROWED_BODY_FIELDS` with its reason, said again in developer English in MCP.md §3; the list is a **ratchet**, an excuse in front of a field the tool now exposes turns the build red. *Earned by:* R19 deriving its obligations from `searchParams.get(...)` only, so four write tools quietly offered a narrower contract than their door accepted, for six weeks, under a green build, `update_team` could not set the logo, `create_role` could not carry its permission matrix, `reply_help_ticket` could not @mention, `agent_chat` could not attach a file. A law that only inspects the query string measures your query strings. | `agent-body-parity` (`workers/mcp/test/filter-parity.test.ts`, beside R19) | enforced |
| R23 | ai | **An answer from the knowledge base carries its sources, or it is not an answer.** Retrieval never writes prose, it hands back the passages it found and the sources they came from, and the assistant composes the reply with those in front of it. So `found`, `passages` and `citations` are ONE decision, made in ONE place (`knowledgeAnswer` in `workers/content/src/lib/knowledge.ts`): no citation means no passage, and a sentence the assistant must say instead of inventing one. No door may assemble that response by hand, the same shape as R14's `pagedJson` seam, and for the same reason: a door that builds half a contract ships half a contract, and here the missing half is the difference between *"we have nothing on that"* and a confident answer with nothing behind it. The compartment a question was answered from, and the REASONING that chose it, ride the same object, because a wrong compartment is invisible otherwise. **And now the assistant writing it is sometimes US** (2026-08-18): the Knowledge tab asks the door to compose the reply, one cheap model call, so the law's own sentence became a code path rather than a hope about a chat panel. It did not bend to make room. The prose is an INPUT to `knowledgeAnswer`, decided in the same expression as the citations (`answer: found ? …`), so a written answer cannot outlive its sources; the writer (`lib/knowledge-compose.ts`) is handed the settled passages and citations, reads nothing about `found`, and hands back a string; the door may not name `answer:` any more than it may name `passages`; and the passages reach the model FENCED, because half of them are words a client wrote. *Earned by:* the owner's own sentence in the brief, *"an answer with no source is a bug, not a style choice"*, and by what a sourceless answer costs where this one is aimed: an agency repeating it to a client. | `cited-answers` (`workers/content/test/cited-answers.test.ts`) | enforced |
| R24 | arch | **An internal number cannot reach the client's side, structurally, not conditionally.** What our own hour costs (`internal_rates`), what an hour of a ROLE is worth (`internal_role_rates`, the second internal rate card — the ACCOUNT card below makes three in all) and the margin computed from them live in ONE file, `workers/tenancy/src/lib/internal-money.ts`, and the check DERIVES the doors that call into it from that file's own exports. Then three things hold, all off disk: none of those doors is on the portal gateway's surface, every one of them opens with `refusePortalCaller`, and nothing in `web-portal/` names the internal table, those doors' paths, or a margin field. SCOPE's ruling is absolute, internal rates and margin never render in the portal under any flag, *ever*: not behind a permission, not behind a feature toggle, not for an admin viewing the portal, and the instruction with it was to make that **structurally true** rather than a condition somebody can invert later. A condition can be inverted and a permission can be granted; an import cannot be forgotten. The ACCOUNT rate card (what a client *is charged*, which they may be shown when their price visibility is on) is a separate file and a separate table for the same reason: two numbers of identical shape and opposite audiences must never share a `WHERE` clause. **AND THE OUTBOUND HALF (added 5 Sep 2026): a conversation that has READ an internal number may not then WRITE to a door the client's own browser opens.** The three clauses above are all about the import graph, and the assistant needs no import: it reads the margin through a door R24 fences correctly — as an agency admin holding `commercials:read` — and then replies into a ticket thread the client reads. Nothing was forgotten and nothing was inverted; every door did its own job, and the figure still landed in the client's portal thread and their inbox with no confirm panel anywhere on the path (`reply_help_ticket` is gated on `help:read` and confirms only when it @mentions somebody). The instruction came from the client themselves: a portal ticket description is 20,000 characters of their prose, read by the assistant the next time anybody here asks a question that touches tickets. So the fourth clause is a **per-turn taint, refused at the step, before the door is called** — and both its sets are derived: the money doors from `internal-money.ts`'s own exports through tenancy's own `ROUTES` (the same walk clause 1 makes), the client-readable doors from the non-GET half of `PORTAL_DOORS`, and the TOOLS from those doors off the shipped catalogue at load time, so a money tool added tomorrow on an existing door is covered the moment it exists. It **refuses before it defers**: a confirm ends the turn and `confirmAndRun` resumes from a stored row that remembers nothing about it, so a proposal is judged when it is made *and* again as it runs. What stood there before was a sentence of prose in a tool description — the least structural defence available, asked to hold against prose written by the person it protects the number from. | `internal-money-never-in-portal` | enforced |
| R25 | ui | **A savings figure never renders without saying what it is made of.** Every screen on either front door that shows a saving renders `SAVINGS_CAPTION` (`shared/workers/savings.ts`) word for word: *the times are estimates we agreed with you; the subtraction is arithmetic.* The screens are DERIVED from the payload they read (`savedSecondsPerMonth` / `savedHours`), never hand-listed, so a new screen is held to it the day it is written. *Earned by:* the owner naming "the numbers stop being believable" as one of three things that would make him abandon this and go back to a spreadsheet. A client who understands that the inputs are agreed and the arithmetic is arithmetic trusts the figure; one who believes we held a stopwatch stops trusting every other number in the app the day one of them looks wrong. The caption is not decoration around the feature, it is half of it. | `savings-caption` | enforced |
| R26 | arch | **The vector index NARROWS; the team's database DECIDES.** The knowledge base's search moved out of the per-team database into one account-wide Vectorize index, which trades a structural property for a usable one, so the structure is bought back twice on the other side, and both halves are the law. **First, tenancy is a PARTITION, not a filter:** every call into the store passes `namespace: guard.teamId`, built in one function (`namespaceFor`) from the caller's own guard and never from anything on a request. Vectorize applies a namespace *before* the search and a query may name exactly one, so a query cannot reach another tenant's vectors even if every metadata filter were wrong. **Second, and this is the clause that makes the first survivable: nothing readable comes out of the index.** It is asked for ids and scores alone, `returnValues: false`, `returnMetadata: "none"`, and a hit type with no text field, and every passage in every answer is then read back out of the TEAM'S OWN DATABASE, under the caller's own owner clause, with excluded sources gone. So a mislabelled or stale vector can cost a relevant passage; it cannot produce one the caller was never allowed to read. *Earned by:* the decision this replaced, which kept vectors in the team's own D1 precisely because "do not put every team's vectors in one undifferentiated index and rely on a filter you wrote correctly today", a correct objection that had to be ANSWERED rather than waved at when the search outgrew a SQL statement. The check does both halves: it reads the two files off disk, and it runs the doors against an index deliberately planted with lying vectors (one claiming the team shelf for a private source, one left behind by an excluded source, one belonging to another team) and asserts that none of them reaches an answer. | `vector-fence` (`workers/content/test/vector-fence.test.ts`) | enforced |
| R27 | ai | **Described contracts, every `backticked` identifier in a tool description names something real.** One of: an argument the tool's own schema declares; a query param or body field its door reads (the same door census R19/R22 stand on); a field its response actually carries, the `pagedJson` contract (`total`, `totalCapped`, `hasMore`, `nextCursor`) and the door's own extras read out of the call itself (help's `mineTotal`, work-logs' `totalSeconds`), the handler's own `json({…})` literals, the row fields the module's libs map off a database row (`stepKey: r.step_key`, the one mapper shape the base uses), and R23's `knowledgeAnswer` seam; another tool's name on either machine surface; or a reasoned `DESCRIPTION_VOCABULARY` entry. The vocabulary is a **deny-list in spirit**: every entry carries a reason, an entry nothing uses turns the build red (the rotting-entry rule), and so does an entry a derived class already covers. Nothing hand-listed that can be derived: tools from the three catalogue files themselves, doors from each worker's own switchboard, reads from handler source, response keys from the seams and the module's own libs. **Scope, stated honestly:** the law checks *identifiers*, not sentences, a false sentence built from real identifiers ("filters by `status`" on a door whose rows merely carry status) is R19/R22's to make pointless, since the schema either exposes the filter or doesn't. What this law ends is a name that exists *nowhere* surviving review, because only humans read prose and humans assume a green build checked it. *Earned by:* 2026-08-16, a session reverted a tool description to a false promise, added two parameters that do not exist and a fake filter claim, and `npm run check` passed. R19/R22 read the machine-readable contract (schema, `buildQuery`, `buildBody`) and never the prose, so the words a developer actually reads were the one unchecked surface on the two catalogues. | `described-contracts` (`workers/mcp/test/described-contracts.test.ts`, on the same door census as R19/R22, the suite also replays the three lies from the proof against its own classifier, so a green run means all three still go red) | enforced |
| R28 | ui | **A string the app says is in the catalogue, and the catalogue says nothing the app doesn't.** `shared/i18n-strings.json` must be EXACTLY the set of user-visible English sentences the two front doors say, derived by re-running the one shared definition of what a person reads (`scripts/lib/i18n-source.mjs`, the same walk the extractor writes from and the adoption codemod wraps), never by a second description of it. That definition answers TWO questions and both are derived. **WHICH POSITIONS** a person reads (the seven), and **WHICH FILES** — `appFiles()`, the front doors' own IMPORT CLOSURE. A file is walked because a front door imports it, not because of the folder it sits in. Four failures, named separately because they are four mistakes. **MISSING**: a sentence in the source that is not in the catalogue. English is the KEY (`shared/i18n.ts`), so an uncatalogued string is translated nowhere and ships in English to a reader who chose German, silently, on a screen that looks finished. **ORPHAN**: a catalogue entry no string in the app matches. Nothing breaks today, which is exactly why it rots: a deleted screen's sentences stay in the file, are translated on every build, and turn the catalogue into a record of what the app USED to say. **UNREACHABLE**: a file under `web/`, `web-portal/` or `shared/` that says something a person reads and that the walk never opens — censused off the DISK, so a resolver that missed an import cannot hide behind its own graph, with a reasoned `UNWALKED_OK` line the only way out and a rot check so the list can only shrink. **The walk stops at ONE directory**, `shared/ui/`, the vendored component library. It always did, and by a rule nobody had to write: the library arrived as a bare `@kwapso/ui/...` specifier, and `resolveImport` refused every bare specifier as *somebody else's code and not ours to translate*. Vendoring it on 2026-08-22 changed its ADDRESS, not what it is — a generic component library whose strings are DEFAULTS, while the app says its own words through the props it already translates (`emptyText`, `placeholder`, `label`, `empty`). So the refusal that used to be free is now a stated line in the same function, and the reason is in `VENDORED_UI_SCOPE`. Reversing it would be expensive in the direction nobody notices: ~150 library strings would enter the catalogue in English, and only `scripts/i18n-translate.mjs` turns an English key into German, which spends real money per run. **The residue is real debt and is written down, not waved away** — a few of those strings are screen-reader labels no prop can override ("Close" on Dialog and Sheet, "Loading" on Spinner, "Toggle theme" on ModeToggle), so a blind reader who chose German hears them in English; they are listed in `NEEDS-A-SPEC.md`. And one more clause, the same sentence from the call site's end: **the call site and the definition never disagree** — every `t("…")` literal must be a string the walk accepts, because a developer wrapping `t("of")` has DECLARED it copy while `isUserVisible` refuses it as a non-sentence, and the answer is never to widen the predicate but to write the whole sentence with a `{hole}` in it, which is also the only shape a translator can reorder. And a fourth: **A LANGUAGE THE APP NO LONGER SPEAKS** — a translation on disk for a code outside `LANGUAGES`. `shared/i18n.ts` calls that array the one place a language is decided, which was true of everything DERIVED from it and false of the two files that ACCUMULATE: on 2026-08-20 the app declared four languages while 1.4 MB of Javanese, Hausa and Thai rode in the bundle behind it, and the generated catalogue's own header still claimed twenty-eight. Narrow on purpose — a language the app speaks but has not translated yet is a partly-translated app, which is this law's founding position on missing words. `node scripts/i18n-prune.mjs` fixes it and only ever REMOVES; filling a language IN costs money and is the translator's job. A ratchet, the same shape as R20's exemption list and R27's vocabulary. *Earned by:* the pipeline being BUILD-TIME by design (13.5, every language the app speaks produced once per deploy, costing nothing at runtime), which makes "is the catalogue current?" the assumption the whole feature rests on, enforced until now by somebody remembering to run a script. The extractor already had a `--check` mode; it was wired to nothing. And earned a second time by the answer to "current *against what?*": the set was six hand-written folders, so `formatRelative` in `shared/web/format.ts` returned "just now" and "5d ago" in ENGLISH to nine call sites across both front doors — a German reader was shown *Erstellt von Aurora · 5d ago*, the translated half and the untranslated half in one line — the whole language settings screen and the whole text-size section sat in `shared/web/` already wrapped in `t(...)` and in no catalogue, and `shared/scale.ts` held "Compact", "Comfortable" and "Large" one directory outside the walk. A folder list cannot be made correct, only current, and current is exactly what nobody notices going stale. The fix is one command, and the failure says so. | `catalogued-strings` (`web/test/catalogued-strings.test.ts`) | enforced |
| R29 | ui | **The page has one width, and a screen does not get its own.** Each front door owns exactly ONE page container — `web/components/app-shell.tsx` at `max-w-none`, `web-portal/components/portal-shell.tsx` at `max-w-3xl` (narrower on purpose, UI-RULEBOOK L5) — and no other component may set a page-level width. A page container is identified POSITIONALLY, the way R20 identifies a checked field: one line carrying `mx-auto`, `w-full` and a `max-w-*` together, which is the signature of a centred content column and of nothing else. A dialog, a sheet, a door card, a chat bubble or a capped line of prose is not centred-and-full-width, is not caught, and keeps its own measure (L2: prose is capped, the page is not). Exceptions are DATA in `SCREEN_WIDTH_EXEMPT`, each carrying its reason, and every one is **rot-checked**: an entry whose file no longer sets a width turns the build red, so the list can only shrink. *Earned by:* the owner naming it as an inconsistency rather than a bug — work logs, tasks and meetings use the whole width and he could not see why other pages did not. The answer was that those three render through the one shell and six screens capped themselves at `max-w-2xl`/`max-w-3xl`, at 60% of the room they had. One of the six was the shell's OWN loading skeleton, so every cold load of the agency app visibly snapped sideways as 672px of skeleton became 1120px of content. UI-RULEBOOK L1 already said "one container, one cap" and round one implemented it in `deep-link-screen.tsx`; it stayed unimplemented in six other files, silently, under a green build, because a width is invisible to every other check here — TypeScript sees a string, the lint sees a string, and no test reads layout. **Earned a second time, 31 Aug 2026:** `deep-link-screen.tsx`'s own return only reaches five of the ten screens `AppShell` renders — home, settings, profile, invitations and kwapso return earlier, straight into the shell, and never hit that wrapper — so those five ran uncapped, invisibly, until a wide monitor showed Kwapso running past 2100px while Meetings, three clicks over, stopped at 1600. The cap moved up to `app-shell.tsx`'s own content div, the one node every screen the shell renders passes through, so it is set once and every screen inherits it the same way. **Earned a third time, same day, twice more:** live on staging, the client called `1600px` too narrow — "screen width. you made them thinner! i wanted them even wider!" — answered with a guessed fluid cap, `min(96vw,2200px)`. Then a real target, not a bigger guess: "wider screens, align to the right same level as breadcrumbs!" The breadcrumb row (the shell's own `header` prop) sets no width of its own — it already runs to the full padded edge of the content column — so `max-w-none` is what makes the page's own right edge land on that same edge, on every rail state and every viewport, which no guessed number (fixed or fluid) can promise. | `one-page-width` (`web/test/rules.test.ts`) | enforced |
| R30 | workflow | **Every email is classified, and one that names a record carries the way back to it.** Every function in `workers/` that composes a branded message (the `brandedEmail` / `sendBrandedEmail` seams, read off each file's own imports so an alias cannot dodge it) must appear in `EMAIL_CENSUS` as either **`refersToRecord: true`**, it is about a specific record, so it passes a `ctaUrl` built through the ONE helper, `shared/workers/record-link.ts`, or **`false` with a real reason**, and then it must NOT pass a `ctaUrl`: an email that names no destination may not promise one. Both directions, like R27's vocabulary, an unclassified send fails the build, and a census line matching no send is an orphan and fails too. **No email spells an origin.** The two front doors arrive as configuration (`PUBLIC_APP_URL` / `PUBLIC_PORTAL_URL`) because THE RECIPIENT DECIDES WHICH APP THE LINK OPENS: staff read the agency app, a client contact reads the portal, and the same ticket therefore has two addresses. An agency URL in a client's inbox is a link they cannot open and a door they may not pass, advertised (R21), so the helper takes the audience, a record kind with no screen on a front door returns `null` rather than an invented one, and a portal address never carries the agency's team id. *Earned by:* the mention notice reading "Open the ticket to read the full conversation and reply" in an email that contained no link of any kind, and no email in the fleet having one. The copy instructed an action the message made impossible, and nothing in the build could tell the difference between an email that links and one that only talks about linking. Buttons alone would have fixed twelve emails; the thirteenth, written next year, would ship the same way. | `linked-emails` (`web/test/linked-emails.test.ts`) | enforced |
| R31 | ui | **Two radii and no third, spelled the kit's way.** A rectangular surface is `rounded-[var(--radius)]`, a pill is `rounded-pill`, and a directional variant of the first (`rounded-t-[var(--radius)]`, for a sheet that meets the bottom of the screen) is the same word applied to one edge. **THE SPELLING BECAME THE KIT'S ON 2026-08-27, when the kit became canon.** Kit `docs/RULES.md` §4.2 forbids `rounded-lg`/`xl`/`2xl`/`md`/`sm` outright and asks for `rounded-[var(--radius)]` and `rounded-pill`. Substantively identical — `rounded-xl` renders at 24 — but ONLY because `tokens.css` loads after Tailwind's theme: Tailwind emits `--radius-lg: 0.5rem`, `tokens.css` emits `1.5rem`, and the kit wins by cascade order alone. So 184 corners across both front doors were correct by IMPORT ORDER rather than by declaration, and no check stood under that — reorder `globals.css` and every card silently becomes 12px with the suite green. Respelled in one commit; `rounded-pill` graduated out of `RADIUS_EXCEPTION` because it is the primary word now, not an exception. No other step may be written in `web/`, `web-portal/` or `shared/` — with ONE admitted exception, and the design kit is why. **`rounded-select` (6px), on the mark of a selection control.** Two numbers cannot draw everything: at `rounded-xl` a 16px checkbox is a lozenge and at `rounded-full` it is a radio button, so this is the one place a third number is unavoidable rather than convenient. The kwapso kit rules exactly this (ruling 03) and its own specimen calls it *"the ONE named exception to the two-radius law: 6px. Nothing else may."* It is DATA in `RADIUS_EXCEPTION` with its reason, and rot-checked in the shape R29's width pins and R32's colour literals already use: an entry nothing uses turns the build red, so the vocabulary can only shrink. The kit names a SECOND exception — 4px for a bar or a heat cell, *"a bar is not a box"* — and it is deliberately **not** defined here, because nothing in either app draws one and an unused exception is exactly what the ratchet catches. It arrives with the commit that needs it, which is the only moment anyone can check its reason is true. A THIRD entry, `rounded-ss-none`, is zero rather than a number — the assistant panel's own leading corner, removed where its new folder tab (2026-09-03, matching the content card's trail) attaches, the exact argument `screen-shell.tsx` already makes for that card's corner one directory over, out of this file's reach because that surface is vendored. **A third BOX radius is still forbidden.** *Earned by:* the cheapest rule here to obey being the most broken — 63 of 125 radius classes were off-vocabulary (`rounded-lg` 52 times, `rounded-md` 7, `rounded-2xl` 2) and changing every one of them moved NOTHING on screen, because `--radius-sm` through `--radius-3xl` are all `var(--radius)`. Five words for one value is five decisions a developer can make where there is only one, and the day the theme gives those steps different numbers the app acquires five radii it never chose. The law could not ship before the fix, because a law that arrives with a 57-line exemption list is a list. The bare `rounded` class is deliberately NOT caught: it is 4px here, not 24, so on an inline highlight and a 32px thumbnail it is a different value doing a different job. **The vendored library is IN scope, and the way it got there is the point.** `shared/ui/` arrived writing `rounded-sm/md/lg/2xl` at about sixty sites, and it could not simply be swept: the sentence above ("all `var(--radius)`") had stopped being true of the code the law now scanned, because that file defined the four steps as four different `calc()`s, so rewriting the classes would have RESHAPED every button, input, menu and card rather than moved nothing. It was excused in `VENDORED_UI_SCOPE` for exactly one day, with a rot check asserting the directory still offended. When the reskin's shape stage collapsed 45 of those radii into `rounded-xl` and pinned every Tailwind step back to one `--radius`, that check went red naming its own exemption, and the exemption was deleted by the commit that earned it. The sentence is true again, and the ratchet is the reason anyone can say so. | `two-radii` (`web/test/rules.test.ts`) | enforced |
| R32 | ui | **Every colour resolves through a token.** No screen may name a Tailwind colour ramp (`amber-*`, `emerald-*`, `slate-*` …) or write a hex literal. What a colour MEANS has a token — `warning`, `success`, `destructive`, `muted`, `primary`, `chart-1` to `chart-5` — and a mark comes from the chart series (UI-RULEBOOK C6). The six files that legitimately hold a literal are data in `PALETTE_LITERAL_OK` with a reason each, rot-checked so the list can only shrink. *Earned by:* `import-screen.tsx` saying `amber-600` and `emerald-500` where it meant warning and success, so the one screen in the app that reports a RESULT was the one screen a rebrand could not reach; `shared/departments.ts` holding five hexes the LEGACY app chose, none of them one of kwapso's own seven, so a department dot was the single mark on screen that did not belong to this product's palette; and the month grid painting today's square `#ffe9b0`, which is the soft mango `--secondary` already resolves to, written out by hand. None of the three is a bug anybody would file — they all look fine, in one theme, on one day. Colour drift is only ever visible in aggregate, and nobody sees the aggregate. | `closed-palette` (`web/test/rules.test.ts`) | enforced |
| R33 | ui | **Every extracted position asks for its translation.** R28 makes the catalogue match the code; this makes the code READ the catalogue. Every position `scripts/lib/i18n-source.mjs` reports in `web/` or `web-portal/` — the SAME one definition, read the other way round, so the two laws can never disagree about what a sentence is — must sit inside a `t(...)` call. Two ways out, and they are different in kind. **Positional:** a `label:` or `helpText:` on an object that spreads a field config is translated on the way to the screen by `shared/web/field.tsx`, because `t` is a HOOK and a field config is a module-level constant — those words genuinely cannot be wrapped where they are declared, which is the one class of string in this app a developer could not have fixed by remembering to. That exemption is held shut by an IMPORT BAN: no file in either front door may import `Field` from the library directly, so the seam cannot be walked around (an import cannot be forgotten the way a `t(` can). The ban matches the PATH TAIL of the kit's Field — `components/field/field` since the kit's v1.1.0 layout move folded `controls/` and `structures/` into one `components/` on 2026-08-27 (it was `controls/field/field` between the 2026-08-25 design-kit swap and that move, and `primitives/field/field` before the swap). The tail must move with the file: after the FIRST rename the old `primitives/…` tail matched nothing, so the ban passed vacuously for one evening until the story pass caught it — a ban is only as alive as the path it names. The SECOND rename is why the check no longer trusts the literal on its word: `web/test/wrapped-strings.test.ts` asserts the seam itself imports that exact tail before using it to judge anybody, so a third rename fails loudly instead of reporting all clear. *(This sentence itself named the dead `controls/…` tail from 2026-08-27 until 2026-09-06 — the check had moved and its description had not, which is the documentation half of the same defect.)* **Pinned:** a copy TABLE read back through `t` somewhere else — the screen recipes, the nav registry, the sort and filter vocabularies, the portal's status words — is data in `TRANSLATED_WHERE_READ` with the `via` call that reads it, rot-checked both ways so the list can only shrink. *Earned by:* R28 being perfectly satisfiable by an app that speaks English to everybody, and on 2026-08-18 it was — 666 of 2,001 extracted positions were in the catalogue, translated into all twenty-nine languages of the time, every build, and never asked for: every form field label in the app, 119 toasts, both error boundaries, and most dialog titles (a ternary is a JSX EXPRESSION, so they were the last hole R28 closed and the first this one found still English). *Scope, stated honestly:* it stands entirely on the positions R28's walk can SEE. A sentence in a position the walk does not visit is in neither the catalogue nor this census; widening the walk is R28's job, and every widening lands here for free. | `wrapped-strings` (`web/test/wrapped-strings.test.ts`) | enforced |
| R34 | ui | **The glossary is the dictionary the SCREENS speak.** R6's second clause, finally read by something. Every user-visible English sentence in the two front doors — `shared/i18n-strings.json`, which R28 makes exactly that set, re-derived from the source on every build — is checked for a KNOWN SYNONYM of a glossary term. The banned words are DATA (`GLOSSARY_SYNONYMS`), each naming the term it competes with and carrying its reason; a sentence with a real claim to keep one is a line in `GLOSSARY_SYNONYM_OK`, rot-checked in both directions so an exemption whose sentence has been reworded goes red and the list can only shrink. Two more rot checks hold the list to the dictionary: every banned word names a glossary key that still exists, and no banned word may appear in the glossary's own term or definition — a rule that contradicts its dictionary is a rule people learn to exempt past. It is deliberately NARROW: a word earns a line only when, in this app, it can mean nothing but a record that already has a name. "client" is left off (it is the relationship, not the record), "option" is left off (the glossary's own definition of Dropdown values uses it), "request" is left off (it means a ticket on five screens and an HTTP call on three). *Earned by:* `glossary-wellformed` reading the glossary FILE and never one line of copy, so for a year the half of R6 a person actually experiences was enforced by nobody — and the app shipped green calling the same thing "Permissions" on the Roles screen and "access rights" on two others, plus "teammate" for a member, "cost card" for the internal rates and "Portal login" for portal access. Two words for one thing is not a typo: a manager reading both has to work out whether they mean the same, every time, and the answer is not on the screen. | `glossary-in-copy` (`web/test/rules.test.ts`) | enforced |
| R35 | ui | **A record never appears without its face.** Wherever a record or a dropdown value is shown to be CHOSEN or SCANNED — a picker option, a row in a collection, a row in a nested panel inside another record's screen — it carries its visual beside its name: its own picture where it has one, its type's glyph where the type has one, its initial where it has neither. Never nothing, and never the glyph written INTO the words, which is a pictograph inside a sentence and the one shape UI-CONVENTIONS §5 refuses. *Checked at the three places it can be LOST, not by inspecting markup:* `PickerOption` and `PickableRecord` must DECLARE the visual fields (a type that drops a picture makes the omission invisible at every call site); every list recipe must name its `leading` column; and the one shared nested row takes its mark as a REQUIRED prop, so `null` is a visible answer and an omission is a build error. *Earned by:* a census on 19 Aug 2026 after the owner asked for this a third time — 33 pickers, none of which COULD show a visual because the option type had no field for one; ten of fourteen list recipes naming no leading column; ~20 nested panels drawing bare words for records that lead with a glyph on their own screen; and `PickablePerson` dropping `imageUrl` one line before every picker that offers a person. There is no honest regex for "this JSX is a record row" — `.map(x => <li>` matches attachments, replies and comments too — so the rule stands on chokepoints instead, the same move that makes `record-picker.tsx` the only composer of `command`. | `records-carry-their-face` (`web/test/rules.test.ts`) | enforced |
| R36 | arch | **Every switch on the permission matrix decides something.** The grid gives every module four boxes whether or not four decisions exist behind them, so an inert box looks exactly like a live one — and an owner who ticks it, saves it and gets a green toast has been told they granted something. The offered set is DATA (`MODULE_OFFERED_RIGHTS` in `shared/team-modules.ts`, exceptions only, each carrying its reason, so the list can only shrink as doors get written); the consulted set is DERIVED off the source, from all FOUR places a right is asked for — a literal `requireRight`/`gated`/`gatedBody` pair, the MCP `TOOL_GATES` strings, every module in `ACTIVITY_GATE_MAP` (which the record activity feed asks for `read` on) and every import `TARGETS` module (which the importer asks for `create` on). *The check fails BOTH ways.* Offered-but-unasked is theatre. Asked-but-unoffered is the half that matters: a door written against a right no role can hold refuses EVERYBODY, and the Admin role is locked, so nobody can tick their way out of it. *Earned by:* the owner opening the Roles screen on 21 Aug 2026 and asking what the twenty-three rows were for. Fifteen of the eighty-eight boxes decided nothing, seven of them with no note anywhere saying so; one whole module (`screens`) had four boxes and no door, because both its doors gate on `teams:edit`, which is the correct answer for a team setting; and `learning` and `marketing` still held permission rows six weeks after the product purged them, because a purge deletes forward and migration 0021 had CROSS JOINed them onto every role that existed. A census that knew only about literal pairs would have called four LIVE rights dead, `work:create` among them — which is why the derivation names all four sources. | `offered-rights` (`web/test/rules.test.ts`) | enforced |
| R37 | arch | **A link inside the app never leaves the shell.** The whole post-auth app is ONE client-resolved shell (`web/components/deep-link-screen.tsx`) that mounts once and never unmounts, so every move within it is a History-API push through the single soft-navigation bus (`softNavigate`, backed by the host's `go()`) and nothing is fetched. A bare `<a href="/t/…">` opts out of all of it: the browser throws the document away, every module re-runs from nothing, the warm cache and any running agent are destroyed, and the boot mark plays again — because its trigger is a module-level `sessionCache` that only a fresh document can reset. So an in-app destination is written with `<InAppLink>` — a REAL anchor, so middle-click, copy-address and screen readers all still work, with only the plain left click intercepted — or it carries that same interception inline. *The check is a census OFF THE DISK* of every component's anchors, classified by where the href points: inside the app, an `/api/` door, a pre-auth route, or elsewhere. *Earned three times by one class of bug, green each time:* the knowledge base, missing from `TOP_LEVEL_MODULES` so every tap on it left the History API (the note survives in `deep-link/route.ts`); "Manage dropdowns", reported by the owner on 24 Aug 2026 in words that name the mechanism exactly — "I can see the app reload because I see the boot loading animation"; and the internal rate card, whose own comment says it copied the dropdowns link, which is what turns a typo into a pattern. The guard that existed could not see two of the three: it read SIX hand-listed files for ONE spelling (`router.push`), and a bare anchor in any of two hundred other components is a different spelling in a file nobody listed. R21's lesson arriving in a second module — enumerate by what NAVIGATES, never by what somebody remembered to write down. | `in-app-anchors` (`web/test/shell-nav.test.ts`) | enforced |
| R38 | ui | **A record detail may not look its record up in a page.** A screen showing ONE record of a collection R14 makes page (`GROWING_COLLECTIONS`) must read it BY ID — a dedicated per-record door, or a `<module>:one:<id>` read beside the list — never a `find` over the cached list, which holds only the loaded prefix. *The check is positional*, as R20's is: it ties the `find` to the query VARIABLE built from a paged cache key, so a find over a BOUNDED collection (apps, member roles), where page one IS the collection, is never caught. The by-id key must then reach a listener like any other (R15), or a status change patches the list and leaves the open record showing yesterday. *Earned:* the owner opened a ticket from triage on 26 Aug 2026 and was told "That ticket no longer exists." It existed — number 1,030 of 1,820 — and the whole lookup was a `find` over the newest fifty. Every ticket past the cursor was unreachable by direct link, from an email button, from a bookmark, and the screen made the most alarming claim available to it. Nothing was red: the door had answered `?id` since paging landed and says so in its own comment, and `helpOne` had been written and never called. Three sibling screens already had it right. R14 made the lists page and nothing asked what that did to the screens reading them. | `details-ask-the-door` (`web/test/rules.test.ts`) | enforced |
| R39 | ui | **The kit supplies the UI, and nothing else does.** No file in `web/`, `web-portal/` or `shared/web/` imports a UI package. The kit at `shared/ui/` is the one source of a control, a structure, a glyph and a toast; its own dependencies — Radix, sonner, recharts, class-variance-authority — are ITS to import, and the app reaches them THROUGH it (`@shared/ui/components/sonner/sonner`, never `sonner`). *The deny-list is derived* from the kit's own package.json peers plus the known icon packs, so a dependency the kit adds tomorrow is covered without anyone editing this law. Exceptions are data in `UI_PACKAGE_EXEMPT` with a reason each, rot-checked, so a pin whose file no longer imports the package turns the build red and the list can only shrink. *Earned by the icon swap of 2026-08-27:* 96 kit glyphs were not enough, so five files imported lucide directly and a sixth reached for lucide's RUNTIME loader — and the app quietly carried a second icon pack of 3,924 glyphs. When the kit's art became Iconoir, thirty-seven stored names kept drawing the OLD pack beside the new one, on the same screen, under a green build. No single import was wrong; the whole was a second design system nobody decided to have. | `kit-supplies-the-ui` (`web/test/rules.test.ts`) | enforced |
| R40 | arch | **A stored file must reach a person.** Every call that puts BYTES in a bucket — an `env.<BUCKET>.put(` on a binding the wrangler configs declare as an `r2_bucket`, or the one shared seam that does it for you (`storeImageDataUrl`, which hides a `bucket.put` behind a helper and is how four of these doors write without the word `.put` appearing at all) — is claimed by an entry in `STORED_FILES` naming the FIELD a person reads the reference back through and the front-door FILE that renders it. *Both halves are derived, and each end is grounded in a different oracle on purpose, so the check is never a parser agreeing with itself.* The WRITE census comes off the wrangler configs: which bindings are buckets is a deployment fact, so `env.MEDIA.put(` counts and a Durable Object's `storage.kv.put(` does not, and no list of bucket names lives in a test. The READ proof comes off the browser: bytes reach a person through an attribute the browser FETCHES — `href`, `src`, `picture` — directly or through a `const` assigned from `safeHref`/`safeSrc`, and through nothing else. *A field that only ever reaches a form's values is not a read*, and that subtraction is the whole discriminator. Rot-checked both ways, so an entry whose door no longer writes bytes turns the build red and the list can only shrink. *Earned by three instances of one bug, every one of them green:* a story's attachments, written by two dialogs and rendered by no screen at all — the owner attached two screenshots, saved, and never saw them again; a document a CLIENT uploaded through the portal's "Send a file", shown to the agency as a filename in a metadata line while the URL sat on the row read by nothing; and a task's `file_url`, write-only from the day the door shipped. In all three the door answered 200, the bytes were in R2 and the row was correct — which is precisely why nothing caught them. R15 makes a published change reach a listener and R16 makes a count reach a screen; nothing made BYTES reach anybody. The check has to walk write→read, because that is the direction the failure runs: starting from the screens would only ever find the files that are already shown. | `reachable-bytes` (`web/test/reachable-bytes.test.ts`) | enforced |
| R41 | arch | **A file somebody picked is either sent or refused, never dropped.** R40's sibling, and the boundary is the point: R40 asks whether a STORED file reaches a person, so where nothing is stored it is silent by construction — and this is the worse half, because R40's failures at least leave the bytes in the bucket. A create dialog cannot upload while somebody is still typing: R2 is addressed by the record's id and on a create that id does not exist yet. So the picked files WAIT, and are hung on whatever `onSubmit` hands back. Every dialog that defers an upload that way is a line in `DEFERRED_UPLOAD_FORMS` naming the maker whose id it needs — rot-checked against the dialog still deferring, so a form that stops loses its line — and every CREATE call site of it, censused off the disk, must actually return that id: either a concise arrow whose body IS the maker call, or a named value that is returned. A site that passes the record's id is an EDIT and is skipped, read from the same prop the dialog itself switches on, so the check and the code cannot disagree about which sites are at risk. *Deliberately narrow:* the fault is a DISCARDED RESULT rather than a missing call, so there is no absent function to census — only a value that goes nowhere — and an honest small law that can be checked beats a general one that cannot. *Earned by:* three of the four `<StoryFormDialog>` create sites doing `await createStoryFrom(...)` and throwing the id away, so `target` was null, the guarded upload never ran, and every file picked in those forms was dropped in silence — story created, success toast, no error, no row, no object, nothing to recover from. `createStoryFrom` had returned the id since the day the upload was written; the callers just did not pass it on, and a discarded value looks exactly like correct code. | `picked-files-are-sent` (`web/test/picked-files-are-sent.test.ts`) | enforced |
| R42 | arch | **Every accepted source type resolves to a declared reader on EVERY door, or to an honest refusal — and no door chooses its own.** One table, `workers/content/src/lib/source-readers.ts`, maps a type to an ORDERED list of readers, with a declared `UNREADABLE_TYPES` beside it so "we cannot read this" is a decision somebody wrote down rather than the absence of one. Both doors ask it: the upload door (`extractFile`) and the Drive lane (`driveFileText`). The order is per TYPE and not per door, because the two readers are complementary — Cloudflare's `toMarkdown` reads a PDF and an image and cannot read a deck; the in-process OOXML unzip reads a deck and cannot read a PDF or an image. *Earned 27 Aug 2026:* the app had two different pieces of code for reading a file and nothing saying which to use, so the answer depended on which way the file came in. A PDF uploaded through the app was read properly. The same PDF sitting in a Drive folder came out as gibberish — every PDF in the agency's own knowledge base scored zero on any test of whether it held words, the powers of attorney exactly like the logo artwork. Nobody chose that: each door had picked a reader at the moment it needed one, and the two moments were months apart. Asking one table instead also gave the upload door PowerPoint, which the other reader could always handle, without a line of new reading code. *Why it is a law and not a tidy-up:* **reusability**. Adding the next file format is one entry in one table rather than an archaeology exercise across two doors to find out what each already does — the difference between a base you can fork for a different product and one you can only inherit. *The load-bearing clause is the last one*: the check censuses both doors off the disk, comments stripped, and fails if either names a reader the table did not give it — because a predicate nobody calls is not a guard, and a table nobody asks is not a registry. *Provenance:* written by the session that found the defect, enforced by its own check BEFORE it was registered, and held unregistered until the owner authorised it himself. A Law of the Base carrying a session's signature on the constitution it is bound by would be the wrong precedent. | `declared-readers` (`workers/content/test/source-readers.test.ts`) | enforced |
| R43 | ai | **Agent/MCP tool-set parity.** A tool that exists on the agent's own catalog (`TOOL_CATALOG`) exists on MCP's own (`MCP_TOOLS`) too, under its own name or a locked rename, or the gap is a named, reasoned line in a rot-checked table — and the same sentence in reverse. *Why this is a separate law from R19/R22:* their coverage census asks whether a DOOR has a tool on SOME machine surface, which is right for what THEY check (a door reachable from either surface is not a gap the owner asked about) — but it means a door with an agent tool and no MCP tool passes that census trivially, and the asymmetry between the two machine surfaces, the exact sentence the owner asked to be made checkable ("if I can write, I can edit, I can delete... same thing an agent can do, same thing from MCP"), was never itself examined by anything. *Earned 28 Aug 2026:* twenty-five tools lived on the agent and nowhere on MCP, twenty-three lived on MCP and nowhere on the agent — every one of them a real, written decision (MCP.md §3's "intentionally NOT on the machine surface" list: the two confirm-panel bulk writes, the role-permission-matrix read, the SELF import runner, and the whole Google Drive/Gmail/Chat/Calendar surface, kept off MCP because a leaked personal access token's blast radius must not include a mailbox) — but the comment introducing the agent's own `AGENT_ONLY` array had drifted to naming four tools while twenty-five existed, silently, for the six weeks since the Google block landed. A law that stops looking when a block grows is a law that rewards growing it quietly. | `agent-mcp-tool-parity` (`workers/mcp/test/agent-mcp-tool-parity.test.ts`) | enforced |
| R44 | ui | **A catalogued string must be answered, up to a ceiling that only falls.** R28 makes `shared/i18n-strings.json` exactly the set of English sentences the app says; R33 makes every one of those positions ask for its translation. Neither asks whether the asking is ever answered — a string can be extracted and wrapped in `t(...)` and still have no entry anywhere in `overlay(CATALOGUE, SEED)` for a translated language, which is `shared/i18n-catalogue.ts`'s own stated fallback: English on screen, "a sentence rather than a bug." Per translated language, the count of extracted strings with no non-empty entry — the same predicate `coverage()` already uses to tell a person choosing a language what to expect — is pinned in `TRANSLATION_CEILING`. The check computes the true count fresh, off the same three files, and requires it to equal the pin exactly: a string shipped past the ceiling fails the build, and a ceiling left above the true count after a translation lands fails it too, so the pin can fall and can never rise without the count behind it rising first. *Not a hard zero:* `shared/i18n-catalogue.ts`'s own header argues a missing translation degrades to a partly-translated app, never a broken one, and a hard zero would turn the next ordinary feature PR red the moment it adds one new label — a build that fires on unrelated work is a build people route around, which hides the next real regression behind the routine one. *Measured 30 Aug 2026:* the ceiling is 0 in all three languages — the 308 strings the process-map, wave and Google-connections screens had left with no entry in either the generated catalogue or the hand-written seed were translated in one run through the app's own Cloudflare allowance (`scripts/i18n-translate-workers-ai.mjs`, never the owner's own key) and written into the seed. | `translation-ceiling` (`web/test/translation-ceiling.test.ts`) | enforced |
| R45 | ui | **Every kit composition is decided.** All 47 files under `shared/ui/compositions/` are derived off disk, and each one resolves to EITHER an adoption this app actually reaches — a direct `@shared/ui/compositions/...` import in `web/`, `web-portal/` or `shared/web/` — OR a reasoned entry in `COMPOSITION_EXEMPT`, naming why not: a genuine structural mismatch, a shape already assembled from other already-adopted kit parts under a different name, a real gap this app should or should never have, or a question left for the owner. The two acceptable outcomes are adopted, or DELIBERATELY not used for a stated reason — a composition nobody looked at, or hand-rolled screen UI that quietly duplicates one, is the only unacceptable result. Rot-checked both ways: a composition with neither an import nor an exemption turns the build red, and an exemption whose composition is now directly imported turns it red too, so the list can only shrink. *Why:* the owner's own words, 30 Aug 2026 — "make sure that we get 47 out of 47 compositions... if there are some compositions that we don't use, I completely get that, but flag those." Two lanes had worked through 37 of the 47 by hand, in prose, with nothing behind it that would catch a regression the moment a kit update added a composition, or a screen quietly duplicated one. | `composition-coverage` (`web/test/rules.test.ts`) | enforced |
| R46 | ui | **Every kit component and foundation resolves to a reached adoption or a reasoned, rot-checked exemption.** The kit at `shared/ui/` ships every directory under `components/` plus the three foundations (icons, tokens, motion). **The number is DERIVED and written down nowhere:** `kitInventory()` in `scripts/kit-coverage.mjs` reads it off the pinned tree, so it moves with the pin instead of rotting in four documents — run `node scripts/kit-coverage.mjs` for today's figure. The count came from the owner, with his instruction: "all 118 components should be imported, and if you're not using some, I understand that, but there should be nothing hard-coded." A part is REACHED, not merely imported: `computeReachability` (`scripts/kit-coverage.mjs`) seeds from every kit reference either front door or `shared/web/` makes, in EITHER language the kit ships in — a JS/TS `from "@shared/ui/…"` or a CSS `@import "…"` — then closes over the kit's own cross-references (a component that imports another, a stylesheet that imports another) until nothing new appears, so a part reached only through another adopted part, or only through a stylesheet, still counts. Every part the walk does not reach is claimed by a line in `KIT_COMPONENT_EXEMPT` naming the one sentence a non-technical reader can check — no surface in the app has this shape, or adopting it would break another law. Rot-checked BOTH ways: a part the walk now reaches loses its exemption, and a part with no exemption and no reach fails the build, so the list can only shrink. *Earned by:* counting only JS/TS import lines undercounted in the same direction seven times in one day — six parts reach the app only through another kit part it has already adopted (`notes` through Comments, `folder` through Tabs, `title` through the kit's own `record-detail`, `progress` through `file-upload`, `gallery` and `checklist` picked up by two other lanes overnight with nobody re-running the census to notice), and `motion` reaches both front doors only through a CSS `@import` in their own `globals.css` — a reference no JS-import grep can see in either direction, because there is no import line in that language for it to miss. Canaried both ways: deleting the `@import` from both `globals.css` drops the reached-foundations count from 3/3 to 2/3, and restoring it recovers 3/3 — the same assertion is now a permanent part of the check, not a one-time manual proof. | `component-coverage` (`web/test/rules.test.ts`) | enforced |
| R47 | ai | **Every module a person can see, the assistant can answer about — and what is not in the corpus says why.** Two clauses over one census, derived from the permission matrix itself (`TEAM_MODULES`, every module offering a `read` right). FIRST: each resolves to a KNOWLEDGE KIND that mirrors it (`INGEST_KINDS`, its module read off the kind's own `table` through `ACTIVITY_GATE_MAP`/`QUERY_MODULES`, or off the `modules` a kind declares when its text carries more than its table), or a GATED READ TOOL on the agent's own catalogue (the tool's door found in that worker's own switchboard, its module read off the handler's own `requireRight`/`gated` pair), or a reasoned `ASSISTANT_BLIND_MODULES` line. SECOND, the clause with the teeth: a module reachable ONLY by tool declares in writing why its material is not in the searchable corpus — a `CORPUS_EXEMPT` line, rot-checked, so a module that gains a kind loses its excuse and the list can only shrink. Nothing hand-listed: modules from the matrix, kinds from the sweep, tools from the catalogue, each tool's gate from the source of the door it forwards to. *Earned by:* the owner asked "what is Alex's full name?" on 1 Sep 2026 and the base could not answer — not a retrieval failure, but the fact that NOTHING anywhere said who his own colleagues are (`staff_profiles` carries a `user_id` and no name; the names live in the global core database). The first draft of this law asked only whether a module was reachable AT ALL and came back 21 of 22 green, which is exactly why the failure was invisible: a tool answers when the model knows to call it, and a vague question reaches the corpus. The money settles the shape — internal rates and the margin are reachable by tool on the R24-fenced doors and must never be in a corpus that has one gate and cannot fence per module, so the honest sentence is "reachable, but only by the people who could already see it", and it is written down for the owner rather than relayed. | `assistant-coverage` (`workers/mcp/test/assistant-coverage.test.ts`) | enforced |
| R48 | ui | **The toolbar, search included, is a default — never a per-screen choice.** Every collection/data-view screen draws its toolbar's search box UNLESS a named, reasoned entry says otherwise. Two censuses, off the disk, never a hand-list: every `BASE_RECIPES` entry (`web/lib/screens.ts`) whose recipe carries a `CollectionConfig` must have `searchable: true`, or be named in `TOOLBAR_EXEMPT`; and every `<ToolbarRow>` call site across `web/` and `web-portal/` (the bespoke toolbar a bounded collection with no recipe search reaches for, `web/components/deep-link/screen-bits.tsx`) must pass a `search` prop, or be named in the same registry. Both directions are rot-checked, so the list can only shrink. *Why:* the client's own words, correcting a narrower answer already given once — "I don't care here. You're giving me specifics, and I told you that the toolbar, including the search, should be absolutely everywhere we have a data view or a collection view. Stop hardcoding this. Just write it as a rule." `searchable` and `search` were both ordinary optional fields before this law, and two screens quietly omitted the second: Tasks' Calendar tab and the Triage queue each drew a toolbar with a button and no search box, reasoned only in a comment nothing read at build time. Flipping the default is the fix that cannot regress the same way twice — an opt-in can be forgotten by omission, which is what happened; an opt-out has to be written down and given a reason a reviewer can read. A collection genuinely and permanently empty of rows (`WaveFinder`'s and Sprints' own "nothing to search yet" fallback) is the one reason left standing, because a search box over zero rows is a control that cannot do anything. SUPERSEDED IN PART, R50: that fallback — a bare `<ToolbarRow actions={…}>` reached only once the collection was empty — is exactly how a create button kept escaping this law's own two censuses (both only ever asked whether `search` was present, never whether `actions` agreed with it); both call sites now carry one `<ToolbarRow>` gated by R50's own required `empty` prop, and `TOOLBAR_EXEMPT` no longer names either. | `toolbar-shows-search` (`web/test/rules.test.ts`) | enforced |
| R49 | ui | **The gap between a toolbar row and what it sits above is one number, paid by the row itself, never a per-screen margin.** `<ToolbarRow>` (`web/components/deep-link/screen-bits.tsx`) pays `--toolbar-content-gap` (`web/app/globals.css`) as its own trailing margin, on its own root, so every call site gets it for free. No call site may also wrap the row in a gapped flex column or space-y stack, and no call site may pass a competing `mb-*` in its own `className` — either is the same number spent twice. Checked as a census off the disk: `<ToolbarRow>`'s own definition must carry the token, and no call site (or the variable a screen names `*toolbar*` and renders in its place) may sit inside a `flex-col` wrapper that also declares its own `gap-*`/`space-y-*`, or pass a hardcoded `mb-*` of its own, unless named in `TOOLBAR_CONTENT_GAP_EXEMPT` with the real reason. *Why:* the client's own words, item 5 of the 2026-09-03 spacing round — "tehre's wahy too much space between the toolbar and the contenta" — confirmed on every screen she checked. It had drifted into five different numbers doing the identical job: a wrapping `flex flex-col gap-N` div (`gap-2`/`gap-3`/`gap-4`/`gap-6`, 7.5–22.5px), a `space-y-3`, and a `className="mb-4"` passed straight to the row — fourteen call sites, four mechanisms, no shared owner. The same shape `--tab-content-gap` already fixed for a tab strip and its panel, read the other way round, and the same value: `--space-5`, because both are "the gap between a control strip and the content under it" and one rhythm does not mint a second number for the same sentence. | `toolbar-content-gap` (`web/test/rules.test.ts`) | enforced |
| R50 | ui | **Never toolbar on an empty collection — not even the create button.** R48 made the search box a default; this makes the whole row answer one question together. `<ToolbarRow>` (`web/components/deep-link/screen-bits.tsx`) takes a required `empty` prop — true when the collection holds zero rows before any search or filter narrows it — and returns `null` unconditionally when it is true, before any other slot (search, filters, sort, view, or `actions`, the create button) is even considered. `<PagedFind>` (`web/components/paged-find.tsx`) takes the equivalent required `restingEmpty` prop for the door-searched half of the app, suppressing its own row the identical way. `<SectionWithCreate>`'s own header-drawn create button carries the same gate through an optional `empty` prop, for the one shape neither `folderTabs` nor `useKitPanel` already covers. Two censuses, off the disk: every `<ToolbarRow>` call site across `web/` and `web-portal/` must pass an `empty` prop derived from the collection's own row count, and every `<PagedFind>` call site must pass a `restingEmpty` prop the same way — missing, or hardcoded to a bare `{true}`/`{false}` literal, either counts as an offence unless named in `EMPTY_TOOLBAR_EXEMPT`. Rot-checked both ways. *Why:* the client's own words, verbatim, about a Time tab with zero rows: "once again, when empty collection no toolbar at all - fix everywhere and set as a rule." "Once again" is the load-bearing word — R48 had already ruled this once, but its two censuses only ever asked whether `search` was PRESENT, never whether `actions` agreed with it, so a `<ToolbarRow search={data.length > 0 && …} actions={canCreate && <AddButton/>} />` passed R48 outright while still drawing a lone, floating "+" over an empty collection. The audit this law's own census forced turned up the identical shape seven more times, each reasoned in a comment as if it were the intended design: Sprints' Overview/Calendar toolbar, Waves' own finder, a wave's own Sprints tab, Deliverables, Modules, all three of Client-org's lists, Dropdown values, and Triage's toolbar (drawn by a parent component that could not see whether the child's own fetch held a single row). `empty`/`restingEmpty` are REQUIRED rather than optional for the same reason every one of those was a bug: an optional prop a caller can forget to pass is exactly how a `search` gate and an `actions` gate drifted apart in the first place. | `empty-toolbar` (`web/test/rules.test.ts`) | enforced |
| R51 | ui | **A panel that minimises collapses; it is never unmounted on the state that animates it, and never left reachable once shut.** Three clauses, read off `shared/ui/compositions/templates/screen-shell.tsx` and `shared/ui/foundations/motion/motion.css`. (i) Nothing in the aside dock may be JSX-gated on `isAsideOpen` (`isAsideOpen ? (` / `? <`) — gating the column, or the wrapper that collapses it, unmounts the subtree on the flip and leaves the exit transition nothing to play on; every legitimate use picks a VALUE (`? "open" : "shut"`), never a subtree. (ii) The collapsing wrapper must carry all four of `.motion-column-collapse`, `data-state={isAsideOpen ? "open" : "closed"}`, `inert={!isAsideOpen}` and a `--motion-column-size`. (iii) `.motion-column-collapse` must NOT size itself with `grid-template-columns`/`fr`, and must transition `inline-size`. *Why:* the client asked to close the assistant from its own folder tab, "with a nice animation". The first implementation was the row-collapse rule turned ninety degrees — `grid-template-columns: 1fr -> 0fr` — on the reasoning that a fraction interpolates against the grid's own resolved track, so the column would collapse from whatever width it had without restating that width. Correct for a row, wrong here, and invisible in review: the wrapper is a FLEX ITEM, so its grid is sized under an intrinsic constraint, and a `0fr` track is then floored at its own base size instead of resolving to zero. Measured in verify/shell-chat the track never moved off 356.25px — the panel went fully transparent and kept every pixel of its width, leaving the card at 851px instead of the 1207.5px it should recover. That is visibly worse than the instant disappearance it replaced, and it passed tsc, passed lint, and read correctly in the source. Clause (i) is the other half of the same bug: the column was originally unmounted the instant `isAsideOpen` flipped, so the first attempt had nothing to animate. Clause (ii) exists because fixing (i) alone CREATES an accessibility regression — keeping a panel mounted so it can animate is exactly what leaves a keyboard user tabbing into an invisible column — and the earlier ruling on the shut state ("closed assistant show nothing. it's literally only the bar") is not satisfied by a column that merely cannot be seen. | `aside-collapse` (`web/test/rules.test.ts`) | enforced |
| R52 | ui | **Every path that draws a record detail wears the same title treatment — one constant, never a per-call-site class.** `RECORD_TITLE_TREATMENT` (`shared/web/record-heading.tsx`) is the record heading's own step (h1/44, reached as `[&_[data-slot=title-heading]]:text-4xl` because the kit's `Title` primitive has no h1 rung — h2/32, h3/24, h4/20 only) and the 80% title/actions split, as ONE string. One census, off the disk, over `web/`, `web-portal/` and `shared/web/`: every call site that renders the kit's `<RecordDetail>` or its `<RecordChrome>` template — the app's only two ways to draw a detail screen — must apply that exact constant in its `className` AND import it from `shared/web/record-heading`, and none may pass a competing `titleSize` prop of its own. The constant's own definition is pinned too, as whole declarations rather than a substring search, so the law cannot be satisfied by an identifier that has been quietly emptied. A tripwire asserts the census found more than one path at all — a scan that goes blind reports agreement. The kit's component gallery (`tools/screen-builder/samples/`) is out by SCOPE, not by exemption: its job is to draw each vendored export raw. *Why:* the client's own words — "i want that unless other oomponents are above the tabs, the tabs are at exact same heigh in main screen and detail screen / also titels at same high / need to lok more uniform" — over the ruling she has now given three times: "i dont want you to hardcode fixes for single pages, but to state rules about components." The 44px title was a real fix for a real correction ("title on main screens still way too small! it's currently smaller than in detail screens"), written as a PRIVATE constant inside `web/components/record-chrome.tsx`. The other detail path — `renderDetail` in `shared/web/screen-engine/screen-renderer.tsx` — could not see it and fell through to the kit's own `titleSize = "h3"`, so `team.detail` (the app's own landing screen), `members.detail`, `invites.detail`, `brand.detail` and `purposes.detail` set a record's name at 24px while thirteen sibling screens set it at 44px. Every pixel of that 20px step lands on the tab strip below it, which is the height the client was pointing at. The 2026-09-01 title-row ruling ("we always reserve a % on the left for the buttons") had drifted the identical way, in the identical file. Nothing was red and nothing could have been: a DEFAULT on one path and a CLASS on the other is not a contradiction any type or test could see. The tab-strip GAP was already uniform on both paths — because it had been made a token (`--tab-content-gap`) rather than a class in one file. That is the whole difference between the half that drifted and the half that did not. | `record-title-treatment` (`web/test/rules.test.ts`) | enforced |
| R53 | ui | **The collection toolbar's slot set is the row's, not the call site's — and its sort slot is a default.** R48 made the search box a default and R50 made the whole row answer one question about emptiness; this rules the slots BETWEEN them. `<ToolbarRow>` (`web/components/deep-link/screen-bits.tsx`) draws five slots in one fixed order — search → filters → sort → view → actions — and two of them, `sort` and `view`, are STRUCTURED CONFIGS (`ToolbarSortSlot` / `ToolbarViewSlot`) the row builds the `<SortControl>` and `<ViewSwitch>` from itself, exactly as `folderTabs` is a `FolderTabStrip` rather than raw JSX. Three censuses, off the disk. (i) THE CENTRAL GUARD: `ToolbarRow`'s own source declares both slots as configs (never `React.ReactNode`, which accepts anything and therefore enforces nothing) and renders both controls itself. (ii) NOBODY ELSE BUILDS EITHER CONTROL: no `.tsx` under `web/`, `web-portal/` or `shared/web/` may render a `<SortControl>` or `<ViewSwitch>` unless named in `TOOLBAR_CONTROL_OWNERS`, where the app's other toolbar-owning components are written down with the reason each owns one — `<PagedFind>` (the door-searched half), the recipe engine's own `CollectionFrame` (whose sort options are DERIVED from the recipe's columns), and `wave-finder.tsx`, which is a hand-written second copy of `<ToolbarRow>` with a sixth slot and a different order, pinned so the divergence is a line somebody can read rather than a file the toolbar censuses cannot see. (iii) SORT IS A DEFAULT: every `<ToolbarRow>` call site passes a `sort` prop, or its ENCLOSING COMPONENT is named in `TOOLBAR_SORT_EXEMPT` with the real reason — keyed by component rather than by file because three of these files hold two or three toolbars with genuinely different answers. `view` gets no registry, and that is a property of the control rather than a gap: `ViewSwitch` renders nothing for fewer than two views, so a single-body collection is self-exempting. Both lists rot-checked both ways; tripwires fail the build if either census matches nothing. *Why:* the client's own words, 2026-09-06, on two screenshots of her own main collection screens side by side, Apps (`Search apps…` / Filter / ↑ / Name / ▦ Tiles / +) and Tasks (`Search 82 tasks…` / Filter / +): "why the fuck i still have different toolbar variations??? unify joder." Part of the answer is honest and is now written down rather than assumed — Tasks' five table tabs sort by their own column headers and its six views are a tab strip, so it offers neither picker, which is a reasoned exemption. The rest was nobody's decision. `sort` and `view` were ordinary optional `React.ReactNode` props, so a call site could pass the right control, pass nothing, or pass the control to a DIFFERENT slot — and eleven of the eighteen bespoke toolbars drew a sort control while EIGHT handed it to `search`: `<>{searchInput}{statusSelect}{sortControl}</>` on Dropdown values, Modules, both account panels, a contact's meetings, a wave's sprints and all three of Client-org's lists. `search` is the row's ONE GROWING slot, so all eight sat inside the stretching search cluster at whatever label treatment that screen typed, while Apps and Deliverables drew the identical chip in the non-growing box beside `actions`. R48, R49 and R50 could all see the row and none could see this: they ask whether a PROP is present, and a `ReactNode` slot's contents are invisible to a prop census by construction. So the fix is a change of TYPE rather than an eighteenth call-site patch. | `toolbar-slot-set` (`web/test/rules.test.ts`) | enforced |

## How to add a new law

1. Add a row to `RULES_REGISTRY` in `shared/rules/registry.ts` with a real
   `checkId`.
2. Write the check (a per-worker test, or a case in `web/test/rules.test.ts`).
3. Add the matching row to the table above.

The `registry-integrity` check verifies steps 1 and 3 stay in sync. A rule with
no working check is not a law, delete it or write the check.

## Dimensions

Laws are cross-cutting: **arch** (architecture/data), **ui** (interface),
**workflow** (how we build), **ai** (the assistant). The agent/MCP tool surface is
already held to the doors it forwards to (`workers/mcp/test/catalog.test.ts` +
`workers/data-ops/test/trace-parity.test.ts`); a natural next row is a UI law once a
new interface pattern stabilises.
