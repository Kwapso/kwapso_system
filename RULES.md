# RULES.md, the Laws of the Base (machine-checked)

These are the **laws** every part of the Kwapso System (and anything built on top of it) must
obey. They are not style suggestions, each one is enforced by a real test, so a
change that breaks a law turns the build **red**. This is how an agreed rule
actually sticks instead of quietly slipping over time.

The single source of truth is **`shared/rules/registry.ts`** (the laws as data).
This document is the human-readable twin; a check (`registry-integrity`, in
`web/test/rules.test.ts`) asserts this table lists **exactly** the law ids in the
registry, so the doc and the code can never drift. **You cannot add a law without
its check, and you cannot add a check without its law.**

Deny-lists (the reviewed exceptions for each law) live as DATA in the registry, so
every exception is a visible, conscious line. Never a silent bypass.

## The client portal, and the exceptions it earns

`web-portal/` is the second front end (the client portal; `web/` is the agency app).
It obeys the same laws with three recorded differences, and they are written here
because an exception nobody can find is a bypass with better manners:

- **R2 on the portal. `PORTAL_ACTIVITY_EXEMPT`** (in the registry). Every record
  detail in the base can reach its history, because a record's history is what
  makes a shared workspace trustworthy. The Activity TAB was retired on 7 Sep 2026 at
  the client's ruling ("kill all old activity tabs"): the feed is now reached from the
  ink footer's Latest activity eyebrow, which opens the slide-in `ActivityRail`. On the client's side that same feed is a
  disclosure: its rows name the staff who moved a ticket or edited an account, and the
  portal shows work status but never who inside the agency is doing it (SCOPE ch.06).
  Two components are listed, the ticket screen and the company screen, each with the
  sentence for why. The activity door is not on the portal gateway's surface at all,
  so this is not a hidden tab; it is a door that was never opened.
  `web-portal/test/rules.test.ts` fails if a listed component grows an activity feed,
  and if a listed component no longer exists.
- **The account fence. `PORTAL_VISIBLE_READS`** (in the registry). Every read a
  client can reach names the fence its lib function must carry.
  `web-portal/test/portal-fence.test.ts` walks the portal gateway's own door table
  through to the function behind each door and demands that function touch the
  caller's stamp. FUNCTION level, not file level, because this list once said a file
  was fenced while one of its readers was not.
- **`workers/portal-gateway/` has no `gating-seam` and no `publish-seam` suite**, a
  reasoned exemption, not an oversight. That worker owns no writes of its own and no
  tables: every `/api` request it accepts is forwarded, unchanged, to a door that is
  already gated (R10) and already publishes (R1) in the worker that owns it. There is
  nothing there for either seam test to read. What it *does* own is which doors exist
  at all, and that is checked instead by `workers/portal-gateway/test/portal-door.test.ts`,
  which derives the agency's whole `/api` surface off `web/lib/api/` and asserts every
  door the portal does not name returns a 404. A door-list worker is held to a
  door-list check.

| ID | Dimension | Law (plain English) | Check (test id) | Status |
|----|-----------|---------------------|-----------------|--------|
| R1 | arch | Every mutation route publishes a live change ping (so screens stay live). | `publish-seam` (per-worker tests: tenancy, content, data-ops; auth's two user-channel publishes and mcp's caller-private token rows are the reviewed, untested exceptions. CACHING rule 5) | enforced |
| R2 | ui | **Every record-detail screen draws its tab strip through the library `TabsView`, and every record's history is REACHABLE.** **And WHICH screens those are is DERIVED, never hand-listed.** A component under `web/components` named `*-detail.tsx`, or one that renders a `<RecordScreen>` — two independent signals, because a record detail can arrive under another name, and because neither signal is one of the obligations, so nothing here is circular: a file caught by NAME is held to the strip with nothing assumed. **AMENDED 7 Sep 2026, and the amendment is the client's ruling rather than a relaxation** — "kill all old activity tabs", the history reached instead from the ink footer's Latest activity eyebrow (`All activity · N ›`) opening a slide-in rail. Three things followed. The behavioural signal was `<ActivityPanel>` and had to move: no detail renders that panel now, and a signal nothing matches is a census gone blind — it would have caught exactly one file, the rail itself, and held it to a record detail's obligations. The per-screen `<ActivityPanel>` obligation became a per-HOST one, because the rail is wired once in `record-chrome.tsx` and once in the screen engine rather than thirteen times: the law now proves the two hosts mount it and that the rail mounts the unchanged panel, which is the same sentence — a record's history is reachable — checked where the code actually is. And a detail left with ONE panel draws no strip: `selectable-detail`'s tabs were exactly Overview + Activity, so retiring one left a strip naming the thing already on screen. `RECORD_TABS_SINGLE_PANEL` is that reasoned residue, rot-checked so it can only shrink, and read by BOTH this law and R8's tab floor so the two cannot disagree about which screens draw a strip. **It is EMPTY as of 11 Sep 2026** — its one entry was that dropdown-value screen, and the screen was retired with the whole-vocabulary screen that was its only door; every record detail in the app draws a real strip again. The list stays declared because the check reads it and because the next single-panel record needs somewhere to say so. Comments are stripped before the match (CONVENTIONS.md), the census carries a blindness tripwire, and `RECORD_DETAIL_NOT` is the reasoned residue, rot-checked so it can only shrink. *Earned by:* the registry holding an INCLUSION list, `RECORD_DETAIL_COMPONENTS`, so R2 and R8 walked exactly the screens somebody had remembered to type into it. It opened twice. `app-detail` and `process-detail` were added on 17 Aug 2026 after a tester found faults on screens no law had ever read; `sprint-detail` and `story-detail` were found missing on 18 Aug, having shipped tabs and an Activity panel that neither law had ever looked at. Nothing was red either time, because a screen no law walks looks exactly like a screen that passes. A law that enumerates its subject from a hand-kept list has a hole by construction. | `record-detail-tabs` | enforced |
| R3 | ui | Collection tab strips use the library TabsView (icon + count badge), no hand-rolled button toggles. | `no-handrolled-toggles` | enforced |
| R4 | ui | Every form/dialog renders through the shared FormShell (title+subtitle · separator · fields · separator · action). | `forms-use-formshell` | enforced |
| R5 | arch | Record activity is read through ONE generic (table, id) path, any module's history, no per-module read SQL. | `generic-activity-path` | enforced |
| R6 | ui | Product terms live in ONE glossary (clear, brief, no over-explaining), the app speaks one dictionary. | `glossary-wellformed` | enforced |
| R7 | ui | Every form dialog persists its draft per session (useFormDraft), unsaved input survives navigating away (CACHING.md §11). | `forms-persist-drafts` | enforced |
| R8 | ui | **Every tab that reveals a collection carries that collection's count, on BOTH tab surfaces.** A team section tab (`placement:'tab'`) declares a `countCacheKey`; a **record-detail** tab is badged from the block it reveals, recipe details through the `withTabCounts` seam (which collection is DERIVED from the tab's own block: `activity` → its source, `list` → its module), bespoke details in their own tabs config. A tab that shows no collection (Overview, the article body, the permission grid) says so once, as a reviewed `RECORD_TAB_COUNT_EXCEPTIONS` entry. R8 owns WHICH collection a tab's badge describes (derived, never hand-listed). The NUMBER is owned by R16 (an exact server total through `formatCount`); where the two disagree, R16 prevails. **AND WHEN A COLLECTION LEAVES THE TAB STRIP, ITS COUNT GOES WITH IT — amended 7 Sep 2026.** The client retired the Activity tab, which was the last collection tab any recipe detail declared, so the recipe scan legitimately counts zero badged tabs and the old blindness tripwire ("more than two COUNTED tabs") could only be satisfied by putting a collection back on a strip she asked to empty. A law that can only be made green by undoing the ruling has stopped describing the base. The tripwire now measures the WALK — a scan that reads no detail tabs at all is still caught, which is the failure it was written for — and the badge obligation follows the door: the footer's `All activity · N ›` must carry the history's own count through `formatCount`, and must PRINT it, because a count computed and not shown is this fault with extra steps. The per-detail floor of two tabs skips `RECORD_TABS_SINGLE_PANEL` (R2's list, read from the same registry) for the same reason. *Earned by:* every record in the app shipping an Activity tab with no count at all, the check walked the team strip, and the record tabs were built somewhere else. | `tab-counts-derived` | enforced |
| R9 | arch | The agent knows what the app can do, its system prompt carries a capability brief GENERATED from the import/export catalog (+ the glossary), so the UI and the agent can never disagree about a capability. And it knows what the app REFUSES: a vocabulary-gated write states its call ORDER (create the dropdown value first, write the rows second, one turn) on BOTH surfaces the model reads, the tool's own description and the system rule wall. **And it knows what the app can DRAW.** The assistant may emit VISUAL BLOCKS, a fenced ` ```kwapso:<kind> ` region the app renders as a real component instead of a paragraph of figures, and the set of kinds is declared ONCE (`shared/agent-blocks.ts`), so three surfaces are derived from one file and none is hand-listed: the PROMPT (scraped back out of the generated brief), the PARSER, and the RENDERER (read off disk in `web/`, because the model's promise is worth only what the browser paints; `tsc` refuses it too, the renderer map is a mapped type over the catalogue's own union). Every example the prompt teaches is proved by RUNNING the parser over it, not by reading it, the lesson R22 paid for, since an example that drifted from its validator would teach the model, in the prompt, to emit exactly what the app refuses. The wire format is a FENCE so a model that has never heard of it degrades to a code block instead of garbage, and the JSON is treated as untrusted model output (R20's posture off the request path): a bad kind, bad JSON, a non-finite number or a row count over the cap renders as inert text, never as markup. | `agent-app-parity` (workers/data-ops/test/agent-parity.test.ts) | enforced |
| R10 | arch | Every state-changing route opens with a permission gate (requireRight / gated / requireAnyImportRight / adminGuard), unless it's a reviewed identity-gated write (teamless onboarding, own-pointer, ownership) that gates on whoAmI. No ungated door ships. | `gating-seam` (per-worker tests: tenancy, content, data-ops **and mcp**, the security counterpart to `publish-seam`. The mcp suite is the EXTERNAL machine surface's own: it asserts every non-GET route there opens with token or user verification, because a door reached by a machine is reached by a stranger) | enforced |
| R11 | arch | Every external `fetch()` (a bare global fetch to the internet. D1 REST door, email sender, AI model call) carries an `AbortSignal` timeout, so a hung socket can't stall a worker. Service-binding `X.fetch()` calls are Cloudflare-bounded and exempt. | `fetch-timeout` (source-scan in `web/test/rules.test.ts`) | enforced |
| R12 | arch | Every cron / `scheduled` handler records its failures to the error store (`recordWorkerError`), unattended work has no user watching, so a swallowed background failure would vanish from the 90-day error log. (A user-facing catch that shows a friendly message should record too, a convention; see the agent's model-call catch.) | `cron-records` (source-scan in `web/test/rules.test.ts`) | enforced |
| R13 | arch | Shipping the code ships the capability: every module is an import TargetDef or a reviewed `CATALOG_EXEMPT` entry. AND the core catalogue reconciles itself against the code on READ (INSERT-only, `ON CONFLICT DO NOTHING`: a target the owner switched OFF stays off; only a never-existed row is created; the picker filters `is_active` in memory, never in SQL). *Earned by:* staging importing two modules that production, running byte-identical code, could not, rows are data, and no deploy carries data. | `catalog-coverage` (`workers/data-ops/test/catalog-coverage.test.ts`) | enforced |
| R14 | arch | **No unbounded list endpoint, and no capped GROWING one.** Every exported `list*`/`search*` function backing a collection applies a HARD CAP (`LIMIT n`, said in a comment; the caps live in `shared/workers/limits.ts`), but a collection that GROWS with ordinary use (`GROWING_COLLECTIONS` in the registry: support tickets, the activity feed) must **PAGE** instead, and page by KEY not offset: an opaque cursor, an exact total and `hasMore` through the one `pagedJson` seam (`shared/workers/paging.ts`), with a client that can actually reach page two — **its OWN pager, named in the registry (`pagerFile`/`pagerKey`) and proved in the file that holds it**. A cap is an honest refusal to answer; paging is an answer. **And the SEARCH BOX on a paged list is the same sentence:** the library frame searches the array it holds, so on a paged screen it answers about page one, so a paged recipe leaves the frame's box off (`searchable: false`) and the door answers `?q=` instead, with its exact total taken over the SAME `WHERE` (SEARCH.md layer 2 · `web/components/records/paged-find.tsx`). *Earned by:* one unbounded read stalling a worker under a 24,000-row catalogue, then the same catalogue proving a 1,000-row ceiling is just a slower refusal, and then, once every growing list paged correctly, a manager on staging typing a client's name into six of them and being told, six times, that it was not there. *Amended 2026-08-18:* the client half was asserted by a BRANCH (`!c.listRecipe`) that matched three collections while checking one collection's control, so for the team feed and the list of time it was a constant — deleting either pager left the build green. One branch cannot stand for three collections. | `bounded-lists` (source-scan in `web/test/rules.test.ts`) + `paged-search` (`web/test/paged-search.test.ts`) | enforced |
| R15 | arch | **No deaf publishers:** every resource string any worker publishes must reach a listener (`TEAM_RESOURCES` / `SIMPLE_INVALIDATIONS` in `web/lib/live-resources.ts`, or the portal's own `PORTAL_LISTENERS`) or a reasoned `DEAF_EXEMPT` entry, the publisher set DERIVED by scanning `publishChange` calls, never hand-listed. *Earned by:* a manager screen staling because its worker pinged a resource nothing listened to. **A half was retired, on purpose:** R15 also used to require every paged screen to subscribe via `useLiveRefetch`. That clause detected paged screens by matching `/search?` or `usePagedList` in `web/components`, **zero files matched**, so its offender list was permanently empty and it protected a hook with **no call sites**. The need was real when written and then went away: paging moved to opaque **cursors over the shared store**, so a paged list's rows now live in a cache key (`accounts:<team>`, `help:<team>`, `activity:record:…`) with its cursor in a sidecar, exactly the caches the row-level registry patches and the portal's listener map invalidates. No screen holds page state *outside* those caches any more, which was the hook's whole premise. So the clause and `web/lib/use-live-refetch.ts` were **retired** rather than re-detected: a law kept alive by a filter matching nothing buys confidence without paying for it. | `live-collections` (source-scan in `web/test/rules.test.ts`) | enforced |
| R16 | ui | Every screen showing a collection shows its count, exactly once. The NUMBER: a server COUNT(*) through the ONE `formatCount` seam (`shared/web/format-count.ts`, floored abbreviation at every magnitude, zero/loading render nothing, a paginated screen still badges the WHOLE collection), **counted EXACTLY up to `TOTAL_COUNT_CAP` (1,000,000) and reported as "at least" beyond it**, through the one bounded seam `shared/workers/count.ts`, for every `GROWING_COLLECTIONS` total. A total that stopped early SAYS SO in the same object: `pagedJson` DERIVES `totalCapped` from the total itself, so no door can forget it and no door can disagree with the seam that stopped counting. A door that answers with `json` rather than `pagedJson` owes that flag itself (`summariseWorkLogs`). And a **stat tile is a badge with a label**: every `value:` inside a `<StatGrid>` is a figure, held to the same seam as a `badge:`. A number that feeds a DECISION rather than a display, billable seconds, an export's completeness, stays EXACT and does not come through the capped path. The PLACE: a tab badge where the screen has a counted tab, else a `CollectionHeading`. The ARBITRATION: a React context (`CountedTabs`/`CountedAbove`), a counted tab WINS and the heading stands down, decided per-permission at render, never by a prop. **WHAT the number counts: the ROWS the collection lists, never the categories or groups they fall into.** A tab over five automations badges 5, not "1 kind of thing"; a tab over fifteen dropdown values badges 15, not the number of groups those values are filed under. R16 owns the number; R8 owns which collection a tab describes. *Earned by:* a 24,011-product catalogue advertising "1000" (a capped list's length), and the same "24k" shown twice on one screen. *Amended 2026-08-14:* an unbounded `COUNT(*)` over a growing table was the one read in the product with no ceiling at all, and the exactness it bought above a thousand was precision the ladder threw away (24,011 and 24,499 both render "24k"). *Amended 2026-08-18:* the check read `badge:` props only, so one panel shipped BOTH original failures inside a stat grid — `String(summary.total)` beside a tab strip rendering `formatCount` of the same collection, and `people.length` off a `LIMIT 50` read presented as "People on it". *Amended 2026-09-15:* the "what it counts" clause above did not exist, and one badge used the gap — the Choices tab on a module's own settings page counted GROUPS (categories) rather than VALUES, ruled that way on purpose the day before: client, 2026-09-14, "show the total count for Automations and for Choice Components categories, not for the amount of choices." A census of every tab badge in the app found it the LONE exception to "counts the rows the panel lists" — ~25 others already did, correctly, without this clause ever saying so. The owner reversed the 14 Sep ruling the next day, on seeing a badge read 1 over a panel listing 15: "I would rather all badge counts show me the count of rows rather than types, so please change it." Fixed at `module-settings-screen.tsx`'s `choiceValueCount`, which stands down to no badge (`formatCount(undefined)`) rather than a possibly-wrong one when its source — the same capped `selectable`/`meeting_purposes` reads the panel itself uses, R56, no second door — is still loading or came back at `LIST_HARD_CAP`: a wrong number is worse than no number, the refusal R23 and R42 already make elsewhere. | `counted-collections` (source-scan in `web/test/rules.test.ts` + `format-count.test.ts` + `count-seam.test.ts`) | enforced |
| R17 | arch | State transitions are idempotent: every deactivate/reactivate UPDATE carries the current-status predicate (`AND deactivated_at IS [NOT] NULL`; status moves: `AND status <> ?`), reads the changed-row count back, and when ZERO rows moved writes no activity row and publishes no change. *Earned by:* a double-clicked Deactivate writing two "deactivated" rows 2.0s apart into one record's history, history says what happened, not how many times a button was pressed. | `idempotent-transitions` (source-scan in `web/test/rules.test.ts`) | enforced |
| R18 | arch | A cross-module read carries the caller's module rights: the team activity feed subtracts denied modules through ONE shared clause (`activityVisibilityClause`, any count over the feed must reuse it), and every `relatedTable` a worker writes resolves through `ACTIVITY_GATE_MAP` or a pinned, reasoned `ACTIVITY_TABLE_EXEMPT` entry. *Earned by:* a member with a single read right seeing every module's before/after ("changed BIG-0000001 price from 4,500 to 3,900") through the one ungated feed. | `activity-gate-coverage` (source-scan in `web/test/rules.test.ts`) | enforced |
| R19 | ai | Agent/MCP filter parity: any tool sitting on a screen's list/search door EXPOSES and FORWARDS every filter that door parses, the required set is DERIVED from the door's own parameter parsing, never hand-listed. *Earned by:* the assistant falling back to free text and answering a DIFFERENT question, 3,465 descriptions that mentioned the words instead of the 134 records carrying the value. | `agent-filter-parity` (`workers/mcp/test/filter-parity.test.ts`) | enforced |
| R20 | arch | **Input is validated at the boundary, and now it is SCANNED.** Every field a worker reads off a request body must sit in a **checking position**: the first argument of a validator from `shared/workers/validate.ts` (`requireText` / `optionalText` / `queryText` / `requireIdList`, plus `parseUploadDataUrl`, the same seam for bytes), the operand of `typeof`, inside `Array.isArray()` or `Number()`, a strict comparison against a literal, or the needle of an allow-list `.includes()`. Nothing else: **a truthiness guard is not a type check** (`if (!body.id)` lets `{}`, `[]`, `123` and a 10 MB string straight through), and a cast is not a check at all. A body may not be **destructured at the read**, that scatters untrusted values into bare locals nothing can follow back to the boundary. A door that genuinely cannot validate is a reasoned `RAW_BODY_EXEMPT` line in the registry, and the list is a **ratchet**: a listed line that is no longer an offender turns the build red, so it can only shrink. *Earned by:* `POST /api/auth/email/start` with `{"email": 123}`, an unauthenticated **500** that crashed *before* the send throttle and wrote an `error_logs` row into the **global core database** on every request. And earned twice over by how long it hid: this law lived for months as a sentence in CLAUDE.md claiming to be "locked by `workers/content/test/validate.test.ts`", which locks the helpers' behaviour and the **query** half and excludes `workers/auth` outright. Every other law had a scanner; the one about never trusting a request body had prose. | `validated-bodies` (source-scan in `web/test/rules.test.ts`) | enforced |
| R21 | arch | **A door on the agency's own material refuses a client login, at the door.** Every route reachable at the AGENCY origin that a caller holding only the **Client role's** rights can pass, including every door gated by nothing but membership, must do one of four things: refuse a portal caller (`refusePortalCaller`), resolve the caller's account fence (`accountScope`), be a door the **client portal itself opens** (`PORTAL_DOORS`), or be a reasoned `CLIENT_REACHABLE_EXEMPT` line whose reason is always the same shape, *the door answers about the caller themselves*. Nothing is hand-listed: the Client role's rights are read out of the seed, the routes out of each worker's own `ROUTES` table, the gate out of the handler source (following route-local helpers, so a refusal one frame down still counts), the portal's surface out of `PORTAL_DOORS`. *Earned twice.* The client portal's gateway forwards a NAMED allow-list and leaves the agency's own doors out, saying why; the agency gateway forwards **by prefix**, and a client login is an ordinary team member holding an ordinary role, so every door the portal withheld was served to the same person at the other hostname. First the learning library, the module has since been purged, the lesson has not, and the dropdown vocabulary. Then, because the enumeration that followed listed *what the accounts module owns* instead of *what a client can reach*, the ticket **stakeholder** list, which names the agency's staff admins with their email addresses, and answered on a POST as well as a GET. **Enumerate by what a client can reach, never by what a module owns.** | `client-reachable-doors` (source-scan in `web/test/rules.test.ts`) | enforced |
| R22 | ai | **Agent/MCP body-field parity**. R19's sentence about the OTHER HALF of the request. A tool sitting on a WRITE door exposes and forwards every field that door reads off the request **body**. The required set is DERIVED from the door's own `body.<field>` reads, its handler plus any helper in the same file it calls, one level deep (R20 is what makes them legible: a body is never destructured at the read). That one level is R19's blind spot closed in the same breath: while the scan read the handler alone, a door that factored its parsing into a helper beside it dropped out of the census entirely and its tool's obligations silently became none, a law that stops looking when you tidy up is a law that rewards tidying up. The FORWARDING half is proved by **running** the tool's `buildBody` on a filled-in call rather than reading its source, so a builder that delegates to a helper is judged by what the door actually receives, not by which words happen to appear in it. A field deliberately left off is a named line in `NARROWED_BODY_FIELDS` with its reason, said again in developer English in MCP.md §3; the list is a **ratchet**, an excuse in front of a field the tool now exposes turns the build red. *Earned by:* R19 deriving its obligations from `searchParams.get(...)` only, so four write tools quietly offered a narrower contract than their door accepted, for six weeks, under a green build, `update_team` could not set the logo, `create_role` could not carry its permission matrix, `reply_help_ticket` could not @mention, `agent_chat` could not attach a file. A law that only inspects the query string measures your query strings. | `agent-body-parity` (`workers/mcp/test/filter-parity.test.ts`, beside R19) | enforced |
| R23 | ai | **An answer from the knowledge base carries its sources, or it is not an answer.** Retrieval never writes prose, it hands back the passages it found and the sources they came from, and the assistant composes the reply with those in front of it. So `found`, `passages` and `citations` are ONE decision, made in ONE place (`knowledgeAnswer` in `workers/content/src/lib/knowledge.ts`): no citation means no passage, and a sentence the assistant must say instead of inventing one. No door may assemble that response by hand, the same shape as R14's `pagedJson` seam, and for the same reason: a door that builds half a contract ships half a contract, and here the missing half is the difference between *"we have nothing on that"* and a confident answer with nothing behind it. The compartment a question was answered from, and the REASONING that chose it, ride the same object, because a wrong compartment is invisible otherwise. **And now the assistant writing it is sometimes US** (2026-08-18): the Knowledge tab asks the door to compose the reply, one cheap model call, so the law's own sentence became a code path rather than a hope about a chat panel. It did not bend to make room. The prose is an INPUT to `knowledgeAnswer`, decided in the same expression as the citations (`answer: found ? …`), so a written answer cannot outlive its sources; the writer (`lib/knowledge-compose.ts`) is handed the settled passages and citations, reads nothing about `found`, and hands back a string; the door may not name `answer:` any more than it may name `passages`; and the passages reach the model FENCED, because half of them are words a client wrote. *Earned by:* the owner's own sentence in the brief, *"an answer with no source is a bug, not a style choice"*, and by what a sourceless answer costs where this one is aimed: an agency repeating it to a client. | `cited-answers` (`workers/content/test/cited-answers.test.ts`) | enforced |
| R24 | arch | **A conversation that has read a WITHHELD figure may not then write where the client reads.** Per turn, refused at the step, before the door is called. `GET /api/tenancy/app-money` hands over what one app gives back priced IN FULL; the client's own value door (`GET /api/tenancy/impact`) nulls those prices on any app whose account has price visibility switched off. Same subtraction, one of them unredacted, so a figure out of it is a number a particular client may be forbidden to see. The chain it closes is an INJECTED one: a client raises a portal ticket carrying 20,000 characters of their own prose, that prose is read by the model the next time anybody here asks a question that touches tickets, and `reply_help_ticket` is a write gated on `help:read` — the lowest bar in the catalogue — confirming only when it @mentions somebody. Every door on that path does its own job and the number still lands in the client's thread and their inbox. Both sets are DERIVED: the money doors from tenancy's own `ROUTES` and every handler's own source (the pin in `shared/workers/money-taint.ts` must equal them exactly), the client-readable doors from the non-GET half of `PORTAL_DOORS`, and the TOOLS from those doors off the shipped catalogue at load time, so a money tool added tomorrow on an existing door is covered the moment it exists. It **refuses before it defers**: a confirm ends the turn and `confirmAndRun` resumes from a stored row that remembers nothing about it, so a proposal is judged when it is made *and* again as it runs. MCP cannot use this predicate at all — one `tools/call` has no turn, so `moneyIsInContext` is always false there and the ported check would have passed with the hole open — so that surface refuses the money door outright, at `forwardTool`, asked of the door the call will actually open rather than of the tool's name. **THE INBOUND HALF WAS RETIRED ON 10 SEP 2026, and the retirement is the more interesting half of this row.** R24 was written about `internal_rates`: what an hour of our own work cost us, and the margin that was computed from it, which until that day lived in ONE file nothing a client login could reach imported — structural rather than conditional, because a condition can be inverted and a permission can be granted while an import cannot be forgotten. The client retired that whole feature — *"kill the whole internal rates thing. will develop this in the future much much more but for now i iwanna wipe it clean"* — and the two tables, seven doors, six tools, two screens and `workers/tenancy/src/lib/internal-money.ts` went with it, along with the margin panel that was the margin's only consumer. It was **retired rather than re-pointed**, and that was the decision, not an omission: after the removal there is no structurally-fenced number left in this base. Every money figure that still reaches a client reaches them because a CONDITION let it through — a sprint's sold price behind their account's price-visibility switch, a step's role rate behind the main-stakeholder fence, an app's running cost nulled on the row for a portal scope — and not one of them is fenced by the import graph, which is the only thing the inbound half ever measured. A law whose whole doctrine is "not conditional" cannot take a subject whose exposure is a condition without asserting something false. **That sentence was corrected the same day it was written, and the correction belongs in the row.** It first said that every surviving money surface *is shown to a client deliberately, behind their account's price-visibility switch*, and it enumerated three: the account rate card, which was removed an hour later, a sprint's sold price, and the savings priced off the client's own role rates. An hour later the client retired the account rate card too (*"the whole account rates also killed it"*), which took the first item off that list and left the switch governing exactly ONE figure rather than two. The doctrine survived the edit because it never rested on WHICH surfaces exist; the enumeration did not, and a stated `why` that has quietly stopped being true is the rot these laws are written against. R15's retired half is the precedent and its sentence applies word for word: *a law kept alive by a filter matching nothing buys confidence without paying for it.* The outbound half never rested on the import graph, which is why it survived its own subject. What it paid: its door list is now derived from a NAMED SET OF FUNCTIONS (`MONEY_READERS`) rather than from a file's exports, which is weaker — a name is a decision somebody makes where an import was a fact somebody could not forget — and that is written down in `money-taint.ts` rather than hidden. *What did NOT go with it:* the suites that used an internal rate as their FIXTURE were re-pointed rather than deleted, because each measures a MECHANISM and not a number — the query-door fence now hunts a Google refresh token, the sort-menu clause forbids the switch-gated price columns, and both taint suites moved to `get_app_impact`. Only the margin's own arithmetic tests were retired, because a pure function has nothing to re-point AT. **Two of those re-points had to be made TWICE**, an hour apart, because the first one landed on the account rate card, which was itself removed an hour later: the knowledge sweep's fence went from the internal-money file, to the charged card's library, to the TEAM SCHEMA's own money columns — the database's own shape, which cannot be deleted by a ruling — and the query door's positive control went from the charged card to a sprint's sold price. Each move made the oracle stronger, which is the argument for re-pointing rather than deleting, made twice in one afternoon. | `money-taint-outbound` | enforced |
| R25 | ui | **A savings figure never renders without saying what it is made of.** Every screen on either front door that shows a saving renders `SAVINGS_CAPTION` (`shared/workers/savings.ts`) word for word: *the times are estimates we agreed with you; the subtraction is arithmetic.* The screens are DERIVED from the payload they read (`savedSecondsPerMonth` / `savedHours`), never hand-listed, so a new screen is held to it the day it is written. *Earned by:* the owner naming "the numbers stop being believable" as one of three things that would make him abandon this and go back to a spreadsheet. A client who understands that the inputs are agreed and the arithmetic is arithmetic trusts the figure; one who believes we held a stopwatch stops trusting every other number in the app the day one of them looks wrong. The caption is not decoration around the feature, it is half of it. | `savings-caption` | enforced |
| R26 | arch | **The vector index NARROWS; the team's database DECIDES.** The knowledge base's search moved out of the per-team database into one account-wide Vectorize index, which trades a structural property for a usable one, so the structure is bought back twice on the other side, and both halves are the law. **First, tenancy is a PARTITION, not a filter:** every call into the store passes `namespace: guard.teamId`, built in one function (`namespaceFor`) from the caller's own guard and never from anything on a request. Vectorize applies a namespace *before* the search and a query may name exactly one, so a query cannot reach another tenant's vectors even if every metadata filter were wrong. **Second, and this is the clause that makes the first survivable: nothing readable comes out of the index.** It is asked for ids and scores alone, `returnValues: false`, `returnMetadata: "none"`, and a hit type with no text field, and every passage in every answer is then read back out of the TEAM'S OWN DATABASE, under the caller's own owner clause, with excluded sources gone. So a mislabelled or stale vector can cost a relevant passage; it cannot produce one the caller was never allowed to read. *Earned by:* the decision this replaced, which kept vectors in the team's own D1 precisely because "do not put every team's vectors in one undifferentiated index and rely on a filter you wrote correctly today", a correct objection that had to be ANSWERED rather than waved at when the search outgrew a SQL statement. The check does both halves: it reads the two files off disk, and it runs the doors against an index deliberately planted with lying vectors (one claiming the team shelf for a private source, one left behind by an excluded source, one belonging to another team) and asserts that none of them reaches an answer. | `vector-fence` (`workers/content/test/vector-fence.test.ts`) | enforced |
| R27 | ai | **Described contracts, every `backticked` identifier in a tool description names something real.** One of: an argument the tool's own schema declares; a query param or body field its door reads (the same door census R19/R22 stand on); a field its response actually carries, the `pagedJson` contract (`total`, `totalCapped`, `hasMore`, `nextCursor`) and the door's own extras read out of the call itself (help's `mineTotal`, work-logs' `totalSeconds`), the handler's own `json({…})` literals, the row fields the module's libs map off a database row (`stepKey: r.step_key`, the one mapper shape the base uses), and R23's `knowledgeAnswer` seam; another tool's name on either machine surface; or a reasoned `DESCRIPTION_VOCABULARY` entry. The vocabulary is a **deny-list in spirit**: every entry carries a reason, an entry nothing uses turns the build red (the rotting-entry rule), and so does an entry a derived class already covers. Nothing hand-listed that can be derived: tools from the three catalogue files themselves, doors from each worker's own switchboard, reads from handler source, response keys from the seams and the module's own libs. **Scope, stated honestly:** the law checks *identifiers*, not sentences, a false sentence built from real identifiers ("filters by `status`" on a door whose rows merely carry status) is R19/R22's to make pointless, since the schema either exposes the filter or doesn't. What this law ends is a name that exists *nowhere* surviving review, because only humans read prose and humans assume a green build checked it. *Earned by:* 2026-08-16, a session reverted a tool description to a false promise, added two parameters that do not exist and a fake filter claim, and `npm run check` passed. R19/R22 read the machine-readable contract (schema, `buildQuery`, `buildBody`) and never the prose, so the words a developer actually reads were the one unchecked surface on the two catalogues. | `described-contracts` (`workers/mcp/test/described-contracts.test.ts`, on the same door census as R19/R22, the suite also replays the three lies from the proof against its own classifier, so a green run means all three still go red) | enforced |
| R28 | ui | **A string the app says is in the catalogue, and the catalogue says nothing the app doesn't.** `shared/i18n-strings.json` must be EXACTLY the set of user-visible English sentences the two front doors say, derived by re-running the one shared definition of what a person reads (`scripts/lib/i18n-source.mjs`, the same walk the extractor writes from and the adoption codemod wraps), never by a second description of it. That definition answers TWO questions and both are derived. **WHICH POSITIONS** a person reads (the seven), and **WHICH FILES** — `appFiles()`, the front doors' own IMPORT CLOSURE. A file is walked because a front door imports it, not because of the folder it sits in. Four failures, named separately because they are four mistakes. **MISSING**: a sentence in the source that is not in the catalogue. English is the KEY (`shared/i18n.ts`), so an uncatalogued string is translated nowhere and ships in English to a reader who chose German, silently, on a screen that looks finished. **ORPHAN**: a catalogue entry no string in the app matches. Nothing breaks today, which is exactly why it rots: a deleted screen's sentences stay in the file, are translated on every build, and turn the catalogue into a record of what the app USED to say. **UNREACHABLE**: a file under `web/`, `web-portal/` or `shared/` that says something a person reads and that the walk never opens — censused off the DISK, so a resolver that missed an import cannot hide behind its own graph, with a reasoned `UNWALKED_OK` line the only way out and a rot check so the list can only shrink. **The walk stops at ONE directory**, `shared/ui/`, the vendored component library. It always did, and by a rule nobody had to write: the library arrived as a bare `@kwapso/ui/...` specifier, and `resolveImport` refused every bare specifier as *somebody else's code and not ours to translate*. Vendoring it on 2026-08-22 changed its ADDRESS, not what it is — a generic component library whose strings are DEFAULTS, while the app says its own words through the props it already translates (`emptyText`, `placeholder`, `label`, `empty`). So the refusal that used to be free is now a stated line in the same function, and the reason is in `VENDORED_UI_SCOPE`. Reversing it would be expensive in the direction nobody notices: ~150 library strings would enter the catalogue in English, and only `scripts/i18n-translate.mjs` turns an English key into German, which spends real money per run. **The residue is real debt and is written down, not waved away** — a few of those strings are screen-reader labels no prop can override ("Close" on Dialog and Sheet, "Loading" on Spinner, "Toggle theme" on ModeToggle), so a blind reader who chose German hears them in English; they are listed in `NEEDS-A-SPEC.md`. And one more clause, the same sentence from the call site's end: **the call site and the definition never disagree** — every `t("…")` literal must be a string the walk accepts, because a developer wrapping `t("of")` has DECLARED it copy while `isUserVisible` refuses it as a non-sentence, and the answer is never to widen the predicate but to write the whole sentence with a `{hole}` in it, which is also the only shape a translator can reorder. And a fourth: **A LANGUAGE THE APP NO LONGER SPEAKS** — a translation on disk for a code outside `LANGUAGES`. `shared/i18n.ts` calls that array the one place a language is decided, which was true of everything DERIVED from it and false of the two files that ACCUMULATE: on 2026-08-20 the app declared four languages while 1.4 MB of Javanese, Hausa and Thai rode in the bundle behind it, and the generated catalogue's own header still claimed twenty-eight. Narrow on purpose — a language the app speaks but has not translated yet is a partly-translated app, which is this law's founding position on missing words. `node scripts/i18n-prune.mjs` fixes it and only ever REMOVES; filling a language IN costs money and is the translator's job. A ratchet, the same shape as R20's exemption list and R27's vocabulary. *Earned by:* the pipeline being BUILD-TIME by design (13.5, every language the app speaks produced once per deploy, costing nothing at runtime), which makes "is the catalogue current?" the assumption the whole feature rests on, enforced until now by somebody remembering to run a script. The extractor already had a `--check` mode; it was wired to nothing. And earned a second time by the answer to "current *against what?*": the set was six hand-written folders, so `formatRelative` in `shared/web/format.ts` returned "just now" and "5d ago" in ENGLISH to nine call sites across both front doors — a German reader was shown *Erstellt von Aurora · 5d ago*, the translated half and the untranslated half in one line — the whole language settings screen and the whole text-size section sat in `shared/web/` already wrapped in `t(...)` and in no catalogue, and `shared/scale.ts` held "Compact", "Comfortable" and "Large" one directory outside the walk. A folder list cannot be made correct, only current, and current is exactly what nobody notices going stale. The fix is one command, and the failure says so. | `catalogued-strings` (`web/test/catalogued-strings.test.ts`) | enforced |
| R29 | ui | **The page has one width, and a screen does not get its own.** Each front door owns exactly ONE page container — `web/components/shell/app-shell.tsx` at `max-w-none`, `web-portal/components/portal-shell.tsx` at `max-w-3xl` (narrower on purpose, UI-RULEBOOK L5) — and no other component may set a page-level width. A page container is identified POSITIONALLY, the way R20 identifies a checked field: one line carrying `mx-auto`, `w-full` and a `max-w-*` together, which is the signature of a centred content column and of nothing else. A dialog, a sheet, a door card, a chat bubble or a capped line of prose is not centred-and-full-width, is not caught, and keeps its own measure (L2: prose is capped, the page is not). Exceptions are DATA in `SCREEN_WIDTH_EXEMPT`, each carrying its reason, and every one is **rot-checked**: an entry whose file no longer sets a width turns the build red, so the list can only shrink. *Earned by:* the owner naming it as an inconsistency rather than a bug — work logs, tasks and meetings use the whole width and he could not see why other pages did not. The answer was that those three render through the one shell and six screens capped themselves at `max-w-2xl`/`max-w-3xl`, at 60% of the room they had. One of the six was the shell's OWN loading skeleton, so every cold load of the agency app visibly snapped sideways as 672px of skeleton became 1120px of content. UI-RULEBOOK L1 already said "one container, one cap" and round one implemented it in `deep-link-screen.tsx`; it stayed unimplemented in six other files, silently, under a green build, because a width is invisible to every other check here — TypeScript sees a string, the lint sees a string, and no test reads layout. **Earned a second time, 31 Aug 2026:** `deep-link-screen.tsx`'s own return only reaches five of the ten screens `AppShell` renders — home, settings, profile, invitations and kwapso return earlier, straight into the shell, and never hit that wrapper — so those five ran uncapped, invisibly, until a wide monitor showed Kwapso running past 2100px while Meetings, three clicks over, stopped at 1600. The cap moved up to `app-shell.tsx`'s own content div, the one node every screen the shell renders passes through, so it is set once and every screen inherits it the same way. **Earned a third time, same day, twice more:** live on staging, the client called `1600px` too narrow — "screen width. you made them thinner! i wanted them even wider!" — answered with a guessed fluid cap, `min(96vw,2200px)`. Then a real target, not a bigger guess: "wider screens, align to the right same level as breadcrumbs!" The breadcrumb row (the shell's own `header` prop) sets no width of its own — it already runs to the full padded edge of the content column — so `max-w-none` is what makes the page's own right edge land on that same edge, on every rail state and every viewport, which no guessed number (fixed or fluid) can promise. | `one-page-width` (`web/test/rules.test.ts`) | enforced |
| R30 | workflow | **Every email is classified, and one that names a record carries the way back to it.** Every function in `workers/` that composes a branded message (the `brandedEmail` / `sendBrandedEmail` seams, read off each file's own imports so an alias cannot dodge it) must appear in `EMAIL_CENSUS` as either **`refersToRecord: true`**, it is about a specific record, so it passes a `ctaUrl` built through the ONE helper, `shared/workers/record-link.ts`, or **`false` with a real reason**, and then it must NOT pass a `ctaUrl`: an email that names no destination may not promise one. Both directions, like R27's vocabulary, an unclassified send fails the build, and a census line matching no send is an orphan and fails too. **No email spells an origin.** The two front doors arrive as configuration (`PUBLIC_APP_URL` / `PUBLIC_PORTAL_URL`) because THE RECIPIENT DECIDES WHICH APP THE LINK OPENS: staff read the agency app, a client contact reads the portal, and the same ticket therefore has two addresses. An agency URL in a client's inbox is a link they cannot open and a door they may not pass, advertised (R21), so the helper takes the audience, a record kind with no screen on a front door returns `null` rather than an invented one, and a portal address never carries the agency's team id. *Earned by:* the mention notice reading "Open the ticket to read the full conversation and reply" in an email that contained no link of any kind, and no email in the fleet having one. The copy instructed an action the message made impossible, and nothing in the build could tell the difference between an email that links and one that only talks about linking. Buttons alone would have fixed twelve emails; the thirteenth, written next year, would ship the same way. | `linked-emails` (`web/test/linked-emails.test.ts`) | enforced |
| R31 | ui | **Two radii and no third, spelled the kit's way.** A rectangular surface is `rounded-[var(--radius)]`, a pill is `rounded-pill`, and a directional variant of the first (`rounded-t-[var(--radius)]`) is the same word applied to one edge. **THE LAW CONSTRAINS THE VALUE, NOT THE POSITION**, and always has — `two-radii` admits any `rounded-(t|b|s|e)-[var(--radius…)]` because the value and the token are the two radii's own. What the prose keeps is a list of the positions that draw one, so a reader can tell a new corner from a new radius, and it grew by one on **2026-09-10**: (1) a SHEET that meets the bottom of the screen, and (2) **the top band of a PINNED TOOLBAR**, which carries its container's own top corners with it while the container's real top edge has scrolled away — R63 part 4, the client's *"When pin, I still want it round. That's exactly what I asked for, so do whatever you have to do."* The band is `--pinned-behind` (the ground behind the container) with a `::before` painting `--pinned-ground` over it at `rounded-t-[var(--radius)]`, so the notch shows exactly what the container's real corners show at rest; `shared/web/pinned-chrome.ts` part 4 carries the measurement and the two-faults argument. Recorded here rather than left implicit because the file would otherwise read as a breach of a law it obeys — and the kit's own `list.tsx` had already spelled the same word on a list's FIRST ROW, which is neither a sheet nor a pin, so the one-position prose was describing the code inaccurately before this change too. **THE SPELLING BECAME THE KIT'S ON 2026-08-27, when the kit became canon.** Kit `docs/RULES.md` §4.2 forbids `rounded-lg`/`xl`/`2xl`/`md`/`sm` outright and asks for `rounded-[var(--radius)]` and `rounded-pill`. Substantively identical — `rounded-xl` renders at 24 — but ONLY because `tokens.css` loads after Tailwind's theme: Tailwind emits `--radius-lg: 0.5rem`, `tokens.css` emits `1.5rem`, and the kit wins by cascade order alone. So 184 corners across both front doors were correct by IMPORT ORDER rather than by declaration, and no check stood under that — reorder `globals.css` and every card silently becomes 12px with the suite green. Respelled in one commit; `rounded-pill` graduated out of `RADIUS_EXCEPTION` because it is the primary word now, not an exception. No other step may be written in `web/`, `web-portal/` or `shared/` — with ONE admitted exception, and the design kit is why. **`rounded-select` (6px), on the mark of a selection control.** Two numbers cannot draw everything: at `rounded-xl` a 16px checkbox is a lozenge and at `rounded-full` it is a radio button, so this is the one place a third number is unavoidable rather than convenient. The kwapso kit rules exactly this (ruling 03) and its own specimen calls it *"the ONE named exception to the two-radius law: 6px. Nothing else may."* It is DATA in `RADIUS_EXCEPTION` with its reason, and rot-checked in the shape R29's width pins and R32's colour literals already use: an entry nothing uses turns the build red, so the vocabulary can only shrink. The kit names a SECOND exception — 4px for a bar or a heat cell, *"a bar is not a box"* — and it is deliberately **not** defined here, because nothing in either app draws one and an unused exception is exactly what the ratchet catches. It arrives with the commit that needs it, which is the only moment anyone can check its reason is true. A THIRD entry, `rounded-ss-none`, is zero rather than a number — the assistant panel's own leading corner, removed where its new folder tab (2026-09-03, matching the content card's trail) attaches, the exact argument `screen-shell.tsx` already makes for that card's corner one directory over, out of this file's reach because that surface is vendored. **A third BOX radius is still forbidden.** *Earned by:* the cheapest rule here to obey being the most broken — 63 of 125 radius classes were off-vocabulary (`rounded-lg` 52 times, `rounded-md` 7, `rounded-2xl` 2) and changing every one of them moved NOTHING on screen, because `--radius-sm` through `--radius-3xl` are all `var(--radius)`. Five words for one value is five decisions a developer can make where there is only one, and the day the theme gives those steps different numbers the app acquires five radii it never chose. The law could not ship before the fix, because a law that arrives with a 57-line exemption list is a list. The bare `rounded` class is deliberately NOT caught: it is 4px here, not 24, so on an inline highlight and a 32px thumbnail it is a different value doing a different job. **The vendored library is IN scope, and the way it got there is the point.** `shared/ui/` arrived writing `rounded-sm/md/lg/2xl` at about sixty sites, and it could not simply be swept: the sentence above ("all `var(--radius)`") had stopped being true of the code the law now scanned, because that file defined the four steps as four different `calc()`s, so rewriting the classes would have RESHAPED every button, input, menu and card rather than moved nothing. It was excused in `VENDORED_UI_SCOPE` for exactly one day, with a rot check asserting the directory still offended. When the reskin's shape stage collapsed 45 of those radii into `rounded-xl` and pinned every Tailwind step back to one `--radius`, that check went red naming its own exemption, and the exemption was deleted by the commit that earned it. The sentence is true again, and the ratchet is the reason anyone can say so. | `two-radii` (`web/test/rules.test.ts`) | enforced |
| R32 | ui | **Every colour resolves through a token.** No screen may name a Tailwind colour ramp (`amber-*`, `emerald-*`, `slate-*` …) or write a hex literal. What a colour MEANS has a token — `warning`, `success`, `destructive`, `muted`, `primary`, `chart-1` to `chart-5` — and a mark comes from the chart series (UI-RULEBOOK C6). The six files that legitimately hold a literal are data in `PALETTE_LITERAL_OK` with a reason each, rot-checked so the list can only shrink. *Earned by:* `import-screen.tsx` saying `amber-600` and `emerald-500` where it meant warning and success, so the one screen in the app that reports a RESULT was the one screen a rebrand could not reach; `shared/departments.ts` holding five hexes the LEGACY app chose, none of them one of kwapso's own seven, so a department dot was the single mark on screen that did not belong to this product's palette; and the month grid painting today's square `#ffe9b0`, which is the soft mango `--secondary` already resolves to, written out by hand. None of the three is a bug anybody would file — they all look fine, in one theme, on one day. Colour drift is only ever visible in aggregate, and nobody sees the aggregate. | `closed-palette` (`web/test/rules.test.ts`) | enforced |
| R33 | ui | **Every extracted position asks for its translation.** R28 makes the catalogue match the code; this makes the code READ the catalogue. Every position `scripts/lib/i18n-source.mjs` reports in `web/` or `web-portal/` — the SAME one definition, read the other way round, so the two laws can never disagree about what a sentence is — must sit inside a `t(...)` call. Two ways out, and they are different in kind. **Positional:** a `label:` or `helpText:` on an object that spreads a field config is translated on the way to the screen by `shared/web/field.tsx`, because `t` is a HOOK and a field config is a module-level constant — those words genuinely cannot be wrapped where they are declared, which is the one class of string in this app a developer could not have fixed by remembering to. That exemption is held shut by an IMPORT BAN: no file in either front door may import `Field` from the library directly, so the seam cannot be walked around (an import cannot be forgotten the way a `t(` can). The ban matches the PATH TAIL of the kit's Field — `components/field/field` since the kit's v1.1.0 layout move folded `controls/` and `structures/` into one `components/` on 2026-08-27 (it was `controls/field/field` between the 2026-08-25 design-kit swap and that move, and `primitives/field/field` before the swap). The tail must move with the file: after the FIRST rename the old `primitives/…` tail matched nothing, so the ban passed vacuously for one evening until the story pass caught it — a ban is only as alive as the path it names. The SECOND rename is why the check no longer trusts the literal on its word: `web/test/wrapped-strings.test.ts` asserts the seam itself imports that exact tail before using it to judge anybody, so a third rename fails loudly instead of reporting all clear. *(This sentence itself named the dead `controls/…` tail from 2026-08-27 until 2026-09-06 — the check had moved and its description had not, which is the documentation half of the same defect.)* **Pinned:** a copy TABLE read back through `t` somewhere else — the screen recipes, the nav registry, the sort and filter vocabularies, the portal's status words — is data in `TRANSLATED_WHERE_READ` with the `via` call that reads it, rot-checked both ways so the list can only shrink. *Earned by:* R28 being perfectly satisfiable by an app that speaks English to everybody, and on 2026-08-18 it was — 666 of 2,001 extracted positions were in the catalogue, translated into all twenty-nine languages of the time, every build, and never asked for: every form field label in the app, 119 toasts, both error boundaries, and most dialog titles (a ternary is a JSX EXPRESSION, so they were the last hole R28 closed and the first this one found still English). *Scope, stated honestly:* it stands entirely on the positions R28's walk can SEE. A sentence in a position the walk does not visit is in neither the catalogue nor this census; widening the walk is R28's job, and every widening lands here for free. | `wrapped-strings` (`web/test/wrapped-strings.test.ts`) | enforced |
| R34 | ui | **The glossary is the dictionary the SCREENS speak.** R6's second clause, finally read by something. Every user-visible English sentence in the two front doors — `shared/i18n-strings.json`, which R28 makes exactly that set, re-derived from the source on every build — is checked for a KNOWN SYNONYM of a glossary term. The banned words are DATA (`GLOSSARY_SYNONYMS`), each naming the term it competes with and carrying its reason; a sentence with a real claim to keep one is a line in `GLOSSARY_SYNONYM_OK`, rot-checked in both directions so an exemption whose sentence has been reworded goes red and the list can only shrink. Two more rot checks hold the list to the dictionary: every banned word names a glossary key that still exists, and no banned word may appear in the glossary's own term or definition — a rule that contradicts its dictionary is a rule people learn to exempt past. It is deliberately NARROW: a word earns a line only when, in this app, it can mean nothing but a record that already has a name. "client" is left off (it is the relationship, not the record), "option" is left off (the glossary's own definition of Dropdown values uses it), "request" is left off (it means a ticket on five screens and an HTTP call on three). *Earned by:* `glossary-wellformed` reading the glossary FILE and never one line of copy, so for a year the half of R6 a person actually experiences was enforced by nobody — and the app shipped green calling the same thing "Permissions" on the Roles screen and "access rights" on two others, plus "teammate" for a member, "cost card" for the internal rates, since removed — that deny-list line was re-pointed at the surviving Rate card on 10 Sep 2026 and "Portal login" for portal access. Two words for one thing is not a typo: a manager reading both has to work out whether they mean the same, every time, and the answer is not on the screen. | `glossary-in-copy` (`web/test/rules.test.ts`) | enforced |
| R35 | ui | **A record never appears without its face.** Wherever a record or a dropdown value is shown to be CHOSEN or SCANNED — a picker option, a row in a collection, a row in a nested panel inside another record's screen — it carries its visual beside its name: its own picture where it has one, its type's glyph where the type has one, its initial where it has neither. Never nothing, and never the glyph written INTO the words, which is a pictograph inside a sentence and the one shape UI-CONVENTIONS §5 refuses. *Checked at the three places it can be LOST, not by inspecting markup:* `PickerOption` and `PickableRecord` must DECLARE the visual fields (a type that drops a picture makes the omission invisible at every call site); every list recipe must name its `leading` column; and the one shared nested row takes its mark as a REQUIRED prop, so `null` is a visible answer and an omission is a build error. *Earned by:* a census on 19 Aug 2026 after the owner asked for this a third time — 33 pickers, none of which COULD show a visual because the option type had no field for one; ten of fourteen list recipes naming no leading column; ~20 nested panels drawing bare words for records that lead with a glyph on their own screen; and `PickablePerson` dropping `imageUrl` one line before every picker that offers a person. There is no honest regex for "this JSX is a record row" — `.map(x => <li>` matches attachments, replies and comments too — so the rule stands on chokepoints instead, the same move that makes `record-picker.tsx` the only composer of `command`. | `records-carry-their-face` (`web/test/rules.test.ts`) | enforced |
| R36 | arch | **Every switch on the permission matrix decides something.** The grid gives every module four boxes whether or not four decisions exist behind them, so an inert box looks exactly like a live one — and an owner who ticks it, saves it and gets a green toast has been told they granted something. The offered set is DATA (`MODULE_OFFERED_RIGHTS` in `shared/team-modules.ts`, exceptions only, each carrying its reason, so the list can only shrink as doors get written); the consulted set is DERIVED off the source, from all FOUR places a right is asked for — a literal `requireRight`/`gated`/`gatedBody` pair, the MCP `TOOL_GATES` strings, every module in `ACTIVITY_GATE_MAP` (which the record activity feed asks for `read` on) and every import `TARGETS` module (which the importer asks for `create` on). *The check fails BOTH ways.* Offered-but-unasked is theatre. Asked-but-unoffered is the half that matters: a door written against a right no role can hold refuses EVERYBODY, and the Admin role is locked, so nobody can tick their way out of it. *Earned by:* the owner opening the Roles screen on 21 Aug 2026 and asking what the twenty-three rows were for. Fifteen of the eighty-eight boxes decided nothing, seven of them with no note anywhere saying so; one whole module (`screens`) had four boxes and no door, because both its doors gate on `teams:update`, which is the correct answer for a team setting; and `learning` and `marketing` still held permission rows six weeks after the product purged them, because a purge deletes forward and migration 0021 had CROSS JOINed them onto every role that existed. A census that knew only about literal pairs would have called four LIVE rights dead, `work:create` among them — which is why the derivation names all four sources. | `offered-rights` (`web/test/rules.test.ts`) | enforced |
| R37 | arch | **A link inside the app never leaves the shell.** The whole post-auth app is ONE client-resolved shell (`web/components/deep-link/deep-link-screen.tsx`) that mounts once and never unmounts, so every move within it is a History-API push through the single soft-navigation bus (`softNavigate`, backed by the host's `go()`) and nothing is fetched. A bare `<a href="/t/…">` opts out of all of it: the browser throws the document away, every module re-runs from nothing, the warm cache and any running agent are destroyed, and the boot mark plays again — because its trigger is a module-level `sessionCache` that only a fresh document can reset. So an in-app destination is written with `<InAppLink>` — a REAL anchor, so middle-click, copy-address and screen readers all still work, with only the plain left click intercepted — or it carries that same interception inline. *The check is a census OFF THE DISK* of every component's anchors, classified by where the href points: inside the app, an `/api/` door, a pre-auth route, or elsewhere. *Earned three times by one class of bug, green each time:* the knowledge base, missing from `TOP_LEVEL_MODULES` so every tap on it left the History API (the note survives in `deep-link/route.ts`); "Manage dropdowns", reported by the owner on 24 Aug 2026 in words that name the mechanism exactly — "I can see the app reload because I see the boot loading animation"; and the since-removed internal rate card, whose own comment said it copied the dropdowns link, which is what turns a typo into a pattern (that screen was removed on 10 Sep 2026, and the bug it earned this law with was real when it shipped). The guard that existed could not see two of the three: it read SIX hand-listed files for ONE spelling (`router.push`), and a bare anchor in any of two hundred other components is a different spelling in a file nobody listed. R21's lesson arriving in a second module — enumerate by what NAVIGATES, never by what somebody remembered to write down. | `in-app-anchors` (`web/test/shell-nav.test.ts`) | enforced |
| R38 | ui | **A record detail may not look its record up in a page.** A screen showing ONE record of a collection R14 makes page (`GROWING_COLLECTIONS`) must read it BY ID — a dedicated per-record door, or a `<module>:one:<id>` read beside the list — never a `find` over the cached list, which holds only the loaded prefix. *The check is positional*, as R20's is: it ties the `find` to the query VARIABLE built from a paged cache key, so a find over a BOUNDED collection (apps, member roles), where page one IS the collection, is never caught. The by-id key must then reach a listener like any other (R15), or a status change patches the list and leaves the open record showing yesterday. *Earned:* the owner opened a ticket from triage on 26 Aug 2026 and was told "That ticket no longer exists." It existed — number 1,030 of 1,820 — and the whole lookup was a `find` over the newest fifty. Every ticket past the cursor was unreachable by direct link, from an email button, from a bookmark, and the screen made the most alarming claim available to it. Nothing was red: the door had answered `?id` since paging landed and says so in its own comment, and `helpOne` had been written and never called. Three sibling screens already had it right. R14 made the lists page and nothing asked what that did to the screens reading them. **WHERE IT BITES BY MACHINE, and where it does not (stated 7 Sep 2026):** the census walks `web/components` and only files named `*-detail.tsx`, so the CLIENT PORTAL is outside it in two independent ways — `web-portal/components/` is a different tree, and it has no `*-detail.tsx` at all (its record screens are `*-screen.tsx`). The portal's ticket screen shipped the exact fault this law was earned by, on the one screen a client reaches from an email, and it was found by a review rather than by this check; it now reads `cacheKeys.ticket(ticketId)` by id always and paints from the list row while that lands (`web-portal/components/ticket-screen.tsx`). Widening the walk is not a one-line change — the filename filter, the directory and the cache-key dialect are three separate gates and loosening one alone produces false positives — so until it is widened, the portal obeys R38 by REVIEW and the law is honest about it here rather than implying a coverage it does not have. | `details-ask-the-door` (`web/test/rules.test.ts`) | enforced |
| R39 | ui | **The kit supplies the UI, and nothing else does.** No file in `web/`, `web-portal/` or `shared/web/` imports a UI package. The kit at `shared/ui/` is the one source of a control, a structure, a glyph and a toast; its own dependencies — Radix, sonner, recharts, class-variance-authority — are ITS to import, and the app reaches them THROUGH it (`@shared/ui/components/sonner/sonner`, never `sonner`). *The deny-list is derived* from the kit's own package.json peers plus the known icon packs, so a dependency the kit adds tomorrow is covered without anyone editing this law. Exceptions are data in `UI_PACKAGE_EXEMPT` with a reason each, rot-checked, so a pin whose file no longer imports the package turns the build red and the list can only shrink. *Earned by the icon swap of 2026-08-27:* 96 kit glyphs were not enough, so five files imported lucide directly and a sixth reached for lucide's RUNTIME loader — and the app quietly carried a second icon pack of 3,924 glyphs. When the kit's art became Iconoir, thirty-seven stored names kept drawing the OLD pack beside the new one, on the same screen, under a green build. No single import was wrong; the whole was a second design system nobody decided to have. | `kit-supplies-the-ui` (`web/test/rules.test.ts`) | enforced |
| R40 | arch | **A stored file must reach a person.** Every call that puts BYTES in a bucket — an `env.<BUCKET>.put(` on a binding the wrangler configs declare as an `r2_bucket`, or the one shared seam that does it for you (`storeImageDataUrl`, which hides a `bucket.put` behind a helper and is how four of these doors write without the word `.put` appearing at all) — is claimed by an entry in `STORED_FILES` naming the FIELD a person reads the reference back through and the front-door FILE that renders it. *Both halves are derived, and each end is grounded in a different oracle on purpose, so the check is never a parser agreeing with itself.* The WRITE census comes off the wrangler configs: which bindings are buckets is a deployment fact, so `env.MEDIA.put(` counts and a Durable Object's `storage.kv.put(` does not, and no list of bucket names lives in a test. The READ proof comes off the browser: bytes reach a person through an attribute the browser FETCHES — `href`, `src`, `picture` — directly or through a `const` assigned from `safeHref`/`safeSrc`, and through nothing else. *A field that only ever reaches a form's values is not a read*, and that subtraction is the whole discriminator. Rot-checked both ways, so an entry whose door no longer writes bytes turns the build red and the list can only shrink. *Earned by three instances of one bug, every one of them green:* a story's attachments, written by two dialogs and rendered by no screen at all — the owner attached two screenshots, saved, and never saw them again; a document a CLIENT uploaded through the portal's "Send a file", shown to the agency as a filename in a metadata line while the URL sat on the row read by nothing; and a task's `file_url`, write-only from the day the door shipped. In all three the door answered 200, the bytes were in R2 and the row was correct — which is precisely why nothing caught them. R15 makes a published change reach a listener and R16 makes a count reach a screen; nothing made BYTES reach anybody. The check has to walk write→read, because that is the direction the failure runs: starting from the screens would only ever find the files that are already shown. | `reachable-bytes` (`web/test/reachable-bytes.test.ts`) | enforced |
| R41 | arch | **A file somebody picked is either sent or refused, never dropped.** R40's sibling, and the boundary is the point: R40 asks whether a STORED file reaches a person, so where nothing is stored it is silent by construction — and this is the worse half, because R40's failures at least leave the bytes in the bucket. A create dialog cannot upload while somebody is still typing: R2 is addressed by the record's id and on a create that id does not exist yet. So the picked files WAIT, and are hung on whatever `onSubmit` hands back. Every dialog that defers an upload that way is a line in `DEFERRED_UPLOAD_FORMS` naming the maker whose id it needs — rot-checked against the dialog still deferring, so a form that stops loses its line — and every CREATE call site of it, censused off the disk, must actually return that id: either a concise arrow whose body IS the maker call, or a named value that is returned. A site that passes the record's id is an EDIT and is skipped, read from the same prop the dialog itself switches on, so the check and the code cannot disagree about which sites are at risk. *Deliberately narrow:* the fault is a DISCARDED RESULT rather than a missing call, so there is no absent function to census — only a value that goes nowhere — and an honest small law that can be checked beats a general one that cannot. *Earned by:* three of the four `<StoryFormDialog>` create sites doing `await createStoryFrom(...)` and throwing the id away, so `target` was null, the guarded upload never ran, and every file picked in those forms was dropped in silence — story created, success toast, no error, no row, no object, nothing to recover from. `createStoryFrom` had returned the id since the day the upload was written; the callers just did not pass it on, and a discarded value looks exactly like correct code. | `picked-files-are-sent` (`web/test/picked-files-are-sent.test.ts`) | enforced |
| R42 | arch | **Every accepted source type resolves to a declared reader on EVERY door, or to an honest refusal — and no door chooses its own.** One table, `workers/content/src/lib/source-readers.ts`, maps a type to an ORDERED list of readers, with a declared `UNREADABLE_TYPES` beside it so "we cannot read this" is a decision somebody wrote down rather than the absence of one. Both doors ask it: the upload door (`extractFile`) and the Drive lane (`driveFileText`). The order is per TYPE and not per door, because the two readers are complementary — Cloudflare's `toMarkdown` reads a PDF and an image and cannot read a deck; the in-process OOXML unzip reads a deck and cannot read a PDF or an image. *Earned 27 Aug 2026:* the app had two different pieces of code for reading a file and nothing saying which to use, so the answer depended on which way the file came in. A PDF uploaded through the app was read properly. The same PDF sitting in a Drive folder came out as gibberish — every PDF in the agency's own knowledge base scored zero on any test of whether it held words, the powers of attorney exactly like the logo artwork. Nobody chose that: each door had picked a reader at the moment it needed one, and the two moments were months apart. Asking one table instead also gave the upload door PowerPoint, which the other reader could always handle, without a line of new reading code. *Why it is a law and not a tidy-up:* **reusability**. Adding the next file format is one entry in one table rather than an archaeology exercise across two doors to find out what each already does — the difference between a base you can fork for a different product and one you can only inherit. *The load-bearing clause is the last one*: the check censuses both doors off the disk, comments stripped, and fails if either names a reader the table did not give it — because a predicate nobody calls is not a guard, and a table nobody asks is not a registry. *Provenance:* written by the session that found the defect, enforced by its own check BEFORE it was registered, and held unregistered until the owner authorised it himself. A Law of the Base carrying a session's signature on the constitution it is bound by would be the wrong precedent. | `declared-readers` (`workers/content/test/source-readers.test.ts`) | enforced |
| R43 | ai | **Agent/MCP tool-set parity.** A tool that exists on the agent's own catalog (`TOOL_CATALOG`) exists on MCP's own (`MCP_TOOLS`) too, under its own name or a locked rename, or the gap is a named, reasoned line in a rot-checked table — and the same sentence in reverse. *Why this is a separate law from R19/R22:* their coverage census asks whether a DOOR has a tool on SOME machine surface, which is right for what THEY check (a door reachable from either surface is not a gap the owner asked about) — but it means a door with an agent tool and no MCP tool passes that census trivially, and the asymmetry between the two machine surfaces, the exact sentence the owner asked to be made checkable ("if I can write, I can edit, I can delete... same thing an agent can do, same thing from MCP"), was never itself examined by anything. *Earned 28 Aug 2026:* twenty-five tools lived on the agent and nowhere on MCP, twenty-three lived on MCP and nowhere on the agent — every one of them a real, written decision (MCP.md §3's "intentionally NOT on the machine surface" list: the two confirm-panel bulk writes, the role-permission-matrix read, the SELF import runner, and the whole Google Drive/Gmail/Chat/Calendar surface, kept off MCP because a leaked personal access token's blast radius must not include a mailbox) — but the comment introducing the agent's own `AGENT_ONLY` array had drifted to naming four tools while twenty-five existed, silently, for the six weeks since the Google block landed. A law that stops looking when a block grows is a law that rewards growing it quietly. | `agent-mcp-tool-parity` (`workers/mcp/test/agent-mcp-tool-parity.test.ts`) | enforced |
| R44 | ui | **A catalogued string must be answered, up to a ceiling that only falls.** R28 makes `shared/i18n-strings.json` exactly the set of English sentences the app says; R33 makes every one of those positions ask for its translation. Neither asks whether the asking is ever answered — a string can be extracted and wrapped in `t(...)` and still have no entry anywhere in `overlay(CATALOGUE, SEED)` for a translated language, which is `shared/i18n-catalogue.ts`'s own stated fallback: English on screen, "a sentence rather than a bug." Per translated language, the count of extracted strings with no non-empty entry — the same predicate `coverage()` already uses to tell a person choosing a language what to expect — is pinned in `TRANSLATION_CEILING`. The check computes the true count fresh, off the same three files, and requires it to equal the pin exactly: a string shipped past the ceiling fails the build, and a ceiling left above the true count after a translation lands fails it too, so the pin can fall and can never rise without the count behind it rising first. *Not a hard zero:* `shared/i18n-catalogue.ts`'s own header argues a missing translation degrades to a partly-translated app, never a broken one, and a hard zero would turn the next ordinary feature PR red the moment it adds one new label — a build that fires on unrelated work is a build people route around, which hides the next real regression behind the routine one. *Measured 30 Aug 2026:* the ceiling reached 0 in all three languages — the 308 strings the process-map, wave and Google-connections screens had left with no entry in either the generated catalogue or the hand-written seed were translated in one run through the app's own Cloudflare allowance (`scripts/i18n-translate-workers-ai.mjs`, never the owner's own key) and written into the seed. **It did not stay there, which is the law working rather than failing.** Every feature since has raised it with its reason written into the pin, and on 7 Sep 2026 it stands at **213 in each of the three** (`shared/rules/registry.ts`, `TRANSLATION_CEILING`) — 213 of `shared/i18n-strings.json`'s sentences ship in English to somebody who chose German, Spanish or Catalan. Read the CURRENT figure off the pin, never off this sentence; the pin is the only place the number is allowed to live, and the next reviewed translation run takes all three down together. | `translation-ceiling` (`web/test/translation-ceiling.test.ts`) | enforced |
| R45 | ui | **Every kit composition is decided.** All 47 files under `shared/ui/compositions/` are derived off disk, and each one resolves to EITHER an adoption this app actually reaches — a direct `@shared/ui/compositions/...` import in `web/`, `web-portal/` or `shared/web/` — OR a reasoned entry in `COMPOSITION_EXEMPT`, naming why not: a genuine structural mismatch, a shape already assembled from other already-adopted kit parts under a different name, a real gap this app should or should never have, or a question left for the owner. The two acceptable outcomes are adopted, or DELIBERATELY not used for a stated reason — a composition nobody looked at, or hand-rolled screen UI that quietly duplicates one, is the only unacceptable result. Rot-checked both ways: a composition with neither an import nor an exemption turns the build red, and an exemption whose composition is now directly imported turns it red too, so the list can only shrink. *Why:* the owner's own words, 30 Aug 2026 — "make sure that we get 47 out of 47 compositions... if there are some compositions that we don't use, I completely get that, but flag those." Two lanes had worked through 37 of the 47 by hand, in prose, with nothing behind it that would catch a regression the moment a kit update added a composition, or a screen quietly duplicated one. | `composition-coverage` (`web/test/rules.test.ts`) | enforced |
| R46 | ui | **Every kit component and foundation resolves to a reached adoption or a reasoned, rot-checked exemption.** The kit at `shared/ui/` ships every directory under `components/` plus the three foundations (icons, tokens, motion). **The number is DERIVED and written down nowhere:** `kitInventory()` in `scripts/kit-coverage.mjs` reads it off the pinned tree, so it moves with the pin instead of rotting in four documents — run `node scripts/kit-coverage.mjs` for today's figure. The count came from the owner, with his instruction: "all 118 components should be imported, and if you're not using some, I understand that, but there should be nothing hard-coded." A part is REACHED, not merely imported: `computeReachability` (`scripts/kit-coverage.mjs`) seeds from every kit reference either front door or `shared/web/` makes, in EITHER language the kit ships in — a JS/TS `from "@shared/ui/…"` or a CSS `@import "…"` — then closes over the kit's own cross-references (a component that imports another, a stylesheet that imports another) until nothing new appears, so a part reached only through another adopted part, or only through a stylesheet, still counts. Every part the walk does not reach is claimed by a line in `KIT_COMPONENT_EXEMPT` naming the one sentence a non-technical reader can check — no surface in the app has this shape, or adopting it would break another law. Rot-checked BOTH ways: a part the walk now reaches loses its exemption, and a part with no exemption and no reach fails the build, so the list can only shrink. *Earned by:* counting only JS/TS import lines undercounted in the same direction seven times in one day — six parts reach the app only through another kit part it has already adopted (`notes` through Comments, `folder` through Tabs, `title` through the kit's own `record-detail`, `progress` through `file-upload`, `gallery` and `checklist` picked up by two other lanes overnight with nobody re-running the census to notice), and `motion` reaches both front doors only through a CSS `@import` in their own `globals.css` — a reference no JS-import grep can see in either direction, because there is no import line in that language for it to miss. Canaried both ways: deleting the `@import` from both `globals.css` drops the reached-foundations count from 3/3 to 2/3, and restoring it recovers 3/3 — the same assertion is now a permanent part of the check, not a one-time manual proof. | `component-coverage` (`web/test/rules.test.ts`) | enforced |
| R47 | ai | **Every module a person can see, the assistant can answer about — and what is not in the corpus says why.** Two clauses over one census, derived from the permission matrix itself (`TEAM_MODULES`, every module offering a `read` right). FIRST: each resolves to a KNOWLEDGE KIND that mirrors it (`INGEST_KINDS`, its module read off the kind's own `table` through `ACTIVITY_GATE_MAP`/`QUERY_MODULES`, or off the `modules` a kind declares when its text carries more than its table), or a GATED READ TOOL on the agent's own catalogue (the tool's door found in that worker's own switchboard, its module read off the handler's own `requireRight`/`gated` pair), or a reasoned `ASSISTANT_BLIND_MODULES` line. SECOND, the clause with the teeth: a module reachable ONLY by tool declares in writing why its material is not in the searchable corpus — a `CORPUS_EXEMPT` line, rot-checked, so a module that gains a kind loses its excuse and the list can only shrink. Nothing hand-listed: modules from the matrix, kinds from the sweep, tools from the catalogue, each tool's gate from the source of the door it forwards to. *Earned by:* the owner asked "what is Alex's full name?" on 1 Sep 2026 and the base could not answer — not a retrieval failure, but the fact that NOTHING anywhere said who his own colleagues are (`staff_profiles` carries a `user_id` and no name; the names live in the global core database). The first draft of this law asked only whether a module was reachable AT ALL and came back 21 of 22 green, which is exactly why the failure was invisible: a tool answers when the model knows to call it, and a vague question reaches the corpus. The money settles the shape — internal rates and the margin are reachable by tool on the R24-fenced doors and must never be in a corpus that has one gate and cannot fence per module, so the honest sentence is "reachable, but only by the people who could already see it", and it is written down for the owner rather than relayed. | `assistant-coverage` (`workers/mcp/test/assistant-coverage.test.ts`) | enforced |
| R48 | ui | **The toolbar, search included, is a default — never a per-screen choice.** Every collection/data-view screen draws its toolbar's search box UNLESS a named, reasoned entry says otherwise. Two censuses, off the disk, never a hand-list: every `BASE_RECIPES` entry (`web/lib/screens.ts`) whose recipe carries a `CollectionConfig` must have `searchable: true`, or be named in `TOOLBAR_EXEMPT`; and every `<ToolbarRow>` call site across `web/` and `web-portal/` (the bespoke toolbar a bounded collection with no recipe search reaches for, `web/components/deep-link/screen-bits.tsx`) must pass a `search` prop, or be named in the same registry. Both directions are rot-checked, so the list can only shrink. **AMENDED 2026-09-11 — THE FOURTH CENSUS, AND IT IS THE ONE THAT DOES NOT STAND ON A TOOLBAR.** The three censuses above all key on a TAG THE FIX ITSELF PUTS THERE — a recipe, a `<ToolbarRow>` call site, a `<CollectionHeading>` — so a wall of records built by hand is invisible to this law BY CONSTRUCTION. That is how Settings › Modules shipped twelve cards and no search box under a green build on 10 Sep 2026, and how it became visible to census (ii) the NEXT day, when the client asked for the toolbar herself and the screen gained a `<ToolbarRow>`: a law you enter by being fixed is a law that could never have caught you. So the fourth subject is the WALL. Every component under `web/`, `web-portal/` or `shared/web/` that renders the kit's `CardGrid` or the kit's `List` over a `.map()` has DECLARED itself a wall or a register of records — those two parts exist for nothing else, and no comment can satisfy the predicate. Identified by IMPORT BINDING and never by tag name (`@shared/ui/components/card-grid/card-grid`, `@shared/ui/components/list/list`, `@shared/web/list-compat`), so Phosphor's `List` icon and `agent-markdown.tsx`'s `const List = block.tag` are not walls and a renamed import still is. The ROOM is the nearest Capitalised component, never a `<section>` — `sprints-screen.tsx` cuts one collection into five state sections that ONE toolbar narrows, and rooming by section would report the screen with the toolbar as the screen without one. Searching is four signals, each grounded in something already held shut: `<ToolbarRow>` (census ii requires its `search`), `<PagedFind>` (unconditional `<SearchInput>`), `<SearchInput>` itself, and the portal's `useDoorSearch`. A GROWING wall may not be exempted at all, the same refusal ii-b makes for a paging portal room. Exemptions are keyed `path#Component` so the next wall in a file cannot inherit a reason written about a different collection; `shared/web/list-compat.tsx` (it DECLARES the `List`) and `shared/web/screen-engine/` (its walls are every recipe's body, which census i holds one by one) are out by SCOPE. **It under-reaches on purpose** — a hand-rolled `<ul>` is not caught, and no non-fuzzy predicate catches one without filing a conversation thread as a data view; read `TOOLBAR_EXEMPT`'s six `path#Component` lines, not this sentence. *Why:* the client's own words, correcting a narrower answer already given once — "I don't care here. You're giving me specifics, and I told you that the toolbar, including the search, should be absolutely everywhere we have a data view or a collection view. Stop hardcoding this. Just write it as a rule." `searchable` and `search` were both ordinary optional fields before this law, and two screens quietly omitted the second: Tasks' Calendar tab and the Triage queue each drew a toolbar with a button and no search box, reasoned only in a comment nothing read at build time. Flipping the default is the fix that cannot regress the same way twice — an opt-in can be forgotten by omission, which is what happened; an opt-out has to be written down and given a reason a reviewer can read. A collection genuinely and permanently empty of rows (`WaveFinder`'s and Sprints' own "nothing to search yet" fallback) is the one reason left standing, because a search box over zero rows is a control that cannot do anything. SUPERSEDED IN PART, R50: that fallback — a bare `<ToolbarRow actions={…}>` reached only once the collection was empty — is exactly how a create button kept escaping this law's own two censuses (both only ever asked whether `search` was present, never whether `actions` agreed with it); both call sites now carry one `<ToolbarRow>` gated by R50's own required `empty` prop, and `TOOLBAR_EXEMPT` no longer names either. **AMENDED 2026-09-17 — THE FIFTH CENSUS, AND IT WALKS BACKWARD.** Censuses (i)/(ii)/(iv) all walk FORWARD from something the fix itself puts on the page — a recipe's `searchable`, a `<ToolbarRow>` tag, a wall's own kit import — so a hand-rolled paged list that never gained any of those three tags is invisible to all three of them, by the identical construction that made census (iv) necessary for walls. That is how the per-token MCP call log on Settings → Access tokens (`access-tokens.tsx#TokenCallLog`) shipped a hand-built `<Table>` with its own `<LoadMore>` — reading a paged door, R14 — and no toolbar, no search box and no pin at all, under a green build, verified by the owner on staging at three widths. So census (v) walks the other way: every `.tsx` file under either front door that renders `<LoadMore` (the one client-side mark every paged reader carries, R14's own cursor sidecar) must also render `<ToolbarRow` — directly, or through one of the app's other toolbar-drawing shared bodies (`<PagedFind>`, `PagedPanelBody`, or the record's own slide-in activity rail, `ActivityRail`/`ActivityPanel`, each already proved to draw one by census ii) — or be named in `TOOLBAR_EXEMPT` with the real reason. The four files that DECLARE one of those shared bodies (`screen-bits.tsx`, `paged-find.tsx`, `work-panels.tsx`, `activity-panel.tsx`) are excluded the same way census (ii) already excludes `screen-bits.tsx`: a declaration is not a call site. Rot-checked into the same `TOOLBAR_EXEMPT` ratchet the other four censuses already share. | `toolbar-shows-search` (`web/test/rules.test.ts`) | enforced |
| R49 | ui | **The gap between a toolbar row and what it sits above is one number, paid by the row itself, never a per-screen margin.** `<ToolbarRow>` (`web/components/deep-link/screen-bits.tsx`) pays `--toolbar-content-gap` (`web/app/globals.css`) as its own trailing margin, on its own root, so every call site gets it for free. No call site may also wrap the row in a gapped flex column or space-y stack, and no call site may pass a competing `mb-*` in its own `className` — either is the same number spent twice. Checked as a census off the disk: `<ToolbarRow>`'s own definition must carry the token, and no call site (or the variable a screen names `*toolbar*` and renders in its place) may pass a hardcoded `mb-*` of its own, or sit in a BOX that spaces its own children apart — a `gap-*` on a flex column, or a `space-y-*` stack, which needs no flex at all — with something rendering after the row inside that box; unless named in `TOOLBAR_CONTENT_GAP_EXEMPT` with the real reason. **THE SECOND CLAUSE WAS DEAD FROM THE DAY IT SHIPPED, AND WAS REBUILT ON 11 SEP 2026.** It read backward over a 400-character window for a `<div>`/`<section>` opening tag ANCHORED TO THE END of that window, then tested what the anchor had not consumed — the 360 characters BEFORE the wrapper, which is ordinary code at every call site — against a pattern admitting only whitespace, an emptied `{/* … */}` and a `{cond && (` guard. So it returned `null` before it ever read a class. Measured against the real stripper: twenty-four call sites, twenty-four `null`s, not one of them for a reason about spacing, and a lane that deliberately put `gap-4` on a toolbar's own wrapper could not make it fail. The box is resolved properly now — a JSX tag census carrying `stripComments`'s own `afterValue` bit, because `useState<string>(…)` and `a < b` are spelled like opening tags and a scanner that counts them loses the stack within two lines; a FRAGMENT walked THROUGH, because it paints no box; and a COMPONENT wrapper followed to the element it puts its children in (`{children}` where it writes one, else the element wearing the props SPREAD, which is how every kit primitive passes them on). That last hop is the one the old scan could never make, and it is where the only real offender was: `<TeamPanel>`, the members wall's own `flex flex-col gap-4` container, had been spending this number a second time on every team since it shipped, in a file the census read and passed. Two things the repair carries with it. A row that is the LAST thing in its box is NOT an offence, because a column gap with no following sibling cannot reach the content — that is the difference between a law and a registry of apologies, and it is why `client-org-panel.tsx`'s three deliberate heading-and-row `gap-2` pairs are GREEN (the content is a sibling of the PAIR; the file said so in a comment written before any check could read it) rather than pinned as three exemptions that would then have blinded the law to the real double-spend somebody adds to that file next. And a `space-y-*` is judged without asking for `flex-col`, because a margin needs no flex. *What it still cannot see*, said out loud rather than left to look like compliance: a row drawn at a COMPONENT'S OWN ROOT — `tickets-collection.tsx` returns `<>…<ToolbarRow/>…</>` — is spaced by whoever renders that component, which is a render graph and not a file. Three call sites are in that shape; none of them sits in a gapped box, checked by hand on 11 Sep 2026. *Why:* the client's own words, item 5 of the 2026-09-03 spacing round — "tehre's wahy too much space between the toolbar and the contenta" — confirmed on every screen she checked. It had drifted into five different numbers doing the identical job: a wrapping `flex flex-col gap-N` div (`gap-2`/`gap-3`/`gap-4`/`gap-6`, 7.5–22.5px), a `space-y-3`, and a `className="mb-4"` passed straight to the row — fourteen call sites, four mechanisms, no shared owner. The same shape `--tab-content-gap` already fixed for a tab strip and its panel, read the other way round, and the same value: `--space-5`, because both are "the gap between a control strip and the content under it" and one rhythm does not mint a second number for the same sentence. | `toolbar-content-gap` (`web/test/rules.test.ts`) | enforced |
| R50 | ui | **Never toolbar on an empty collection — not even the create button.** R48 made the search box a default; this makes the whole row answer one question together. `<ToolbarRow>` (`web/components/deep-link/screen-bits.tsx`) takes a required `empty` prop — true when the collection holds zero rows before any search or filter narrows it — and returns `null` unconditionally when it is true, before any other slot (search, filters, sort, view, or `actions`, the create button) is even considered. `<PagedFind>` (`web/components/records/paged-find.tsx`) takes the equivalent required `restingEmpty` prop for the door-searched half of the app, suppressing its own row the identical way. `<SectionWithCreate>`'s own header-drawn create button carries the same gate through an optional `empty` prop, for the one shape neither `folderTabs` nor `useKitPanel` already covers. Two censuses, off the disk: every `<ToolbarRow>` call site across `web/` and `web-portal/` must pass an `empty` prop derived from the collection's own row count, and every `<PagedFind>` call site must pass a `restingEmpty` prop the same way — missing, or hardcoded to a bare `{true}`/`{false}` literal, either counts as an offence unless named in `EMPTY_TOOLBAR_EXEMPT`. Rot-checked both ways. *Why:* the client's own words, verbatim, about a Time tab with zero rows: "once again, when empty collection no toolbar at all - fix everywhere and set as a rule." "Once again" is the load-bearing word — R48 had already ruled this once, but its two censuses only ever asked whether `search` was PRESENT, never whether `actions` agreed with it, so a `<ToolbarRow search={data.length > 0 && …} actions={canCreate && <AddButton/>} />` passed R48 outright while still drawing a lone, floating "+" over an empty collection. The audit this law's own census forced turned up the identical shape seven more times, each reasoned in a comment as if it were the intended design: Sprints' Overview/Calendar toolbar, Waves' own finder, a wave's own Sprints tab, Deliverables, Modules, all three of Client-org's lists, Dropdown values, and Triage's toolbar (drawn by a parent component that could not see whether the child's own fetch held a single row). `empty`/`restingEmpty` are REQUIRED rather than optional for the same reason every one of those was a bug: an optional prop a caller can forget to pass is exactly how a `search` gate and an `actions` gate drifted apart in the first place. **AMENDED 2026-09-11 — THE ROW MAY DRAW ONE THING WHEN EMPTY, AND IT IS THE SECTION'S OWN NAME.** The client ruled that a settings section's title sits inside its container, above the toolbar (*"ticket types should be on top of the searchbar inside the container without subtitle, make this. always"*), and `<ToolbarRow title>` is where it is drawn — a STRING, never a node, so no call site can place it — because the pinned band is the only thing inside the container that a title can ride without pushing the pinned box's top edge into the middle of the title block and cutting two page-coloured notches into the card's sides (R63 part 3's own `band`, filled for the first time). SO THE EMPTY EXIT RETURNS `heading` INSTEAD OF `null`, and `heading` is `title ? … : null`: seventeen of the eighteen call sites pass no title and are byte-for-byte unchanged. **This law's sentence is untouched** — a title is not a toolbar; it is pressed by nobody and offers nothing — and taking a section's NAME away at the exact moment its collection is empty is R67's Access-tokens finding said about the heading rather than the register. **The check's proxy got TIGHTER, not looser:** it asserted the literal `if (empty) return null`, which only ever proved the shape on the day somebody read it; it now asserts `if (empty) return heading`, that `heading` is declared exactly once as a conditional on `title`, and that nothing reads it before that — so the empty exit is provably a title block or nothing and can never come to carry a slot. | `empty-toolbar` (`web/test/rules.test.ts`) | enforced |
| R51 | ui | **A panel that minimises collapses; it is never unmounted on the state that animates it, and never left reachable once shut.** Three clauses, read off `shared/ui/compositions/templates/screen-shell.tsx` and `shared/ui/foundations/motion/motion.css`. (i) Nothing in the aside dock may be JSX-gated on `isAsideOpen` (`isAsideOpen ? (` / `? <`) — gating the column, or the wrapper that collapses it, unmounts the subtree on the flip and leaves the exit transition nothing to play on; every legitimate use picks a VALUE (`? "open" : "shut"`), never a subtree. (ii) The collapsing wrapper must carry all four of `.motion-column-collapse`, `data-state={isAsideOpen ? "open" : "closed"}`, `inert={!isAsideOpen}` and a `--motion-column-size`. (iii) `.motion-column-collapse` must NOT size itself with `grid-template-columns`/`fr`, and must transition `inline-size`. *Why:* the client asked to close the assistant from its own folder tab, "with a nice animation". The first implementation was the row-collapse rule turned ninety degrees — `grid-template-columns: 1fr -> 0fr` — on the reasoning that a fraction interpolates against the grid's own resolved track, so the column would collapse from whatever width it had without restating that width. Correct for a row, wrong here, and invisible in review: the wrapper is a FLEX ITEM, so its grid is sized under an intrinsic constraint, and a `0fr` track is then floored at its own base size instead of resolving to zero. Measured in verify/shell-chat the track never moved off 356.25px — the panel went fully transparent and kept every pixel of its width, leaving the card at 851px instead of the 1207.5px it should recover. That is visibly worse than the instant disappearance it replaced, and it passed tsc, passed lint, and read correctly in the source. Clause (i) is the other half of the same bug: the column was originally unmounted the instant `isAsideOpen` flipped, so the first attempt had nothing to animate. Clause (ii) exists because fixing (i) alone CREATES an accessibility regression — keeping a panel mounted so it can animate is exactly what leaves a keyboard user tabbing into an invisible column — and the earlier ruling on the shut state ("closed assistant show nothing. it's literally only the bar") is not satisfied by a column that merely cannot be seen. | `aside-collapse` (`web/test/rules.test.ts`) | enforced |
| R52 | ui | **Every path that draws a record detail wears the same title treatment — one constant, never a per-call-site class.** `RECORD_TITLE_TREATMENT` (`shared/web/record-heading.tsx`) is the 80% title/actions split and the head's own container query, as ONE string. One census, off the disk, over `web/`, `web-portal/` and `shared/web/`: every call site that renders the kit's `<RecordDetail>` or its `<RecordChrome>` template — the app's only two ways to draw a detail screen — must apply that exact constant in its `className` AND import it from `shared/web/record-heading`, and none may pass a competing `titleSize` prop of its own. The constant's own definition is pinned too, as a whole declaration rather than a substring search, so the law cannot be satisfied by an identifier that has been quietly emptied. A tripwire asserts the census found more than one path at all — a scan that goes blind reports agreement. The kit's component gallery (`tools/screen-builder/samples/`) is out by SCOPE, not by exemption: its job is to draw each vendored export raw. **AMENDED 2026-09-22 (kit v1.2.150): the constant no longer carries a SIZE.** `RECORD_TITLE_SIZE` — the descendant selector that used to force the h1/44 step from outside, because the kit's `Title` primitive had no h1 rung to ask `RecordDetail` for directly (h2/32, h3/24, h4/20 only) — is removed. Neither `RECORD_TITLE_TREATMENT` nor either call site names a `titleSize` of its own any more; a record's title size is the kit's own `RecordDetail` default, `h2`/32px, source-censused separately against `shared/ui/components/record-detail/record-detail.tsx`. *Why:* the client's own words — "i want that unless other oomponents are above the tabs, the tabs are at exact same heigh in main screen and detail screen / also titels at same high / need to lok more uniform" — over the ruling she has now given three times: "i dont want you to hardcode fixes for single pages, but to state rules about components." The 44px title was a real fix for a real correction on 2026-08-31 ("title on main screens still way too small! it's currently smaller than in detail screens"), written as a PRIVATE constant inside `web/components/records/record-chrome.tsx`. The other detail path — `renderDetail` in `shared/web/screen-engine/screen-renderer.tsx` — could not see it and fell through to the kit's own `titleSize = "h3"`, so `team.detail` (the app's own landing screen), `members.detail`, `invites.detail`, `brand.detail` and `purposes.detail` set a record's name at 24px while thirteen sibling screens set it at 44px. Every pixel of that 20px step lands on the tab strip below it, which is the height the client was pointing at. The 2026-09-01 title-row ruling ("we always reserve a % on the left for the buttons") had drifted the identical way, in the identical file. Nothing was red and nothing could have been: a DEFAULT on one path and a CLASS on the other is not a contradiction any type or test could see. The tab-strip GAP was already uniform on both paths — because it had been made a token (`--tab-content-gap`) rather than a class in one file. That is the whole difference between the half that drifted and the half that did not. **The 44px title itself was later found to be the wrong fix, not merely a right fix in the wrong place** — the client's later, standing typography ruling caps every screen and record title at 32px, and kit v1.2.150 moved `RecordDetail`'s own default from h1 to h2 at the source rather than have the app keep reaching around it. What R52 still holds — the 80% split, the head's own container query — is independent of the title's font size and stayed exactly as uniform across both paths as it was the day this law shipped. | `record-title-treatment` (`web/test/rules.test.ts`) | enforced |
| R53 | ui | **The collection toolbar's slot set is the row's, not the call site's — and its sort slot is a default.** R48 made the search box a default and R50 made the whole row answer one question about emptiness; this rules the slots BETWEEN them. `<ToolbarRow>` (`web/components/deep-link/screen-bits.tsx`) draws five slots in one fixed order — search → filters → sort → view → actions — and two of them, `sort` and `view`, are STRUCTURED CONFIGS (`ToolbarSortSlot` / `ToolbarViewSlot`) the row builds the `<SortControl>` and `<ViewSwitch>` from itself, exactly as `folderTabs` is a `FolderTabStrip` rather than raw JSX. Three censuses, off the disk. (i) THE CENTRAL GUARD: `ToolbarRow`'s own source declares both slots as configs (never `React.ReactNode`, which accepts anything and therefore enforces nothing) and renders both controls itself. (ii) NOBODY ELSE BUILDS EITHER CONTROL: no `.tsx` under `web/`, `web-portal/` or `shared/web/` may render a `<SortControl>` or `<ViewSwitch>` unless named in `TOOLBAR_CONTROL_OWNERS`, where the app's other toolbar-owning components are written down with the reason each owns one — `<PagedFind>` (the door-searched half), the recipe engine's own `CollectionFrame` (whose sort options are DERIVED from the recipe's columns), and `wave-finder.tsx`, which is a hand-written second copy of `<ToolbarRow>` with a sixth slot and a different order, pinned so the divergence is a line somebody can read rather than a file the toolbar censuses cannot see. (iii) SORT IS A DEFAULT: every `<ToolbarRow>` call site passes a `sort` prop, or its ENCLOSING COMPONENT is named in `TOOLBAR_SORT_EXEMPT` with the real reason — keyed by component rather than by file because three of these files hold two or three toolbars with genuinely different answers. `view` gets no registry, and that is a property of the control rather than a gap: `ViewSwitch` renders nothing for fewer than two views, so a single-body collection is self-exempting. Both lists rot-checked both ways; tripwires fail the build if either census matches nothing. *Why:* the client's own words, 2026-09-06, on two screenshots of her own main collection screens side by side, Apps (`Search apps…` / Filter / ↑ / Name / ▦ Tiles / +) and Tasks (`Search 82 tasks…` / Filter / +): "why the fuck i still have different toolbar variations??? unify joder." Part of the answer is honest and is now written down rather than assumed — Tasks' five table tabs sort by their own column headers and its six views are a tab strip, so it offers neither picker, which is a reasoned exemption. The rest was nobody's decision. `sort` and `view` were ordinary optional `React.ReactNode` props, so a call site could pass the right control, pass nothing, or pass the control to a DIFFERENT slot — and eleven of the eighteen bespoke toolbars drew a sort control while EIGHT handed it to `search`: `<>{searchInput}{statusSelect}{sortControl}</>` on Dropdown values, Modules, both account panels, a contact's meetings, a wave's sprints and all three of Client-org's lists. `search` is the row's ONE GROWING slot, so all eight sat inside the stretching search cluster at whatever label treatment that screen typed, while Apps and Deliverables drew the identical chip in the non-growing box beside `actions`. R48, R49 and R50 could all see the row and none could see this: they ask whether a PROP is present, and a `ReactNode` slot's contents are invisible to a prop census by construction. So the fix is a change of TYPE rather than an eighteenth call-site patch. | `toolbar-slot-set` (`web/test/rules.test.ts`) | enforced |
| R54 | ui | **The agency's own people are named by their FIRST NAME, and nobody else is.** One seam, `shared/staff-name.ts`: `staffName` from the structured `first_name`/`last_name` pair (EXACT, so a two-word given name survives), `staffNameFromSnapshot` from the frozen "First Last" a row stored at write time, and `describeWithStaffName`, which rewrites an activity SENTENCE against that same row's own actor snapshot by exact prefix. **The trim happens at the RENDER seam and never in a worker**, and the seam's header carries the three findings that decided it. **The census is DERIVED TWICE, never hand-listed:** the actor-snapshot COLUMNS come off the workers' own writes — the column `actor.name` is stamped into, as an interpolated `sqlString`, as an `insertRow` property, positionally out of an `INSERT … VALUES` list, or through a `?` aligned to its own bind — the PAYLOAD FIELDS come off the mappings that carry those columns onto the wire, and a second source adds any `*Name` field declaring a `*IsClient` sibling in `shared/types.ts`, because a field that has to say which population it holds is a field that holds a person. The census keeps only the `*Name` fields — the reader scan matches by name, and `.by` is how every sort state in the app spells its own column — so a mapping named otherwise goes in `NON_NAME_PAYLOAD` with the file that renders it, and that file is checked by name instead. Declared, not dropped. Every `web/` file that READS one of those fields must resolve it through the seam at least once, judged positionally as R20 judges a checked body field: a pure FORWARD into another census field is not a rendering, and neither is a MATCH position (`.toLowerCase()`, `.localeCompare(`) — which is the whole point of trimming late, since the stored string stays a search and a sort key. `STAFF_NAME_RAW` is the reasoned residue, rot-checked so it can only shrink; two tripwires fail the build if either derivation goes blind. *Earned by:* the client's ruling — "upwise, when it's staff who records activity, only use the first name, so not Audora Alasa, only Audora. Do this across all the app. We only record name and surname for the contacts and the customers." **Two sentences, two populations, and the second is what makes this a law rather than a find-and-replace.** A client login is an ordinary `team_members` row and `toActor` is the estate's only actor constructor, so a row a CONTACT authored through the portal carries THEIR name in the same `creator_name` column ours do. Three read seams already answered that per row for the portal's own redaction (`raiser_is_client`, `from_client`, `is_staff`) and then threw the answer away, leaving the agency app holding one field with two populations in it; those flags ride the wire now, and the activity feed and the to-do grew the one they never had. **The trim is at the render seam for three measured reasons:** `work_logs.user_name` is written from `actor.name` and then used as a LIKE search term, a sort expression AND the keyset cursor key, so a worker-side trim would change which rows a search finds and where a page boundary falls; `assignableMembers` appends an email to a name that is not unique in a picker, and first names collide where full names do not, so that de-duplication must run on the word the reader sees; and `actorName` is on the machine surface too, where a ruling about what a PERSON reads has no business. **And the SENTENCE, which is where she actually saw it:** the kit's ActivityFeed draws `actor` only as an avatar's accessible name — the visible line is `description`, which 140 writers compose with `${actor.name}` inside it and STORE, so shortening the actor field alone would have changed nothing on the screen she was pointing at. The prefix rewrite fixes HISTORY as well, which no change to a writer could. *Named residue:* a description that puts a SECOND person inside its prose keeps that person's full name — no column names them, and guessing which characters in a stored sentence are a surname is prose parsing this refuses to do. Initials are untouched: an initial is a MARK, not a name (R35's own word). | `staff-names-are-first-names` (`web/test/rules.test.ts`) | enforced |
| R55 | arch | **A stored reference is what the FORMULA makes, and the formula is ONE PLACE.** `canonicalRef` (`shared/workers/refs.ts`) is the shape of a reference as a FUNCTION, with a SQL twin (`canonicalRefSql`) beside it, and the two are proved to agree by RUNNING both over the same numbers rather than by reading them — either side of 9,999 included, where a pad that truncated would start minting duplicates against a live unique index. `nextTeamRef` must RETURN that answer and may not build the string itself. Nothing else writes the column: an `INSERT` naming `ref` on a kind's table must sit in a file that mints through the shared door, and NOTHING may `UPDATE … SET ref = …` — a reference is minted once, and the one act allowed to rewrite one is a team migration that keeps the old string in `ref_aliases`. Every door that SEARCHES a reference must also search that alias, through the one `refAliasMatchSql` seam — AND SO MUST THE GENERIC ENGINE, which has no hand-written SQL for that clause to scan: a queryable field on a `ref` column DECLARES `renumbered` exactly when its table is one the backfill wrote aliases for (derived from `TEAM_REF_TABLES`, failing both ways so `tasks` may not claim a history it has none of), the engine ORs the same seam in from that flag, and the behaviour is PROVED by running the engine's own `readWhere` over the replayed ledger — the retired string finds its row, and neither the account fence nor the withheld clause is widened by it. **Which tables those are is DERIVED TWICE and hand-listed nowhere:** the schema's own answer (every `ref TEXT` in a `CREATE TABLE` and every `ALTER TABLE … ADD COLUMN ref` in `TEAM_MIGRATIONS`, two shapes each of which finds tables the other does not) against `TEAM_REF_TABLES`, which `tsc` refuses to let fall behind `TEAM_REF_KINDS`. A ref-bearing table with no kind is a reasoned `REF_TABLES_WITHOUT_A_KIND` line, rot-checked so it can only shrink — `tasks` is the one, a fossil column `createTask` has written NULL into since the ruling. And the DATA half is PROVED rather than asserted: the real migration ledger is replayed into a real SQLite handle over rows in the shapes staging actually held, and every surviving reference must read back through the formula, every retired string must resolve to its row, no counter may be able to mint a number a row already holds, a second run must change nothing, and a newborn team must come out untouched. A blindness tripwire fails the build if either schema scan stops answering or the replay stops moving rows. *Earned by:* the 2026-08-31 ruling changing the MINT while migrations 0059/0060 rewrote **not one stored row**. The formula existed only during the instant a reference was minted, so nothing afterwards could ask the question — refs.ts said the old account-coded shape "is GONE" while all 2,317 stored references in the team holding real data still carried it, and the client read `VU Solutions-T1183` off her own screen for six days under a green build. `npm run check` replays the whole ledger, so the schema it tests is current by construction and only an environment with a HISTORY can drift. | `refs-match-the-formula` | enforced |
| R56 | arch | **A component asks a door once.** Every `useCached(key, fetcher)` read across `web/` and `web-portal/` is censused off the disk, grouped by the COMPONENT it sits in — not the file, which can hold seven panels each with its own local `key` — and by the DOOR its fetcher calls (`tenancy.selectable`, `listFetch.apps`: the receiver and method, since the arguments say which rows and not which question). Two reads of one door in one component is a finding, and the two shapes are graded apart because they cost differently. **Same key twice** is an outright defect with no exemption available or possible: the store dedupes by key (`inFlight` in `shared/web/store.ts`), so the second read buys nothing and exists only as a second place to change one question. **Two different keys on one door** is a real second request the store cannot dedupe, and is sometimes right — those are data in `TWO_READS_ONE_DOOR` with the reason, rot-checked, so the list can only shrink. Identifiers resolve to what they were assigned, and `cond ? KEY : null` normalises to `KEY`, because a read gated on a permission is the same question as an ungated one — which is exactly the shape that shipped. *Why:* `round_trip_review`'s criterion 2 is called "no question is asked twice". It scored 100/100 at weight 13 on 5 Sep 2026, and the next day the same lane found `app-detail.tsx` reading `selectable:<team>` twice — once plainly, once gated on `canRaiseTicket` so the gated one could never be the read that warmed the cache. The probe HAD reported it; a human dismissed it as a false positive on the correct but incomplete grounds that the store dedupes the request. True about the network, wrong about the score: a criterion whose full marks rest on a judgement call made in a hurry is a criterion that says nothing. The owner asked, a month apart, whether the duplicate reads these reviews kept finding were sorted and whether any review still watched for them; the honest answer was that a review watched and nothing checked. The census that came with this law found the property otherwise held — 183 fetching reads across both front doors, zero same-key duplicates, and four components asking one door under two keys, every one a genuinely different question (a week of meetings versus all of them; four versions of one process map; a record's time versus a person's; and the open task list beside the all list, which `use-screen-data.ts` keeps apart on purpose because ticking a task off the OPEN list would make a detail screen sourced from it answer "that record no longer exists"). | `one-door-per-unit` (`web/test/rules.test.ts`) | enforced |
| R57 | ui | **`web/components` is one folder per module or kind, and every folder says what belongs in it.** The 7 Sep 2026 fold turned 148 flat files into fifteen folders on TWO AXES — `tickets-screen.tsx` is in `tickets/` because it draws a module, `home-screen.tsx` is in `screens/` because there is no home module; `collection-heading.tsx` is in `records/` because every collection reuses it, `collection-content.tsx` is in `deep-link/` because only the routing shell renders it. Both pairs are right and neither is guessable, so a newcomer has to READ the rule rather than infer it. The rule is written once, in `web/components/README.md`, one line per folder, and the check DERIVES the permitted set from that file's own rows rather than holding a second copy: a folder nobody described is red, a row for a folder nobody has is red, and a component left loose at the top level is red. *Earned by:* the arrangement was documented in a commit message and enforced by nobody, in a repo whose other fifty-two invariants are all machine-checked — the next component dropped at the top level would have been green, and the one after it would have made "the top level is empty" untrue for good. | `component-folders` (`web/test/component-folders.test.ts`) | enforced |
| R58 | arch | **A path this repo NAMES must resolve on disk.** Every law here is a source scan and every scan reads a path it was HANDED; nothing read the paths the repo WRITES — the ones in a document, and the ones in a comment above the code they explain. ONE pattern through the one walker, over two censuses, and everything the pattern is built from is read off the disk: the roots it accepts are the repo's own top-level folders, and the exemption for a path that is OUTPUT rather than source is `.gitignore`'s own answer. DOCS is every `.md` this repo writes, wherever it lives — including the fifteen that sit next to the code they describe. CODE is every root that holds code of ours, at every depth, in every text extension it writes (`.ts`, `.tsx`, `.mts`, `.mjs`, `.sql`, `.css`, `.json`, `.jsonc`, `.md`, `.html`) — an import specifier never carries one here, so the census reads prose and string literals and never the module graph. A blindness tripwire fails the build if the pattern can no longer recognise the paths of the very files the census reads. The way out is a reasoned `GONE_ON_PURPOSE` line, for a path a document names precisely BECAUSE it is gone ("the clause and `web/lib/use-live-refetch.ts` were retired"), rot-checked both ways so the list can only shrink. *Widened 9 Sep 2026,* because the check was narrower than this sentence: the source half matched only `.ts`/`.tsx` (so a `.md` named in worker source passed green — demonstrated, not argued) out of a hand-typed list of folders, the docs half read only `documents/**.md` and the root canon, and neither could see a Next catch-all segment. Six more real dangling paths came out of the widening, every one fixed or reasoned rather than narrowed away. *Earned by:* eight dangling paths in the canon and twelve in our own source — and five of the twelve named a GUARD that does not exist. `workers/auth/src/lib/sessions.ts` promised the build fails if a fourth copy of the cookie name appears and named a test file that is not there; `workers/content/src/routes/triage.ts` said a whole-repo census watched the triage rota and named another. Both properties are genuinely enforced, by suites under different names. A reader who checks is reassured by a file that is not there. | `named-paths` (`web/test/named-paths.test.ts`) | enforced |
| R59 | ui | **A form is a slide-in; a warning is an overlay.** The client's ruling, 2026-09-09, over a screenshot of the "New access token" dialog: *"This should be a slide-in, like all the other screens. The only ones that are overlays are the warnings, such as archive or delete, and so on."* A surface that COLLECTS — a form, an editor, a picker — presents as the kit's `Sheet`, which slides in from the inline end on desktop and, below 45rem, becomes the bottom sheet capped at 85dvh that her 2026-09-04 ruling asked for; the narrow half is implemented centrally in `shared/ui/components/sheet/sheet.tsx`, so no call site can get it wrong. A surface that ASKS a yes/no question about something that already exists is an `AlertDialog`, centred. **The check does not try to recognise a form — it inverts.** A regex that decides what a form looks like has a hole the week somebody writes one differently: the direct version finds four of the five real offenders and misses `role-picker-dialog.tsx` outright, because a radio group and an onClick that writes is a form with no `<form>`, no `onSubmit` and no `FormShell` in it. So every centred-overlay mount — every `<DialogContent>` across `web/`, `web-portal/` and `shared/web/` — is held to a reasoned `CENTRED_DIALOG_OK` line instead, and a form added next month reaches for a `Dialog`, has no line, and is red the day it is written, whatever it is made of. **The check is deliberately blind to kit v1.2.72's new `presentation` prop**, which looks like the answer and is not: `overlay` and `responsive` are both centred on a desktop (`responsive` flips to the bottom sheet only below 45rem), `sheet` is a bottom sheet on a 1920 monitor, and `fullscreen` is a page — none of the four is a desktop slide-in, and that component's own header says why ("a centred modal does not arrive from a side; it rises 8 and fades"). The shape she asked for is `Sheet side="right"`. Two rot-checks keep the list a ratchet rather than a door: a pin whose file no longer mounts a centred overlay must go, and an **exempt overlay that grows form machinery** (a `<form>`, a `FormShell`, a `<Field>`, an `<Input>`, a `<RadioGroup>`) turns the build red where it stands — without that second clause the exemption is exactly the loophole the law exists to close. Two blindness tripwires, because a scan that matched nothing would report a perfectly clean app: the walk must open files, and the census must still SEE both shapes it steers between (`<AlertDialogContent>` and `<SheetContent>`), which only holds if the kit's exports and the stripper still work. *Earned by:* the ruling was already the app's practice and was enforced by nobody. `FormShellDialog` moved ~35 forms off the centred `Dialog` on 2026-08-31 and argued the case at length in its own header — but it argued it for its own call sites, so the five forms that never adopted that shell stayed centred under a green build for nine days, and one of them was the screen the client happened to screenshot. | `forms-are-not-overlays` (`web/test/rules.test.ts`) | enforced |
| R60 | ui | **An image fills its box; it is never shrunk to fit inside one.** The client's ruling, 2026-09-09, blanket and unhedged: *"everywhere for images: do fill, not fit!"* Every picture either front door draws is `object-cover` — it fills the box it is given and is CROPPED to it — never `object-contain`, `object-fill`, `object-none` or `object-scale-down`. **The cost is the law, not a bug in it:** a wide wordmark in a small square loses its ends and shows its middle. What that was weighed against is the aggregate, which is the only place a fit is ever visible — a marked column where a contained logo sits smaller, paler and a different shape from the filled face beside it and the letter tile below it, grey bars down one row in three; on staging only 48 of 134 accounts hold a picture at all, so most boxes are a solid letter tile either way. **Two censuses, because there are two ways to say the losing word:** the CLASS, in any className under `web/app`, `web/components`, `web/lib`, the portal's three and `shared/web`; and the PROP, `fit="contain"` handed to the kit's own `Image`, which turns exactly that value into exactly that class. Without the second half the law is a one-line evasion — delete the className, pass the prop, ship the same pixels green — and it earned its place on the first run by catching a fit in the kit's gallery the hand census this law was written from had missed for being spelled as a prop. **The vendored kit is out of the requirement and in the count:** `shared/ui/` held five and now holds ONE, and none can be fixed here (it is hash-pinned; a hand-edit is red on its own), so `KIT_CONTAIN_CEILING` pins the number for exact equality the way R44 pins a translation debt — it falls when the upstream fix is tagged and pulled and can never rise. It has now done exactly that once: kit v1.2.73 adopted this ruling upstream, four of the five went, and the pin came down from 5 to 1. The one that remains is `image.tsx`'s own `fit === "contain" ? … : …` branch — the seam that turns the losing word into the losing class, which must exist for as long as `Image` accepts `fit="contain"` at all, so this pin reaching zero would mean the kit had DELETED the option rather than fixed a picture. `OBJECT_FIT_OK` is the reasoned, rot-checked way out and holds exactly one line: a ticket ATTACHMENT's preview, where the picture IS the content rather than a mark standing for a record whose name is written beside it. One clause is held directly rather than by census: `RecordMark`, which draws almost every picture in the product, may not grow a `fit` prop again — it had one, its square DEFAULT was `contain`, and a default applies to every caller who never made the choice. *Earned by:* the census the ruling was made against, nine `object-contain` against eleven `object-cover`, not one of the nine wrong on its own screen — R32's argument about colour drift and R35's about thirteen placeholders, in a third place. | `image-fills` (`web/test/an-image-fills.test.ts`) | enforced |
| R61 | ui | **A module's settings have two doors and one derivation.** The client's ruling, 2026-09-09, in two halves that only work together: a settings gear on each module's own screen, *"Only the ones with something to set"*, and *"somewhere in the settings, we have a tab that says 'Module' or 'Business Logic' (or whatever you define as a good word) to find the module once"* — because *"everything around settings should be under settings screen concentrated (and 'quick access' through the gear in each module) but not in random places across the app."* Two entrances, ONE page, and therefore one question: `visibleModuleSettings` (`web/components/screens/module-settings-screen.tsx`) is the only expression that answers whether a module has settings THIS reader may open, and the gear, the full-screen page and the Modules tab's index all ask it rather than each working it out. Three clauses, every one derived off the disk and none of them a list anybody keeps. **(i) The pair.** Every `segment` in `MODULE_SETTINGS` has exactly ONE `<ModuleSettingsGear segment="…">` mounted in `web/`, and every gear mounted names a segment that table declares — both directions, because each failure is invisible on its own: a module with settings and no gear is a page nobody standing on that module can find, and a gear on a module with nothing to set renders `null` for ever and is a door painted on a wall. **(ii) The index is derived.** The Modules panel on `settings-screen.tsx` calls `moduleSettingsIndex` and spells no module segment of its own, so its rows cannot be hand-kept and cannot fall behind the table. **(iii) One gate.** The settings host holds exactly one `can(` call, the one inside `visibleModuleSettings`, so a tab, a gear and a page can never come to carry three copies of one permission — which is her *"a reader who may see tickets but not the vocabulary should not be offered a door that refuses them"*, held structurally instead of restated three times. A declared segment must also be a real `MODULE_PERMISSION` key: a clause in its own right (a settings page whose segment names no module is an address nothing links to) and the census's own proof that it parsed real words rather than noise. *Earned by:* the pilot shipped with the second entrance unbuilt and nine lines of comment instructing whoever built it to ask the same function — a rule addressed to a future reader, which is the form a rule takes right up until it is ignored. Filling the index is later work, so the next several edits to `MODULE_SETTINGS` are somebody adding a module while looking at Tickets, with the gear and the row in two files neither of which they must open. Written while the index has ONE row, which is the cheapest moment to satisfy it and the only moment at which it is still free. | `module-settings-two-doors` (`web/test/rules.test.ts`) | enforced |
| R62 | ui | **The two zeros are one register, and the add button is the only difference.** The client's ruling, 2026-09-09, verbatim: *"the empty because of filters hosul look the same as empty collection but the add button."* A collection has TWO empty states and they are different facts. **RESTING** — it holds no rows at all; this is first run, and the screen exists to be filled, so it names the one act. **FILTERED** — it holds rows a search, a tab or a facet has narrowed to none; nothing is wrong, the reader has asked a question with no answer, and offering "Add the first" there invites a duplicate of a record a filter is hiding (composition 27.22's own reason: *"clearing filters is a retreat"*). Each front door draws BOTH through ONE component — `CollectionEmptyState` (`shared/web/screen-engine/collection-frame.tsx`) on the agency door, `PortalEmpty` (`web-portal/components/portal-empty.tsx`) on the client portal, which keeps its own centred, boxed measure because the portal has no collection panel to sit inside — and each takes a `filtered` prop that swaps the WORDS and WITHDRAWS the create action. Nothing else changes: same wrapper, same block padding, same heading step, same sentence measure, same action row. Four clauses, all off the disk. **(i) The subtraction is in the component, never at the call site** — each register computes its create action through `filtered ? undefined : …` and its action row renders only that withdrawn name, so a caller hands its action over unconditionally and has nothing left to forget. That is R50's own reason for making `empty` required rather than optional, read one layer down. **(ii) And it is proved by RENDERING, not by reading** — the register is drawn twice in a real DOM, once `filtered` and once not; the button must be absent from the first and present in the second. Every previous attempt to settle a question about this file by reading it reached a confident wrong answer, twice in one week. **(iii) One filtered register, not two** — no `.tsx` under `web/`, `web-portal/` or `shared/web/` renders the kit's `<ShapeStateBody … filtered>`, which was the second one, unless named in `SECOND_ZERO_REGISTER_OK` with a reason; rot-checked, so the list can only shrink. **(iv) The engine asks the question honestly** — `CollectionFrame`'s `narrowed` is its own query/facets OR the `narrowedOutside` a door-searched host hands down, and it is what it passes as `filtered`. A blindness tripwire fails the build if the call-site census matches nothing. *Earned by:* a census off the disk found 113 zero-row render sites in `web/` and 17 in `web-portal/`. Thirty-four agency ones drew the good shared register; **forty-three were bare grey `<p>` tags, twelve of them literally the same sentence** — `t("Nothing here matches that.")`, copy-pasted into eight files. In every one of those eight the two zeros sat in ONE component four lines apart: the full register at rest, one grey line the moment a search emptied the same list. A reader met two different-looking screens by typing one letter. Five more drew `EmptyLine` (one grey line and a glyph — deleted with this law, it had no call sites left) and four drew the kit's `ShapeStateBody`, a genuinely different box: `px-6 py-[var(--space-8)]` against the register's un-inset `py-[var(--space-7)]`, so the two zeros began at different x; a raw `text-2xl` span against `Headline`'s step-plus-tracking; a `text-caption`/40ch body against `Text size="sm" measure`. And that half **shipped in English**: `ShapeStateBody`'s no-results words are defaults inside `shared/ui/`, which R28's walk deliberately does not enter, so they were in no catalogue and translated nowhere — while the engine computed the collection's own translated sentence, passed it as `emptyTitle`, and threw it away, because `filtered` reads `noResultsTitle`. The button was wrong in **both** directions: on the door-searched half (accounts, contacts, stories, processes, knowledge sources, every nested work panel) the host handed the frame already-narrowed rows with `searchable:false`, so the frame's own query was always empty, a search matching nothing read as "this collection is empty", and it drew "Add the first" over a list a term was hiding. Forty-three call-site patches would have been the wrong fix for the same reason R50's were: the FACT is a prop, the LOOK is not the call site's to choose. | `one-zero-register` (`web/test/one-zero-register.test.tsx`) | enforced |
| R63 | ui | **The collection toolbar stays on top, and the pin is the row's.** The client's ruling, 2026-09-10: *"on scroll down, i also want the toolbar to be on top all time visible. everywhere."* R48 made the search box a default, R50 made the whole row answer one question about emptiness, R53 made the slot set the row's — this makes the POSITION the row's too, on both front doors. Every component that owns a collection toolbar wears one class, `PINNED_TOOLBAR` (`shared/web/pinned-chrome.ts`): `position: sticky` against `--pinned-chrome-h`, a flex COLUMN so R49's trailing gap sits inside a box that PAINTS, and `bg-[var(--pinned-ground)]` — the tone it is STANDING on, published by the ground class itself in both front doors' `globals.css` (the mechanism the kit's own `tokens.css` §8 uses to make a secondary button the other tone from whatever it stands on), so no caller is asked which. Measured, dark, 2026-09-10: a row inside a `<CollectionCard>` stands on `--surface-panel` #1C1B18 while its own pill is `--surface-raised` #26241F, and the same row on the Knowledge base stands directly on the shell's body pane, which IS `--surface-raised` — one hardcoded fill cannot be right in both, and a prop would put the answer back at the call site. A pinned bar that paints nothing is a bar the rows scroll through; one that paints the wrong tone is the hole punched in the card the tab strip was reported for; and a gap below it that is a flex `gap` rather than padding is space the rows carry away the moment it sticks, which is the bug `STICKY_FOLDER_TABS` was fixed out of on 2026-09-03 in a third place. **Four clauses, every census off the disk.** (i) The seam declares the pin and the offset, in one file. (ii) Every toolbar-owning component wears it, and the subject is not a list this law keeps: it is `TOOLBAR_CONTROL_OWNERS`, the census R53 already rot-checks both ways, plus the client portal's own collection rows derived as the files that ask the DOOR for a search (`useDoorSearch`) — the portal draws no `<ToolbarRow>` on purpose (R48 §ii-b) and the law is about the FUNCTION. (iii) Nobody writes their own: no file under the three roots spells the offset by hand except the seam. (iv) The offset is declared where chrome pins, one declaration per thing that can pin ABOVE a toolbar — both front doors' `globals.css` (the default zero, and the `:has(> .pinned-strip)` rule that raises it for a collection tab strip's own container, so every host of `renderFolderTabs` is covered by the rule rather than by a line somebody remembered), the strip's marker class, a record screen's root (its strip is the panels' sibling, and a custom property only reaches downward), and the portal shell's MEASURED header. A screen with nothing pinned above its toolbar pins flush at zero and needs no entry anywhere. **(v) The container's own top band pins with the row** — the client's second sentence the same day, 2026-09-10: *"when sticky toolbar, include also the top part of the container above it! if not looks weird. so the spacing between tabs and container should stay, as well as spacing between beginning container and toolbar."* Pin the toolbar ALONE and two of the five things she is looking at leave with the scroll: the card's top edge slides up behind the tab strip and the card's top inset goes with it, so the bar arrives flush under the tabs, standing on nothing, touching a card that no longer has a top. So the pinned box starts at the CONTAINER'S top edge instead of the toolbar's, and her two distances are the two that survive — tabs → container is `--pinned-chrome-h` (iv), already painted by the strip's own `pb`, and container → toolbar is `--pinned-lead`: the container's own top inset, paid as `pt` INSIDE the pinned box and taken straight back as a negative `mt`, so the row does not move by one pixel at rest and the band above it paints on scroll. The pair is asserted as a pair — the padding alone adds that inset to every collection for ever, the margin alone subtracts it. Published by the BOX and consumed by the ROW, exactly as the offset is: `CollectionCard` publishes the same ladder `cn("p-4")` leaves on `CardContent` (`--space-4`, `--space-7` above `lg`), the kit's own collection panel is published from the frame's className because the panel is not this app's element, both `globals.css` declare the `0px` default, and the rule that stands a nested toolbar down zeroes the lead on it too — a pt/mt pair is invisible while sticky and an upward shove the moment it is not. Two files may name the property at all, rot-checked both ways. **(vi) AND THE BAND KEEPS THE CONTAINER'S ROUNDED TOP CORNERS** — her THIRD sentence the same day, after looking at (v): *"When pin, I still want it round. That's exactly what I asked for, so do whatever you have to do."* TWO faults, and either one alone makes the other's fix a no-op. The pinned box sits INSIDE the container, so it spans the container's CONTENT box and never reaches the corner the container's radius is drawn on (its BORDER box, one inset further out); and a rounded corner is a transparent NOTCH with the container's own paper directly behind it — the container runs far below the pin, only its top EDGE has scrolled away — so filling nothing shows the same tone as the band and the corner still reads square. So: `--pinned-inset-x` is the container's SIDE inset, spent back as `px` and a negative `mx` — (v)'s pair on the other axis, asserted as a pair for the same reason — and the box reaches the border box without the row moving a pixel; and `--pinned-behind` is what is BEHIND the container, painted across the whole band, with a `::before` painting `--pinned-ground` over it at `rounded-t-[var(--radius)]` so the notch shows exactly what the container's real top corners show at rest. A pseudo-element and not a wrapper, because (i)'s flex COLUMN is what keeps R49's trailing gap inside something that paints and a real child would join that column. **The fallback is the IDENTITY**, `var(--pinned-behind, var(--pinned-ground))`: a toolbar that is nobody's inset child paints one tone front and back, so its corners are cut out of the tone they show and it pins flush and square exactly as it did before — no branch, no second class, nothing for a screen to decide. **The behind-ground is captured one level UP**, and that is the load-bearing half: `--pinned-ground` is published by the ground class itself, so a container that PAINTS has already overwritten it for itself and everything inside it, and the last element still holding the outer value is the container's PARENT. `CollectionCard` is that ground-class element, so it wears `PINNED_INSET_MARK` and `globals.css` captures on the parent through `*:has(> .pinned-inset)` — the identical `:has()` move (iv) makes off `.pinned-strip`; `PINNED_TOOLBAR_IN_KIT_PANEL` already rides the frame's root, which is outside the kit's panel and paints nothing, so it publishes `--pinned-behind` directly. One arithmetic, two publishers, two ways of reaching the element above the box because the two boxes sit differently. And the stand-down rule puts the identity BACK on a nested row along with a zero side inset: a stood-down toolbar is not pinned, and a rounded band of outside ground in the middle of a panel is a hole cut in the paper. R31 is widened, not broken — same value, same token, one edge — and its prose now names this position beside the bottom sheet. *Measured in a browser* at 1440×900 and 375×812, because a sticky rule that silently does nothing looks exactly like one that works in source. Three things only the browser could say: the offset is real (strip pins at the pane's top edge y=50, toolbar lands on its bottom edge y=116 on Accounts, Apps, Contacts, Sprints and a record's Contacts panel; flush at 50 where there is no strip); `position: sticky` is bounded by its own **containing block**, and the tickets Dashboard — the one call site that boxes the row in furniture of its own, its panels siblings of that card rather than its contents — measured a stuck range of **32px**, i.e. no pin at all, against 3,011px on Accounts, so the pin moved out to the box; and **two pinned toolbars are worse than one** — the knowledge base draws a `<PagedFind>` row and the kit frame's own `+` row, and pinned together the outer painted over the inner one's create button, so a container that already holds a pin stands the nested one down. *What the browser also said and this law does not fix:* at 375px the agency row wraps, so a pinned Accounts toolbar occupies 264 of a phone's 702px — the kit's own `ToolbarRow` solved that upstream with a one-row layout and a `···` overflow and this app's bespoke row has not adopted it. That cost is written down rather than hidden behind a breakpoint nobody asked for. | `pinned-toolbar` (`web/test/rules.test.ts`) | enforced |
| R64 | ui | **A section on the team area's strip has a door, or names the screen that took its place — and that screen carries its acts.** R40 asks whether a stored file reaches a person; this asks the same question one layer up, about a CAPABILITY. `TEAM_SECTIONS` (`web/lib/pages.ts`) entries with `placement: "tab"` live only on the team area's own strip, and the app's one entrance to that area is the "This team" list on Settings › Team, DERIVED from that same table minus the keys its own filter subtracts. So a subtracted key has no door at all unless somebody built it one, and the law is that it must SAY where: a `SECTION_HOSTED_ELSEWHERE` line naming the file that carries that section's material. **Four clauses, every census off the disk, each end grounded in a different oracle.** (i) The offered half is real — the `adminSections` panel actually soft-navigates into `/t/<teamId>/<segment>`, so the subtraction is never checked against a door that has itself been deleted. (ii) Every subtracted tab section has an entry, and every entry names a key that is still both a tab section and subtracted — rot-checked both ways, so the list can only shrink. (iii) The named file exists. (iv) And it carries the section's ACTS: the acts are derived from the recipes bound to that module in `web/lib/screens.ts`, each act resolved to the door-client call its own dispatcher makes in `web/lib/use-screen-actions.ts` (`tenancy.setMemberRole`, `tenancy.removeMember`, `tenancy.revokeInvite`), and the host must make that same call. Nothing hand-listed but the host filename. *Earned by a shipped, green regression.* The Settings › Team redesign (2026-09-09) replaced the members ladder with a gallery that deliberately does not navigate and left three administrative acts — change a member's role, remove a member, revoke a pending invitation — on `/t/<teamId>/members` and `/t/<teamId>/invites`. A census of every `softNavigate(...)` and `href=` under `web/` found exactly ONE link into the team area anywhere in the app, and it points at one member's RECORD rather than the collection; the only other entrance was the "This team" list on that very tab, which by then rendered a single row — Internal rates, gated on `commercials:read`, a section that was itself removed on 10 Sep 2026 and left that list rendering nothing at all. So an owner WITH that right reached member management by opening a rate card and hopping sideways on a tab strip, and an owner WITHOUT it, holding full `team_members` rights, had no in-app path to any of the three. Nothing was broken: every door answered, every gate was right, every test was green, and the gallery's own header comment told the next reader that role changes *"still live on the member's own record, reached from the team area's Members section"* — naming a route nothing linked to. **The app's reachability lived in prose, and prose does not fail a build.** *Why the subtraction and not links:* a link census cannot see the one real door, which is written `/t/${teamId}/${item.id}` over a derived list — a law that counted links would have had to special-case the very door it was checking. Subtracting a key is the one deliberate, greppable act that takes a section's door away, so it is the act that has to say where the material went. **(v) AND THE CONTEXTUAL HALF, ADDED 11 SEP 2026.** Everything above walks `placement: "tab"` rows, so a section was only ever noticed when somebody SUBTRACTED it — and a `placement: "contextual"` row was never on the "This team" list to be subtracted from. It could therefore carry real capability with nothing linking to it, under a permanently green build. That is not hypothetical: `dropdowns` was moved tab → contextual on 2026-09-01 and `ManageDropdownsLink` was repointed at `?tab=choices` the same day, so from that moment nothing in the app opened `/t/<teamId>/dropdowns` — a screen with an import door, an export and a record split — and it sat there for ten days while the law written to catch exactly this shape watched it happen. So a contextual section must be REACHED by a literal navigation under `web/` whose path ENDS at its segment, or it joins the same `SECTION_HOSTED_ELSEWHERE` registry as a subtracted one. **A link census here, and not on the tab half, and the difference is the whole argument:** the objection above — that the one real door is written `/t/${teamId}/${item.id}` over a derived list — is an objection about that list, and a contextual section is never in it. **Ending at the segment is the clause with the teeth,** and it is this law's own lesson read once more: the 2026-09-09 census found exactly one link into the team area and it pointed at a member's RECORD, so `/dropdowns/${v.id}` must not count as a door to `/dropdowns`. **The cost, stated:** the census sees only LITERAL paths, so a contextual section reached by a computed segment reads as unreachable and needs a written line — which is a reviewer reading a claim rather than a build going quiet. *Earned twice over on the day it was written:* the widening turned the build red on a SECOND orphan nobody had filed, `processes`, whose collection screen at `/t/<teamId>/processes` is linked to by nothing at all — its maps are reached from an app's own record. **(vi) THE TAB STRIP ITSELF RETIRED, 2026-09-14.** A client screenshot of a standalone Members page still carrying its own tab strip (Members · Member roles · Invites), and the ruling that closed it: *"what is this? told you to kill it. Now this only lives on settings / team."* `members`/`roles`/`invites` were the only rows that had ever carried `placement: "tab"`, and the 2026-09-09 pass had already subtracted all three from the "This team" list days earlier — but subtracting a key from that list only ever took the address off the list, never off the app, and the collection screens themselves kept resolving, strip and all, for anyone who still had a bookmark or the two legacy shims (`web/app/members/page.tsx`, `web/app/roles/page.tsx`). So this time the screens went, not just the list row: all three moved to `placement: "contextual"`, `web/components/deep-link/module-content.tsx` sends every one of their addresses to Settings › Team instead of drawing a page, and clauses (i)–(iv) above are now permanently vacuous by construction — `web/test/rules.test.ts` proves that positively (every `TEAM_SECTIONS` row that ever carried `placement: "tab"` is asserted `"contextual"` by name) rather than trusting an empty set to mean the same thing an empty set always used to mean. Clause (v) is what actually holds the three now: none is reached by a literal ending at its segment, so all three carry a `SECTION_HOSTED_ELSEWHERE` line same as `processes` does. | `sections-have-a-door` (`web/test/rules.test.ts`) | enforced |
| R65 | ui | **On a card that stands for a record, the chip sits above the title.** The client's ruling, 2026-09-10, on the member cards of Settings › Team: *"in team, adn generlaly in this component write the law, chip on top of title & bigger images"* — the second time, after *"in cards put chips above title"* about the Kanban card. A card is scanned top-down and the chip is what SORTS it (a role, a stage, a type); read UNDER the name it qualifies nothing. **Which cards is DERIVED and the oracle is React's, not this law's:** a card that stands for a record is a kit `<Card>` carrying a `key=`, because a card drawn one per row of a collection MUST have a key and a card that is a panel around a section must not — a partition nobody here can bend. Three clauses over that census: (i) a record card names its record through the kit's `<CardTitle>` — a title hand-rolled into a `<span>` has no position a census can read, and *above* is a claim about position; (ii) every `<Badge>` in it opens BEFORE that `<CardTitle>` in source order, which IS visual order in a card (`Card`'s own cva is `flex flex-col`, "a card is a column"); (iii) the way out is data — `CARD_CHIP_BELOW_OK`, rot-checked both ways, empty today. Earned because the two cards she ruled on were fixed a day apart in opposite directions under a green build, and because clause (i) cost a real change: the member card's name was a bare `<span>`, so a position census over it would have reported a perfectly ordered card while reading nothing at all. | `chip-above-title` (`web/test/rules.test.ts`) | enforced |
| R66 | ui | **A pictograph is not a word, and not a mark either.** The client has ruled three times — *"i said no emojis. why are there still emojis? kill them!"* (2026-08-31), *"for type, kill the emojis. this is legacy. in current system we use colors"* (2026-09-07), *"also kill emojis!!!"* (2026-09-10) — and until the third the whole defence was ONE write door, `optionalMark` (`shared/workers/validate.ts`), on ONE kind of field. No emoji appears in the words a person reads, or in the data behind them. **What counts as one is the door's own answer, imported:** the check CALLS `optionalMark` rather than carrying a codepoint table, so the law and the door share one definition by construction — and the line lands where it should, because `Extended_Pictographic`/`Regional_Indicator` leaves the typographic dingbats this app and the pinned kit legitimately draw (U+2715 on a close button, U+27A4 and U+2605 as department marks) alone. **Four census targets, four different oracles.** (i) `shared/i18n-strings.json`, which R28 makes EXACTLY the set of user-visible English sentences — so this covers every word a person reads without owning a walk of its own, the composition R44 already makes. (ii) `shared/i18n-catalogue.ts` and `shared/i18n-seed.ts`, the other three languages: a German sentence with a pictograph in it is the same fault and invisible to (i). (iii) **The vocabulary data** — the team seed and the migration ledger that back-fill `selectable_data`, plus the three shared tables whose rows carry a `mark`. This is the half a copy census cannot see, because a mark is DATA: it is in no catalogue and no `t(...)`. Censused WHOLE, comments included, because the repo already states the convention in `optionalMark`'s own header — name a glyph by codepoint, never paste one. (iv) `shared/i18n.ts`, the one file that renders a pictograph on purpose. Source comments under `web/`, `web-portal/` and `workers/` are deliberately OUT: 28 of the 32 pictographs in this repo's source are there and every one is EVIDENCE — her own message quoted beside the change it caused, a Google Chat line measured on staging, a test fixture. *Earned by:* the 2026-08-31 ruling WAS answered, at the door and apparently in the data — migrations `0034` and `0044` substituted two-letter codes for the seeded marks. All eight of those statements are guarded `AND mark IS NULL`, so a row that already held a pictograph is not null and not one of them could ever have replaced one: they filled the EMPTY marks and stepped over exactly the rows the ruling was about. Six weeks later, green build, door shut, seed clean, she was still looking at a warning sign beside "Issue". **The exemption is a CLASS, not a list** — widened 2026-09-10 at her fourth ruling, *"keep emojis for countries and languages only"*. A pictograph passes when it is a FLAG: a pair of Regional_Indicator characters (Unicode's own definition of a flag sequence, and a shape nothing else in the emoji set has) naming a region ICU's own table recognises (`Intl.DisplayNames`, `fallback: "none"`). Two oracles, neither of them ours, so a fifth language or the first country flag is covered without editing a law — where a hand-list of four glyphs would have rotted at the next one, and the file-level line this replaced would have excused a warning sign pasted into the same array. **One predicate answers both halves of her sentence**, because Unicode has no language pictograph: a language is drawn by the flag of a country that speaks it (Andorra for Catalan — Catalonia has no sequence of its own), so a further clause asserts off `LANGUAGES` itself that every language the app speaks is marked by a country flag and nothing else. Still red: a lone regional indicator, a pair naming no country, and every other pictograph — an emoji mark on a ticket type above all. **The write door was NOT widened**: `optionalMark` still refuses a flag, so the law permits more than the door and never the reverse, and nobody can type a country flag into a value's `mark` (the `Country` group's ten legacy labels carry no marks at all today). The remaining way out is a reasoned `EMOJI_OK` line, rot-checked both ways, for a pictograph that is the content and is not a flag — it is EMPTY. | `no-emoji-in-copy` (`web/test/rules.test.ts`) | enforced |
| R67 | ui | **A section paints the page or the kit's paper — never a stroke, never a hex.** *(This row's first sentence read "a titled section — or a tab panel — stands on paper; nothing is drawn on the bare page ground" until 21 Sep 2026, when its author overturned it app wide. The whole history below is kept, verbatim: it is the record of what the law was and of the five rulings that built it, and the tenth amendment at the end is what replaced the premise.)** The client's ruling, THREE times in three days, the second time asking for the law and the third about a screen the law as written could not see: *"more members in each row, too much blank space. needs container!! nothing on top of white background, its a rule!"* (2026-09-09, the Team tab) and *"but give it a container. once again, nothing shoudl sit on the white, everything contained! (make this a law)"* (2026-09-10, Settings › Integrations) and *"remember in settings modules card, needs container background."* (2026-09-10, Settings › Modules). *The subject:* every `<section>` element under `web/`, `web-portal/` or `shared/web/` — it carried a heading of its own (`<h1>`…`<h4>`, `<Headline>`, `<CollectionHeading>`) until amendment 4 below dropped that requirement. *What a container is, derived off the kit's own `tokens.css`:* the paper family — every `--surface-*` token plus an alias more than one of them points at (`--card`, which is why `bg-card` counts and `bg-muted` does not), **minus `--surface-page`**, which the kit defines as `var(--background)` and which IS the ground. *Two shapes pass, and they are one sentence read from either end:* the section IS the box (`web/components/team/team-panel.tsx`), or **every body it draws** stands in one (`CollectionFrame` — heading outside, content on paper). *The clause with the teeth:* containment is asked PER BRANCH, through fragments, ternaries, `&&` and `.map()`. Access tokens drew its rows on paper and its error, its skeleton and its zero on the page — "is there a panel in this section anywhere" answers yes and describes the wrong screen. *Deliberately not content, each a decision:* the title block, prose (narrowed to `<a>` and `<br>` by amendment 4 below), a lone act (`<Button>`), and an overlay (a component that reaches a portal, resolved transitively — `<AddLinkDialog>` → `<FormShellDialog>` → kit `<Sheet>` → Radix portal). *Earned by:* 22 uncontained sections on the first run. Eleven in the agency app, every one an inconsistency inside a screen that already used paper — including the three Appearance option groups on the tab she was looking at, where the kit's default `Card` is `--card` and `--card` IS `--background` in light, so the cards measured **contrast 1.000** against the page. All eleven fixed. The remaining nine are the whole client portal and are exempted in `UNCONTAINED_SECTION_OK` with a reason each: the portal is consistent with itself, so panelling it is a redesign, not a repair. *WIDENED 2026-09-11, ON THREE AXES, BECAUSE THE THIRD RULING PROVED EACH ONE HID THE SAME SCREEN — and any one of them alone would have been enough to hide it.* **(1) The ground is a VALUE, not a name.** The family subtracted `--surface-page` and `--background` by SPELLING, and four of the survivors resolve to the page's own colour: in light `--card`, `--surface-raised`, `--surface-lift` and `--surface-selected` are all #FFFEF9, as is `--background` (`--surface-idle` and `--surface-record-footer` collide in dark at #141310). So `bg-card` on the page answered "contained" at **contrast 1.000** — which is not a near-miss of this law, it is the worked example in this row's own text. A fill is now a container only where it differs from the ground in BOTH palettes, resolved off `tokens.css` through its `var()` chains, with the comments stripped first because that file quotes declarations inside its own prose. `bg-card` is still a container on a PANEL; it cannot contain what stands on the PAGE. **(2) A component paints what the CALL SITE picked.** The paint test read a component's whole text, and a `cva` carries every variant's classes whether or not anybody reaches for them — so `<CardGrid>`, whose `tone` defaults to `bare` and paints nothing, answered "I paint" off the `panel` option two lines below it. That WAS the reported wall. The selected variant now decides (the literal passed, else `defaultVariants`); a non-literal prop stays "may paint", so the walk keeps under-reaching. **(3) A tab panel is a titled section.** Its title is in the STRIP, so it has no heading of its own and the `<section>`+heading subject could never reach it — 14 `renderPanel` hosts and 70 bodies outside the law by construction, on the two screens two of her three rulings were about. Same two shapes: the `<TabsView>` MOUNT is boxed by an ancestor (11 of 14 are — a record detail hands its whole strip to `RecordScreen`'s card, and without this the clause reports 18 offenders of which 14 are that container seen from inside), or every body it draws stands on paper, per branch. Panel exemptions are keyed `path#tabValue`, not per file, because one file's tabs are not one decision. *The cost, stated:* five findings, one fixed at the screen (Settings › Modules, `CardGrid tone="panel"` — measured 1.103 panel-on-page and 1.103 card-on-panel in light, 1.079 and 1.111 in dark, against the **1.000** it was shipping in light), one fixed beside it (that tab's refusal branch), and three written down: `apps-screen.tsx`, whose stage tiles are the SAME 1.000 on a busier screen and flat besides, and two panels mid-change in another lane. *Known blind spot, written down:* components resolve by NAME, so a collision (recharts' `Tooltip` vs the kit's) makes the walk under-reach; that is the deliberate direction. Rot-checked, with a blindness tripwire that fails if the fill family, the file walk or the heading census ever comes back empty.  **AMENDED AGAIN 2026-09-11, AND THIS TIME THE LAW WAS OVERRULED RATHER THAN WIDENED.** Her fourth saying of one sentence, over Settings › Integrations: *"i said nothing on white backgorund. remove this text Access tokens / Let an outside tool (an AI agent, a script, an automation) work in your team as you, capped by your role, in the team the token was made for. for google replicate the no tokens yet, sth like "connect to google" and the button to do so. remove the text directly on white background."* Every earlier amendment closed a hole; this one deletes a CLAUSE. PROSE was listed above as deliberately not content on the stated reasoning that *"a sentence directly under a heading is part of the title block. Every settings section in this app is heading + sentence + control, and boxing the sentence would be a different design, not this rule"* — a prediction about what she wanted, made on her behalf, in a lane she was not in, and now rejected by her. **(4a) Prose is content when it is not inside the thing it describes.** It stays exempt where it rides IN the title block (already skipped as a unit) or inside a body that paints (already skipped by the subtree walk); a sentence that is its OWN body, beside the box it describes, fails. Two tags survive and neither for being prose: `<br>` draws nothing readable, and `<a>` is an ACT — *"a control is pressed, not read"* is this law's own line about a lone `<Button>`, and catching the link beside the button would be the law disagreeing with itself. **(4b) A section does not leave this law by deleting its title.** Her fix DELETED two headings, so under the old subject the commit that answered her would have removed both sections from the law. The subject is now the `<section>` tag itself. *What shipped:* Access tokens' eyebrow and sentence deleted outright (the contained register below already titles the section and carries the one first-add — two titles for one thing, one of them floating), with the create `+` moved inside the panel; Google's eyebrow, sentence and connect row folded into ONE card drawing `CollectionEmptyState`, the neighbour's own register rather than a second thing shaped like it, titled "Connect to Google" with "Connect everything" as its one act. *What it cost, in `UNCONTAINED_SECTION_OK` rather than hidden:* seven more files — the three Settings › Appearance sections (the same shape, HALF-FIXED on 2026-09-10: that pass moved the option cards off `bg-card`'s contrast 1.000 and left the title block on the white, because the law said prose was not content), two in the work module and `impact-screen.tsx` on the portal. They are a census awaiting ONE ruling, because on 2026-09-10 she ruled the OTHER WAY about the module settings pages' descriptions — *"The section description: no, I want to keep it."* The two reconcile (keep the words, stop leaving them on the white) but which she means per screen is hers. *Known blind spot, measured not assumed:* the seven module settings pages are the app's biggest heading+sentence-on-the-ground surface and are unreachable — their root is a `<div>` and their words are PROPS off `MODULE_SETTINGS`. A variant judging prose inside the title block was written and measured the same day and is not here: the census came back byte-identical, so it would have enforced nothing. Amendment 4 carries its own tripwire, because both halves are subtractions and a subtraction that stops subtracting is invisible: at least one headless `<section>` must be in the census, and at least one bare body must be a readable sentence. **AMENDED A FIFTH TIME, 2026-09-11 — A COMPONENT PAINTS WHAT ITS OWN ROOT PAINTS, and this one turned red on the commit that fixed four screens.** The client, over a screenshot of Settings › Ticket settings: *"ticket types should be on top of the searchbar inside the container without subtitle, make this. always"* — her FIFTH saying of one sentence in three days, and the first that says where the title goes rather than only where it may not stand. The answer is a CHOKEPOINT rather than a sixth repair, which is what the blind spot amendment 4 wrote down actually called for: the fourteen module settings sections are unreachable by this law because their root is a `<div>` and their words are PROPS, and widening the census to judge a component by the props it is handed is a different oracle with a real cost — so the title stopped being something a call site can PLACE. `<ToolbarRow title>` (a string, never a node) draws it inside the pinned band for the seven vocabulary sections; `shared/web/settings-section.tsx` owns the box AND the heading for the seven Automations ones and for Settings › Appearance's four; and `MODULE_SETTINGS` lost its `description` COLUMN with the sentences, so a seventeenth section has nowhere to declare a subtitle. **THE AMENDMENT IS WHAT THAT COST THIS LAW.** `componentPaints` reads a component's own text and its own file's constants and deliberately does not follow what it RENDERS — so the moment `ThemeSection` and its three neighbours stood in a shared box instead of spelling `bg-surface-panel` themselves, Settings › Appearance reported as a bare `<div>` on the page ground. A law that reddens when four screens are fixed by one component is measuring the WRITING, not the SCREEN. So the walk now follows ONE edge: the single element a component RETURNS, transitively (`ThemeSection` → `SettingsSection` → `<section class="… bg-surface-panel …">`). **IT IS NOT THE VERSION THAT WAS THROWN AWAY,** and the difference is in kind: that one followed every component a component renders, so a fill anywhere in the subtree answered for the whole of it and every uncontained zero register in the app came back green. This follows the box the component IS, which is the box its caller is standing in too. `CollectionEmptyState` roots at a bare `<div>` and still does not paint — asserted in the tripwire, by name, because it is the exact regression this could reintroduce. EVERY return, not the last one: a component with an early `return <ErrorPanel/>` has more than one root and they are not interchangeable, which is this law's own per-branch clause read one level down. Measured: the root walk changed exactly ONE verdict in the whole census, the false one it was written for. **AND R50 MOVED ONE INCH,** because a title drawn by the toolbar must outlive the toolbar: `<ToolbarRow>`'s empty exit returns `heading`, which is `title ? … : null`, so seventeen of eighteen call sites are byte-for-byte unchanged and a section whose NAME is drawn by its row keeps it when the collection is empty. R50's sentence — never a TOOLBAR on an empty collection — is untouched; a title is pressed by nobody. Its check's proxy is now strictly tighter than the `return null` literal it replaced: it pins what that return can BE. **AMENDED A SIXTH AND SEVENTH TIME, 2026-09-14 — THE OVERLAY EXCLUSION WAS MEASURING "RENDERS ANY PORTAL", AND A TOOLTIP DOES.** The OVERLAY exclusion (amendment 0's own text, above) read "a component whose declaring file renders through a portal" — and every floating Radix primitive portals, a tooltip included, for POSITIONING rather than because it is a modal. An instrumented run found 44 sections and 15 panels judged with 20 skip decisions, nine of them excusing a component that is plainly not an overlay (`<PagedFind>`, `<ScreenRenderer>`, `<TodosPanel>`, `<AppSavingsChart>`, `<MatchKind>`/`<StepKind>`, `<BrandPanel>`, `<ModuleAutomations>`, `<SettingsChoicesPanel>`), several of them because the old test read the DECLARING FILE, so one real modal declared anywhere in a multi-panel file (`kwapso-screen.tsx`, `settings-screen.tsx`) excused every OTHER component in that file too. **(6) AN OVERLAY IS A NAMED FAMILY, ASKED PER ROOT.** The family is `Sheet`/`SheetContent` and `AlertDialog`/`AlertDialogContent` (R59's own two shapes), plus bare `Dialog`/`DialogContent` (R59's third, discouraged-but-live centred shape) — Popover was tried and measured OUT, because it regressed the law's own reference shape: `CollectionFrame`'s filter bar reaches for one, and with Popover in the family `isOverlay("CollectionFrame")` came back true, a worse failure than the one being fixed, on the same reasoning that already keeps `Select`/`Combobox`/`DropdownMenu`/`ContextMenu` out (each is a CONTROL, covered by the ACT exclusion already). And the walk is asked of EVERY ROOT a component can return (amendment 5's own `rootElements`, reused), not of any tag its text merely mentions: `ScreenConfirm` (one root, an `AlertDialog`) still resolves true; `ScreenRenderer`, whose other branches are an ordinary `ScreenLayer` and a bare `<div>`, no longer inherits its one confirm branch's verdict. The skip set is PINNED by name (`OVERLAY_FAMILY_OK`, in the test file), rot-checked both ways, so a widening is a reviewed addition and never a silent skip again. **(7) A BARE IDENTIFIER IS AN UNRESOLVED BODY, NOT A ZERO ONE.** The per-branch walk (`bodies()`) resolved a ternary, a `&&`, a `.map()` and a JSX literal and nothing else — a bare `{children}`/`{body}`/`{content}` expression matched none of those and was silently dropped, so a chokepoint that forwards its caller's content without painting itself reported zero bodies, reading as "nothing to judge" rather than "this walk could not see in". `shared/web/settings-section.tsx` draws exactly that shape and is unaffected today only because its own `<section>` paints itself directly, resolved before `bodies()` is ever reached. A bare identifier is now COUNTED rather than silently dropped, pinned at zero against the real app and proved alive against an owned fixture the same two-part way amendments 4 and 5 already prove their own tripwires. **THE CORRECTED CENSUS FOUND THREE NEWLY-JUDGED SECTIONS.** `web-portal/components/impact-screen.tsx`'s standalone savings chart is FIXED — given the same `bg-surface-panel` tone the rest of that file already uses — rather than exempted under N6 ("a block earns a container only with two or more rows/fields"), because this exact file's history already records the client overruling that very argument once. `web/components/accounts/contacts-by-company.tsx` (unreached dead code — no live screen renders it) and `web/components/screens/kwapso-screen.tsx#brand` (matching its already-exempted `#team`/`#default` siblings on the same mid-change screen) are named in `UNCONTAINED_SECTION_OK` with their reasons. **AMENDED AN EIGHTH TIME, 2026-09-14, BY THE LANE BUILDING THE SETTINGS AUTOMATIONS TAB, WHICH HAD TO WORK AROUND TWO BLIND SPOTS RATHER THAN HIT NEITHER:** declarations now resolve an import binding ONE HOP through the same file list this walk already reads, so a tag written under its alias (`KitCollectionFrame`, the app's own name for the kit's real `CollectionFrame`) is no longer read as painting nothing; and `componentPaints` stopped scanning a component's whole function as one string — an early guard return is cut before the scan (so an unrelated `NoAccess`/error/loading branch can no longer vouch for content it has nothing to do with), and the scan gained a same-file-IMPORT hop beside the same-file-constant one it already had, because `rootPaints` cannot follow a Fragment root or a `{children}`-forwarding wrapper the way it follows an ordinary single-root chain. `module-automations.tsx`'s `RecordTable` no longer carries the redundant `className` that used to say by hand what the census can now see for itself. **AMENDED A NINTH TIME, 2026-09-15 — THE SIBLING `renderPanel` SHAPE, AFTER R77 MOVED THREE HOSTS OUT FROM UNDER THIS CENSUS THE SAME DAY IT SHIPPED.** R77 (`tab-strips-pin`) rewrote `kwapso-screen.tsx`, `settings-screen.tsx` and `module-settings-screen.tsx`'s own `<TabsView renderPanel={…}>` JSX ATTRIBUTE into `renderFolderTabs(…)` beside a plain SIBLING `(function renderPanel(panel) {…})(…)` — the split every collection screen already draws, so the pinned strip (`STICKY_FOLDER_TABS`, which pins the WHOLE `<Tabs>` root) no longer pins the panel's own content along with it. R67's panel census found a host by that JSX ATTRIBUTE ONLY, so all three screens dropped out of the census the moment they adopted R77 — their `UNCONTAINED_SECTION_OK` lines were deleted as "matching nothing" rather than re-judged, and the panels underneath were never fixed. A green that measures nothing is this file's own recurring lesson, in a new shape. **The fix is a second host shape, found by POSITION rather than by spelling:** two JSX children of one element, in order — one that CALLS `renderFolderTabs(`, immediately followed by one that is an IMMEDIATELY-INVOKED function expression or arrow. An arrow IIFE carries no name to match, so adjacency is the whole test, and the NAMED form all three screens actually write (`function renderPanel(panel) {…}`) matches the identical clause without a second one. The MOUNT is the shared wrapper — the JSX parent of both siblings — playing the same role `<TabsView>` played for the attribute form. Measured against false positives: `renderFolderTabs(` has three other call sites in the app and not one is followed by an IIFE, so the clause reaches exactly the three screens named above. **Re-judged, not reassumed:** the census found the identical three bare panels `kwapso-screen.tsx#team`/`#default`/`#brand` always had (restored in `UNCONTAINED_SECTION_OK` with the same reasons), while `settings-screen.tsx` and `module-settings-screen.tsx` both pass outright — their own panels resolve through the same chokepoints amendment 5 already taught this walk (`SettingsSection`, a kit `<Card>`, `<ToolbarRow>`'s own pinned band). Measured: 12 hosts (11 boxed, 0 offenders) before this amendment became 15 hosts (11 boxed, 3 offenders, all three on `kwapso-screen.tsx`) after — the same three bodies this census has always reported for that screen. **AMENDED A TENTH TIME, 2026-09-21 — AND THIS TIME THE PREMISE ITSELF WAS OVERTURNED.** Aurora, on the Minimal Kit page, verbatim: *"board A / space 6 / go an implement this appwide, also implement the to the bottom edge for main content and assistant like in your previous artifact / also, make footer not inside a container, but the full row side to side (within the main content) / do think a lot about each component, what this minimalising means so that it still works"*, with the goal she set for the whole pass in her own words: *"the goal: make a more minimal clean app"*. This law was built out of five sayings of ONE sentence in three days — *"nothing on top of white background, its a rule!"* — and she has now ruled the other way over the whole app, with a validated page behind it (rulebook L43). A law does not get to go on enforcing a premise its author retired, so the clause that failed a build for a body on the page ground is **deleted**, and `UNCONTAINED_SECTION_OK` is emptied by its own rot-check: eleven entries that were arrears against "everything contained" are not arrears against anything any more. **WHAT STILL HOLDS IS THE OTHER HALF OF THIS ROW'S OWN SENTENCE**, and it is the whole of the offence now: separation is a fill or an inset shadow, **never a stroke** (BUILD-A-SCREEN.md §6.1, *"no CSS border, ever"*), and a colour resolves through a token, **never a hex** (R32). That clause never had a census here, because while every section carried a fill there was nothing for a stroke to be the alternative TO. There is now: a section that has given up its box is exactly where somebody reaches for an outline to get the edge back, and one outline would undo the pass. **THE `<section>` CENSUS STAYS, WHOLE** — same subject (the literal tag, amendment 4), same walk, same per-branch `bodies()`, same paint resolution, same tab-panel half (amendments 3 and 9), same tripwires. It admits the page ground: `bare` is still computed and still asserted non-trivial, because "the census went blind" and "the app went plain" look identical from outside and that number is the only thing that tells them apart. The matcher is proved both ways against named shapes (`border`, `border-b`, `outline`, a `bg-[#…]` literal fire; `bg-surface-panel`, `border-none`, `border-0` and a `shadow-[var(--hairline-under)]` do not), and the stroke scan rides the EXISTING `bodies()` walk rather than a second one, because that function is not pure — amendment 7's counter ticks inside it and this file's own tripwire pins the number. *The other side of the same ruling* is a new census in the opposite direction, `PAPER_ON_PURPOSE` (`shared/rules/registry.ts`, `web/test/plain-surface-scope.test.tsx`): every `<Card>` in either front door that still paints soft paper must say which of the five things it is that a grouping section is not — a conversation card, an empty or error state, a tile, a well, or a not-a-section. | `sections-stand-on-paper` (`web/test/sections-stand-on-paper.test.ts`) | enforced |
| R69 | arch | **A sighting is never written through the raw primitive.** `knowledge_sightings` decides who may read a folded source, and `team_visible` is a denormalised copy of what its rows imply — so a write that changes a sighting's `shelf` or `gone_at` without recomputing that copy in the SAME script leaves the fence answering from a stale value. `execKnowledgeScript` enforces that at RUNTIME by inspecting the resolved script string and walking each write's own interval; it cannot see a writer that never calls it. *The subject:* every file under `workers/*/src` whose source writes `knowledge_sightings`. *The one exemption is DERIVED, not listed:* the file that EXPORTS `execKnowledgeScript` is the wrapper, and a wrapper must call the thing it wraps — move the wrapper and the exemption moves with it. *Why it needs a law at all:* twenty files in `workers/content/src` already call `d1ExecScript`, so reaching for the familiar primitive is the DEFAULT rather than the exception. *Written the day a real writer existed*, deliberately: pinned against an empty set it would have been a check that passes because there is nothing to check. Its own census asserts it is non-empty for that reason, and both directions are mutation-proved. | `guarded-sighting-writes` (`web/test/rules.test.ts`) | enforced |
| R68 | arch | **One identity per source, re-pointed at the mechanism that was always doing the work.** BUILD-5-knowledge-rebuild.md's fault, closed by 0073: a Google item's key used to be `<readerId>:<externalId>`, so the same Drive folder shared with two colleagues filed as two `knowledge_sources` rows, each chunked, embedded and stored separately. 0073's fix reached for a new column, `identity_key`, and a new unique index over it — but the real fold has always run through migration 0012's `idx_knowledge_sources_origin`, a UNIQUE PARTIAL index on `(origin_table, origin_row_id)`, sixty-one migrations earlier, and `identity_key` sat beside it for six weeks enforcing the same fact a second way, written by nothing (`grep -rn "identity_key" workers/content/src/` found two hits, both comments, before migration 0080 removed the column). **Two clauses, each grounded in a different oracle, now pointed at the real thing.** (i) The constraint is real: read straight off 0012's own migration SQL for the literal `CREATE UNIQUE INDEX idx_knowledge_sources_origin ON knowledge_sources (origin_table, origin_row_id) WHERE origin_row_id IS NOT NULL` — never trusted from a comment. (ii) The fold's contract is the census, not the presence of one function: the naive version of this clause — "does a file exist whose INSERT carries the right ON CONFLICT" — is trivial by construction, since there is exactly one shared upsert function (`workers/content/src/lib/knowledge-ingest.ts`), so of course one file has it; that is the same vacuous-population failure clause (ii) had under the old column, wearing a different hat. The real question is a PER-ROW predicate: does anything that CLAIMS an origin (its own INSERT names `origin_table` in its column list) skip the fold it obligates? Every `INSERT INTO knowledge_sources` under `workers/content/src/` is censused — three today, pinned — and each is decided individually: two are exempt because they never claim an origin (a typed note, an uploaded file, both in `knowledge.ts`), one is compliant because it claims one and folds (`knowledge-ingest.ts`). *Earned twice, by the same shape a fold apart.* First by BUILD-5-knowledge-rebuild.md's own fault above; then by this law's own first draft, which enforced a column nothing wrote and a per-file check with a population of exactly one file — passing for the same reason a check with zero subjects passes, because there was nothing on either side to fail. Re-pointed rather than dropped, because the SENTENCE was right: one identity per source, computed the one way the fold actually reads, never a string with the reader baked in. Proved both ways: breaking the compliant site's `ON CONFLICT` target turns clause (ii) red; adding an unfolded origin claim to an exempt site also turns it red — the second is the one that matters, because it proves the rule decides a ROW rather than counting a population. | `one-identity-per-source` (`workers/content/test/one-identity-per-source.test.ts`) | enforced |
| R71 | ai | **A human-facing label speaks the app's word, never the alias a tool description offered the model.** `describe_module` and `query_records` accept a module by any of its aliases — their own description says so, in words: *"`help` reaches tickets"* — because CLAUDE.md's own "don't finish the rename" keeps `help` as the permission module, the table, the API path and the MCP tool names on purpose. Nothing stops the model reaching for exactly the word its own tool description just offered it, and the agent's step chip / confirm panel built its label straight from that raw argument, so the alias printed VERBATIM on a screen the product has no section named after. The owner's own report, 13 Sep 2026, reading the assistant's step chips on staging verbatim: *"See what help can be asked"*, *"Look up help"*, *"Count help by account"*, *"Count help by app"* — a person reads those, and the product has Tickets, not Help. The fix routes every `summarize()` in `shared/workers/tool-catalog.ts` that builds a label from a schema field spelled `module`, `table` or `targetTable` through `queryLabel`, which wraps query-grammar's own `canonicalModule` — reusing the query engine's own answer to "what did they actually mean" rather than a second map that could drift from the door's. Checked by RUNNING every tool's `summarize()` with a poisoned alias, derived off the schema field NAME rather than a hand-list of the two tools that leaked (the same shape R22's body-parity proof stands on: prove it by calling the function, not by reading it), so a future module's `module`/`table`/`targetTable` argument is covered without anyone updating this law. | `agent-label-vocabulary` (`workers/data-ops/test/agent-label-vocabulary.test.ts`) | enforced |
| R70 | workflow | **Every automation is on its module's settings page, and one that cannot be switched says why.** The client's ruling, 2026-09-11, shown a census of the automatic behaviours in this product: *"include absolutely all of those in settings by module. I want no automation without visibility"* and *"so far i want visibility and on+off"*. Every automatic behaviour is an entry in `AUTOMATIONS` (`shared/automations.ts`) naming the settings SEGMENT whose page carries its row, and every entry answers `switchable`. A `false` carries `helpText`, **a reason a person READS on the page**, under the place the switch would have been — because a row that is visible and inert with no explanation is the control-that-looks-like-it-works this repo has already shipped four times (R36's fifteen dead permission boxes, `screens`' four rights and no door, two purged modules still holding rows, R61's gear that renders `null` for ever). **And that row wears the dictionary's mark beside the reason** — her ruling of the same day, shown the list: *"ah ok, i like it. like we have protected choices to have protected automations! Still have the visibility, but cannot change it"*. It is `GLOSSARY.protectedChoice`'s own word, drawn with the same kit part the CHOICES half of that page draws it with, and the mark and the reason render from ONE guard so the badge can never be left standing alone. That is not tidiness: the word promises something DIFFERENT on each half — a choice's protection comes off in one click, an automation's never comes off, at the door as well as on the screen — so the badge alone would teach a reader the Choices sentence and let them carry it to a row where it is false. Word, part and guard are DERIVED off the two screens' own source, so a reworded term or a restyled Choices badge turns the automations half red rather than letting one page grow two marks for one concept. **Storage copies `screens` and departs from it once.** Keyed by segment, opaque JSON, two audit blocks, no `deactivated_at`, a byte cap AND a row cap enforced insert-only, a bounded ordered read, gated `teams:update`, `refusePortalCaller`, `publishChange`, `logActivity`, a reasoned `TOOLLESS_DOORS` line — and the one departure is that the registry is SHARED, so the write door refuses a key naming no switchable automation, which the recipe store cannot do because the web app owns its shape. **An absent row IS the default and the default is ON**, so nothing migrates and every team behaves tomorrow as it does today; and **"off" is a VALUE** (the word, compared positionally) while absence is nobody having been asked — `OPS_DIGEST_OFF`'s discipline, so a deliberate silence and a forgotten config can never look alike. **Four censuses, all derived, all failing both ways:** every branded send in `workers/` has a row (through `shared/rules/email-sites.ts`, R30's own derivation, extracted so two laws cannot come to disagree about what an email is); every `triggers.crons` entry in every `workers/*/wrangler.jsonc` has a row and every row naming a schedule names a real one; every exported function of `AUTOMATION_OWN_FILES` — the files whose whole job is doing things by themselves — is claimed or excused in writing; and **every switch is consulted**, `switchable: true` matched against every `automationOff("…")` read in worker source, both ways, because an offered switch nothing reads is the one failure a person cannot see. Every `source` must still name code that exists; every segment must be one `MODULE_SETTINGS` declares, which R61 holds to a real `MODULE_PERMISSION` key. *Earned by:* thirty-three automatic behaviours running in this product with a person able to see two of them. Three ticket status flips, a stage trail, an edit lock, a nightly deletion, a fifteen-minute sweep of everybody's Google account and fourteen kinds of email, all invisible. The census is the deliverable rather than the switches, because the thirty-fourth, written next year, would ship invisible exactly as these did. **The honest gap is written down rather than papered over:** a new silent state-change inside a file that also does ordinary work passes through no seam and appears in no configuration, and `AUTOMATION_OWN_FILES` is the strongest thing available to it — it catches the next flip, because the next flip goes where its three siblings are. The first run found a live bug in the sibling its storage discipline is copied from: `ALERT_TO` is the word `"off"` in both environments, `sendOpsDigest` understands it, and `alertNewAlarms` beside it only checked for an EMPTY list — so the word survived the split as a one-element recipient list, the growth alarm tried to email an address called `off` every night, and the `if (!mailed) throw` at the foot of that function turned a normal night into a recorded cron failure on staging and production alike. | `automations-are-visible` (`web/test/automations.test.ts`) | enforced |
| R72 | ui | **No subtitle under a heading, unless she asked.** The client's ruling, 2026-09-14, over Settings › Modules' own intro sentence: *"In settings, modules: delete this. Generally, I don't like subtitles, so stop putting them unless I ask."* The wider sentence is what this law enforces — she had already said the narrower version twice the same week about two other screens (*"in ticket settings (or any other module) no subtilte"*, 10 Sep; *"ticket types should be … without subtitle, make this. always"*, 11 Sep), and both landed as one-screen fixes (`shared/web/settings-section.tsx` deleted the field outright, and `MODULE_SETTINGS` lost its `description` column with it). This is the third saying, about a fourth screen neither fix touched, and it is a DEFAULT for the whole app rather than one more screen repair. **A subtitle is, precisely:** a prose element (`<p>`, `<span>`, `<small>`, `<em>`, `<strong>` — R67's own `READABLE_PROSE` set, reused for the same reason R67 reused it: a real kit component is always Capitalised, so `<Text>`, `<CollectionEmptyState>`, `<NothingYet>` and every other genuine-content component are invisible to a lowercase-tag census by construction) standing as the immediate next SIGNIFICANT sibling of a heading (`<h1>`-`<h4>`, the kit's `<Headline>`) inside the same JSX children array — blank text and a `{/* comment */}` are transparent to the pair, the same move R67's walk makes. **Three shapes are deliberately not a subtitle:** a form field's helper text (rendered through the kit's `Field`, which has no heading sibling — a label, not a title); an empty state's explanation (`CollectionEmptyState`/`PortalEmpty`/`NothingYet`/`ShapeStateBody`, Capitalised, already outside the census); and the reason a switched-off automation cannot be turned on, which R70 *requires* as `helpText` and which answers "why can I not change this" rather than "what is this section for". **A real blind spot, written down:** a heading a CHOKEPOINT COMPONENT draws for its caller (`SettingsSection`, `ToolbarRow`'s `title`, `CollectionHeading`) is invisible to the sibling census if a caller passes prose as that component's `children` — heading and prose then sit in two different JSX children arrays. Those three are held shut the narrower way instead: none may re-grow a prop shaped like a subtitle (`subtitle`/`description`/`subheading`/`caption`, matched as a declared TYPE member so a comment merely discussing the word does not trip it) — `shared/web/settings-section.tsx`'s own header states the argument this check imports: "a section cannot declare a subtitle it has nowhere to put." **Why a new law rather than an R67 amendment:** R67's subject is WHERE a titled section's content stands; a sentence inside the title block is explicitly exempt from R67 (amendment 4's `carriesHeading` skip), because R67 has nothing to say about whether the sentence should exist, only about the ground it stands on if it does — a boxed subtitle passes R67 outright and fails this one, and R67's own header makes the identical point from the other side about the module settings pages it cannot reach: "reaching them means judging a component by the PROPS it is handed rather than the JSX it writes, which is a different check with a different oracle." `SUBTITLE_OK` (`shared/rules/registry.ts`, filed beside R67's own `UNCONTAINED_SECTION_OK` with a coordination note for the lane amending R67's tab census) is the way out, file-keyed and rot-checked both ways so the list can only shrink — the same house pattern `HAND_ROLLED_OK` (the kit-motion check) already uses. **Amendment 1 (2026-09-14), the same day, over the Modules card itself:** the founding case passed this census on the day it was written — the card drew its name through the kit's `CardTitle`, which `HEADING` did not recognise, so the pair with the `<span>` beneath it never formed; the miss was on the HEADING side, not the prose side (the prose was a bare `<span>`, already matched). `HEADING` gains `CardTitle` and `PROSE` gains `CardDescription`, both on the kit's own word for what each is (`shared/ui/components/card/card.tsx` calls one "a heading" and the other "prose") — checked by hand against every call site of either in the whole app: the Modules card (fixed outright, this law's own founding case), `members-gallery.tsx` (structurally outside the pair — its `CardTitle` sits inside its own wrapper `<span>` paired with a `Badge`, so the email line beneath is a sibling of the wrapper, never of `CardTitle`), and `screen-renderer.tsx`'s generic `display: "cards"` branch (a real pair, `SUBTITLE_OK`'d as unreached dead code — no recipe in the app reaches that branch today — rather than ruled on as a live subtitle). | `no-default-subtitles` (`web/test/no-default-subtitles.test.ts`) | enforced |

| R73 | arch | **A deny-list is data in the registry, never a const in the test that reads it.** This repo's own opening sentence, line 13 above: "Deny-lists (the reviewed exceptions for each law) live as DATA in the registry, so every exception is a visible, conscious line." A census off `web/test/**` and `web-portal/test/**` on 14 Sep 2026 found twenty exemption-shaped lists declared INSIDE the test files that read them instead — some at module scope, some nested inside a `describe`/`it` block. Moved, each keeping its exact shape, entries and reasons; each test now imports its list from `shared/rules/registry.ts`. *What counts as one of these*, derived off the census rather than guessed at: a `const` whose name ends `_OK` or `_EXEMPT`, or is one of seven irregular names this base already uses for the same shape (`GONE_ON_PURPOSE`, `DEFERRED_UPLOAD_FORMS`, `PARKED`, `MAY_INJECT`, `BY_HAND`, `NOT_A_WORK_PICKER`, `NO_RECORD_BEHIND_IT`). A test file may reference any of those names only by IMPORTING it from `@shared/rules/registry` — a local `const` matching the shape is the fault, empty object included, because a real import statement declares no `const` at all. Proven by mutation: a throwaway `const FOO_OK = {}` dropped into any test file must turn this red, naming the file, before it is removed again. | `registry-backed-exemptions` | enforced |

| R75 | ui | **The options a person picks from are A→Z, in their own language — everywhere.** The client's ruling, 2026-09-14, over Settings › Automations' own Module filter: *"In settings, automations, make sure that in the sort component, in the modules component, you sort it A to Z. This here, but everywhere in the app, make it a law."* A CHOICE a control offers — a filter facet's options, a `<Select>`'s items, a picker's list — is not a collection row (those already carry their own sort control, R53); it is presented alphabetically, comparing the LABEL a person reads, locale-aware (`localeCompare(lang)`, the app's CURRENT language from `useLanguage()`, never the browser's). **ONE SEAM CARRIES THE FIRST HALF FOR FREE.** Every `FilterFacet` in the app — declared options or derived from the rows — is rendered through `shared/web/screen-engine/filter-bar.tsx`'s own `optionsFor`, the one render chokepoint every facet on both front doors passes through, so wrapping its result once there (`sortedOptions(optionsFor(f), lang)`) sorts the client's own named example — Settings › Automations' Module and Status filters, and Settings › Choices' Module and Status filters beside it — and every other facet, present and future, with nothing to change at any `filterFacets` declaration site. **THE SECOND HALF has no single chokepoint**, so `sortedOptions()` (`shared/web/sorted-options.ts`) is called AT THE CONSUMPTION POINT, positionally: the array immediately feeding a hand-rolled `<Select>`/`SelectItem` `.map()` or a picker's `options` prop must itself be a `sortedOptions(...)` call (`sortedOptions(...).filter(...)` also counts), or the file is named in `ORDERED_OPTIONS_OK` with the field it excuses. Fixed this way: the Choices create sheet's Module and Group selects, the account-manager member picker, ten account/app pickers built off the shared `PickableRecord`/`accountOption` seam (knowledge, meetings, tasks, waves, apps, Google sources), a process step's role and tool pickers, and the portal's raise-a-ticket module picker (grouped by app, both levels sorted). **ONE NAMED BLIND SPOT**, read directly rather than derived: `web/components/team/roles-matrix.tsx`'s own module rows (`moduleColumns`) build a kit-specific row config rather than a `<SelectItem>`, so the derived census cannot see it — asserted by name, the same move R74 makes for Home's own import tile. **THE ONE ESCAPE HATCH IS DATA:** `ORDERED_OPTIONS_OK` names a control whose list is not a naming vocabulary at all — a status PIPELINE, a size SCALE (`shared/web/scale-section.tsx`'s own Compact→Regular→Large, the client's literal example of the shape that must NOT sort), a step's place in a workflow somebody actually designed (`step-form-dialog.tsx`'s process-step pickers, and its own frequency scale day→week→month→year) — reasoned per entry, rot-checked both ways so the list can only shrink. **A SORT-BY MENU IS OUTSIDE THIS LAW'S SUBJECT, NOT A REGISTRY EXEMPTION FROM IT:** `CollectionConfig.sortOptions`/`COLLECTION_SORTS` (WHICH FIELD to sort a collection BY — "Newest first", "Priority order") already carries a reasoned, dated design in `web/lib/collection-sorts.ts`'s own header (dates newest-first because landing on oldest-first reads as broken; tickets' drag-rank first, SCOPE ch.07's own priority order) that a blanket alphabetical pass would silently overwrite — and the concrete screen behind her ruling draws no `SortControl` at all (`sortable: false`; the table's own column headers are the sort control), so the ruling cannot have been about that menu. | `alphabetical-options` (`web/test/alphabetical-options.test.ts`) | enforced |

| R74 | ui | **Import opens its own tab — it never redirects the one you were already in.** The client's ruling, testing Import, 2026-09-14, verbatim: "make sure that it opens as a new solo tab on the breadcrumbs, because now it redirects. In the places where we have import, make sure that's what it does." Every "Import CSV" door, and the generic wizard link on Home, used to `softNavigate`/`go` the CURRENT workspace tab straight to `/t/<teamId>/import…`, so the collection she pressed it from vanished from the strip until she clicked Back — indistinguishable from a redirect. `openInNewTab` (`web/lib/nav.ts`) is the one seam: a SOLO tab is a trail of one, which `visitTrail` (`web/lib/workspace-tabs.ts`) already generalises to on its own terms, so this calls the model's existing single mutator with a one-entry trail instead of the crumb-derived one the shell builds for an ordinary navigation, then navigates. **Checked as a census off the disk, positional like R20:** every call to `go(`/`softNavigate(`/`openInNewTab(` whose argument is a template literal targeting the import wizard must be the `openInNewTab(` one — the seam call is what the dispatch must sit inside. `IMPORT_TAB_EXEMPT` is the reasoned, rot-checked way out (empty today), and Home's own tile — reached through a shared `onItemClick` rather than a direct call, invisible to the positional census — carries its own named clause reading the file for the same guard. *Why the seam alone was not enough:* `buildCrumbs` (`web/components/deep-link/crumbs.ts`) built its one crumb for a leaf module with no `RECORD_FACE` entry (import's "id" is a TARGET KEY, never a fetchable row) as an ANCESTOR link back to the bare collection, so the workspace-tab store recorded the WRONG address for the screen actually on screen and the strip could never match it against `currentPath` — the redirect she saw. Fixed alongside the seam, plus `IMPORT_TARGET_LABEL` (same file) naming the nav's own word per target rather than guessing one from the target key, because `brand`'s own `module` field is `brand_assets` and `purposes`'s is `delivery` (R34). | `import-opens-a-tab` (`web/test/import-opens-a-tab.test.ts`) | enforced |
| R76 | arch | **Protected means active — there is no such state as "active, protected".** The client's ruling, 14 Sep 2026, over Choices' own status column: "if it's protected, it's always active, so you don't need to put active protected, just protected." `is_default` (the column; the word a person reads is "Protected") and `deactivated_at` are not two independent flags a reader reconciles by hand: a protected row IS an active row, always, in both directions. `setSelectableActive` already refused to deactivate a protected, active value (409 `default_value`); the gap was the other order — deactivate a value first, while it is not yet protected, then protect it, and the row ended up protected AND inactive with neither door ever having refused either half. `setSelectableDefault` (`workers/tenancy/src/lib/selectable.ts`) closes it: protecting a value now reactivates it in the same call, on the same idempotent UPDATE R17 already asks for, rather than a second refusal a caller has to route around. Migration 0088 backfills every row the old two-step gap could already have produced, and both screens that display the state (`selectable-screen.tsx`, `settings-choices-panel.tsx`) render one word, `Protected`, mutually exclusive against `Active`/`Inactive`, never a second facet. | `protected-is-active` (`workers/tenancy/test/selectable-protected-active.test.ts`) | enforced |
| R77 | ui | **A screen's own tab strip stays on top when the screen scrolls — not only a collection's.** The client's ruling, 2026-09-15, over Settings: "When I scroll down in settings, the tabs do not stay pinned at the top. Make sure you fix this here and everywhere else. Should be the same behavior when scrolling down: the tab should stay visible." R63 already pinned a collection's own tab strip (`STICKY_FOLDER_TABS`, through `renderFolderTabs`) and a record's own inner strip (`STICKY_TABS`, record-chrome.tsx); this closes the third shape her words name — a MAIN screen drawing a bare `<TabsView>` with its own `renderPanel`, asking neither seam for the class. `settings-screen.tsx` was exactly that, so its strip scrolled away with everything under it. THE FIX IS THE SPLIT EVERY COLLECTION SCREEN ALREADY DRAWS, never a third class: the strip renders through `renderFolderTabs` as a SIBLING of its panel, never a `TabsContent` inside the same sticky `<Tabs>` root — `STICKY_FOLDER_TABS` (unlike `STICKY_TABS`) is not scoped to `[role=tablist]` alone, so a root that also wraps the panel would pin the content along with the strip. A static census off the disk, `tab-strips-pin`, holds every `<TabsView` mount in `web/` and `web-portal/` to the two doors R63's own vocabulary already offers: reached through `renderFolderTabs`, or a `className` carrying `STICKY_FOLDER_TABS`/`STICKY_TABS` literally. `TAB_STRIP_PIN_EXEMPT` is the reasoned, rot-checked way out for a `<TabsView>` that is not a screen's own labelling strip at all — a nested view switch inside a panel already pinned, a strip that navigates instead of labelling a panel, or (named honestly rather than hidden) a known gap two other main screens still carry. | `tab-strips-pin` (`web/test/tab-strips-pin.test.ts`) | enforced |
| R78 | ui | **Calendar views carry no sort.** The client's ruling, 2026-09-15, over the week view design: "Never put the sort in calendar components. Make this a law. Makes no sense." A reader looking at a month grid, a week board (`RecordWeek`, Mon–Fri plus the folded weekend) or a day-by-day agenda is not choosing an ORDER — the calendar's own date axis already fixes one — so a sort control beside it offers a choice that does nothing, the identical "dead UI" argument `TOOLBAR_SORT_EXEMPT` (R53) already makes screen by screen for a month grid or a queue. This turns that argument into a law rather than an exemption every future calendar-shaped screen has to remember to add: `<ToolbarRow>` (`web/components/deep-link/screen-bits.tsx`) draws no `<SortControl>` at all, regardless of what its `sort` prop is given, the moment its `view` slot's ACTIVE value is `"calendar"`, `"week"`, `"agenda"` or `"timeline"` (`NO_SORT_VIEW_VALUES`, the same file) — enforced centrally, at the one seam R53 already made the owner of both controls, so no call site can opt back in by continuing to pass `sort`. `NO_SORT_VIEW_EXEMPT` is the reviewed, rot-checked way out for another `TOOLBAR_CONTROL_OWNERS` file that independently builds its own sort/view pair and cannot honestly suppress it the same way. **Amended 2026-09-15, the same day, for the Waves T3 ruling** ("for waves i choose t3"): `"timeline"` joined the original three — a time axis is time-ordered the same way a date axis is — and `web/components/work/wave-finder.tsx` (already a `TOOLBAR_CONTROL_OWNERS` entry) reads the widened set to suppress its own sort control on Timeline and Calendar, keeping sort on List. | `no-sort-in-calendar-views` (`web/test/no-sort-in-calendar-views.test.tsx`) | enforced |
| R79 | ui | **Staff is picked from a pill row, never a dropdown.** The client's ruling, 15 Sep 2026, verbatim: "On Add Task and generally absolutely everywhere where we are selecting staff, do the horizontal choices, not the dropdown. By default, in all of these where I'm selecting staff, always put the user preselected by default." One component, `shared/web/staff-pill-picker.tsx`'s `StaffPillPicker`, on the same pill idiom `AppearancePillGroup` already drew for Settings › Appearance: a bare `<button role="…">` row, `rounded-pill` (R31), an inset hairline never a CSS border, the selected pill's own hairline strengthening rather than a second colour (R32). `role="radiogroup"`/`role="radio"` for a single pick, `role="group"` + `aria-pressed` for many, A→Z by name (R75's own seam), each pill wearing the person's `RecordMark` (R35, round) and their first name alone. A "Nobody" pill only where the field is genuinely optional. The second half of her ruling — the signed-in user preselected on a NEW record — is wired at each call site (`TaskFormDialog`'s pre-existing `defaultAssigneeId`, and the same shape added to `StoryFormDialog`, `AccountFormDialog` and `AppFormDialog`); an edit form keeps the stored value. A static census off the disk, `staff-pill-row`, holds every `<Select` and every `<RecordPicker` not carrying `layout="row"` (its own chip-row layout already draws the identical pill row) to account: none may be fed a staff/member list, found by the same seams `web/lib/members.ts` supplies one through (`useAssignableMembers`/`assignableMembers`/`staffedOn`) or a value typed `PickablePerson[]`. `STAFF_PILL_ROW_EXEMPT` is the reasoned, rot-checked way out, empty on the day this law shipped — every dropdown the inventory found (task/story assignee, the account manager field, an app's staff checklist and lead, a ticket's `HelpStakeholders` add control, `TriageStrip`'s on-duty pick) was converted rather than excused. `triage-queue.tsx`'s two "who is picking this up?" rows are outside this law's population for the same reason the `RecordPicker` carve-out exists: `layout="row"` there already drew the horizontal choices her ruling asks for, on an action row that commits on the click and has no submit step to preselect into. | `staff-pill-row` (`web/test/staff-pill-row.test.ts`) | enforced |
| R80 | ui | **Rows are a list, never a banded table.** The client's ruling, 2026-09-15, verbatim: "On accounts, I want the views to be gallery and list. I don't like this table anywhere, so anywhere in the app where you have it, replace it with list. I don't want to say this again." Earlier the same day, about Tasks: "Just replicate the list component as we have it in tickets. It's already good there." `RecordTable` (`web/components/records/record-table.tsx`) is the one row-collection component every screen but Tickets' own bespoke `TicketRowsTable` draws through, and it drew two shapes: a bare `<Table>` and, by default, that same table wrapped a second time in `overflow-hidden rounded-[var(--radius)] bg-surface-panel` — a grey, rounded, inset band sitting inside whatever card already held the toolbar above it. That second box was the defect the client is naming, not a legitimate variant: every real caller already sits inside ONE surface of its own (a `CollectionCard` from `<PagedFind>`'s `wrap`, or the kit's own `useKitPanel` collection panel — ruling J2, "toolbar, rows, pager inside it, one surface"), so the banded shape was always a doubly-nested, inset, narrower-reading table standing beside Tickets' flush, full-width one. `RecordTable` draws the bare shape unconditionally now — the branch that produced the band is deleted, not defaulted, and the `frame` prop that used to select between the two accepts only the literal `"bare"` (never `"panel"` again), so no call site can even ask for the old look at compile time. A static census, `rows-are-a-list`, reads `record-table.tsx` off disk and fails if the banded fill ever reappears there, and separately walks every `<RecordTable` mount across `web/` for a `frame` prop carrying anything other than the literal `"bare"`. The word moves with the shape: a view switch that used to offer "Table" offers "List" instead, with the kit's list glyph beside it (`ListBullets`, the same icon Tickets' own view switch already draws). | `rows-are-a-list` (`web/test/rows-are-a-list.test.ts`) | enforced |
| R81 | ui | **A form carries no hints.** The client's ruling, 16 Sep 2026, verbatim: "You put too many explanations and hints that are not necessary, especially on the forms, on the create and edit. Please, can you delete all of that? I will give you a few examples, but I want you to clean it everywhere. If we need hints, I will tell you explicitly, but by default, there are no explanations, just the choice, text, or the form components." Two examples, both `FieldConfig.helpText` sentences: "The system this work is on. Everything below is narrowed by it." (the story form's App field) and "A recording, a page, a document somebody can open." (the story form's and the review dialog's file field). A create/edit form shows the label and the control, nothing else. TWO CENSUSES, over the same parsed walk `field-config-keys.test.ts` and R33's `wrapped-strings.test.ts` already stand on (`appFiles()`, scripts/lib/i18n-source.mjs): (1) no object literal spreading `...defaultFieldConfig` may set `helpText` to anything but the empty string; (2) no bare `<p>` in a file that renders a form (imports `FormShell`/`FormShellDialog`, R4's own marker) whose entire content is one static `{t("…")}` sentence of three words or more, coloured `text-muted-foreground`. What survives, on purpose: a validation/refusal message (`text-warning`/`text-destructive`, never `text-muted-foreground`); a placeholder that is the field's own example value; a picker option's own differentiating description (`Choice`'s `description` prop); and a field showing the record's own settled value where a control would otherwise be — the bare-`<p>` census's own "exactly one static sentence, nothing else interpolated" shape excludes every one of those by construction. `Text`/`helpText` on an AUTOMATIONS catalog row (R70, `automation-edit-sheet.tsx`) is a different fact entirely — why a protected row cannot be switched, not a field's help text — and sits outside both censuses. `FORM_HINT_OK` (`shared/rules/registry.ts`) is the reasoned, rot-checked way out for the one case her own ruling names as a real exception: a hint that carries something the user cannot know otherwise (an irreversible action's consequence) belongs in the CONFIRM dialog that asks about it, never the create/edit form beside it. | `form-carries-no-hints` (`web/test/form-hints.test.ts`) | enforced |
| R82 | ui | **A table row holds at most six columns — the seventh goes somewhere else, never squeezed onto the end.** The client's ruling, 16 Sep 2026, over the Waves List view: "the right side of the container in waves is shape wrong." UI-RULEBOOK N1 already named the ceiling ("at most … six in a table row … it does not get squeezed onto the end") and Tickets' own `TICKET_COLUMNS_DEFAULT` stays at four (five on Closed) by deliberate, dated design — but the ceiling was prose, not a check. `waveListColumns` (`web/components/work/waves-screen.tsx`) broke it silently: the same commit that gave the T3 timeline's left column the wave's own App also gave List a seventh column for it, on a collection already at the six-column ceiling. A seventh column does not overflow the frame — the kit's own `<Table>` self-scrolls inside its own container (R39) — it SQUEEZES: every column loses width, most visibly at the row's own right end, where Start/End/Status crowd together against the card's own inset. The fix is N1's own prescription: the App fact rides the Account cell's own second line, the identical primary-plus-muted-subline shape `record-timeline.tsx`'s own `TimelineRow.sublabel` already draws one column along, never a column of its own. A static census reads every literal array in `web/`, `web-portal/` and `shared/web/` whose elements ALL carry both a `key` and a `label` property (the shape `TableColumn` needs of both), and fails past six entries unless the array's enclosing function is named in `TABLE_COLUMN_BUDGET_EXEMPT` with the real reason. Out of reach by construction, stated rather than exempted: a recipe-driven column list built by `.map()` over a config array (`tasks-screen.tsx`, `stories-screen.tsx`, `contacts-screen.tsx`'s `contactColumnHeaders`) is not a literal array of object literals at all — its own ceiling is the recipe's `fields` array. | `table-column-budget` (`web/test/rules.test.ts`) | enforced |
| R83 | ui | **A tab strip and what it labels share one gapless column — the strip pays the whole distance, a caller never pays it twice.** The client's ruling, 16 Sep 2026: "reduce the spacing above ALL TOOLBARS. i want it exactly as its currently below, make it like that above." `<ToolbarRow>` already pays its own trailing gap below itself, once, as `mb-[var(--toolbar-content-gap)]` (R49) — `--toolbar-content-gap`, `--space-5`. The ABOVE number is the identical value, `--tab-content-gap` (`web/app/globals.css`: "one value, not a new one … the same '--space-5' both already spend"), paid by the tab strip above the toolbar as its own trailing `pb-[var(--tab-content-gap)]` (`STICKY_FOLDER_TABS`, `shared/web/screen-engine/tabs-view.tsx`) — so the two tokens were already equal, and every screen but one spent each exactly once: `paged-find.tsx`, `tickets-collection.tsx`, `kwapso-screen.tsx`, `settings-screen.tsx` (twice), `module-settings-screen.tsx` and `screen-bits.tsx`'s own `SectionWithCreate` all wrap a `renderFolderTabs(…)` call and the card/panel it labels in a column carrying NO `gap-*` of its own — `paged-find.tsx`'s own comment states the rule: "this column has nothing to say about it either way and must not grow a `gap-*` of its own — that would be a second opinion about one number." `waves-screen.tsx` was the one call site that disagreed: its `renderFolderTabs(…)` call and the `<CollectionCard>` beneath it sat directly inside the screen's own outer `flex flex-col gap-6` column, alongside the page heading — a per-screen wrapper spending a second, unrelated 24px on top of the strip's own 20px, above the toolbar and nowhere else. Fixed by giving the strip and its card their own inner `flex w-full flex-col` (no `gap-*`), the shape the other six call sites already draw, with the heading staying in the outer `gap-6` where a real, single gap belongs. A static census fails any `renderFolderTabs(` call whose immediate JSX parent (fragments walked through) carries a `gap-*`/`space-y-*` utility, unless the file is named in `TOOLBAR_LEAD_GAP_EXEMPT` with the real reason. **AMENDED 16 Sep 2026, same day, measured on staging:** "every screen but one spent each exactly once" was true of the wrapper census above and false of the screen — a `<CollectionCard>` whose first child is the toolbar ALSO spends `CardContent`'s own leading `p-4`/`lg:p-[var(--space-7)]` inset on top of the strip's correct `pb-[var(--tab-content-gap)]`. Tasks (1600px) measured 52px above its toolbar against 20px below; Settings › Team › Members and Contacts (1280px) measured 44px above against 20px below. Fixed once, in `web/app/globals.css`: `.pinned-strip + [data-slot="card"]` zeroed both `[data-slot="card-content"]`'s real `padding-top` and the R63 `--pinned-lead` property that reproduces it while a toolbar is pinned (leaving the second unchanged while the first dropped to zero would pull a pinned toolbar past the card's own top edge). **AMENDED A SECOND TIME, 16 Sep 2026 evening, by the client's own reaction to the flush-zero fix:** "I'm not happy about this. It doesn't look good. Can we do an in-between with what it was and what it is now? Also, make sure it's the same on every page. I don't understand how task was 52 and setting and contacts were 44. It should be the fucking same everywhere." **The law now: above a toolbar = `--toolbar-lead-gap` (32px) on every screen; below = 20px** (`--toolbar-content-gap`, untouched). One token, `--toolbar-lead-gap: var(--space-7)` (`web/app/globals.css`, defined beside `--tab-content-gap`/`--toolbar-content-gap`) — 32px is the scale's own exact step (`--space-7`, "p-8 is 32px, which is --space-7 here"; `--space-6` is 24px, a card inset, not 32). The strip still pays `--tab-content-gap` (20px); the card pays only the remainder, `calc(var(--toolbar-lead-gap) - var(--tab-content-gap))` (12px), on both `padding-top` and `--pinned-lead` together. Proved by `web/test/toolbar-lead-gap-card.test.tsx`, which also derives, off the disk, exactly which `renderFolderTabs(` call sites the rule reaches (`screen-bits.tsx`, `paged-find.tsx`, `tickets-collection.tsx`, `waves-screen.tsx`, `settings-screen.tsx`'s outer strip) and confirms the three that correctly draw no card at all. | `toolbar-lead-gap` (`web/test/rules.test.ts`), `toolbar-lead-gap-card` (`web/test/toolbar-lead-gap-card.test.tsx`) | enforced |
| R84 | ui | **Mango lives only in the title component; every button off it is black.** The client's ruling, 16 Sep 2026 evening, verbatim: "Let's revisit the rule of only one mango button per screen. Only mango buttons on the title level. Title means the title component on top. Only those can be mango, the others black." This is a NEW law, not an amendment — no numbered app law ever governed mango's position; the only prior rule was the vendored kit's own §2.5 ("Mango is a brand fill… The kit also rules one mango per view", `shared/ui/docs/RULES.md`), echoed by hand in three code comments (`triage-queue.tsx`'s Undo — "the kit rules one brand fill per view"; `roles-matrix.tsx`'s "New role" — "the kit rules one mango per view… this tab's one mango belongs to Invite"; `members-gallery.tsx`'s "+" — "THE ONE MANGO ON THIS TAB IS THE `+`, AND IT IS HERE"). That rule capped the COUNT (one, anywhere on the view); this one restricts the POSITION (only inside the title), and a title-only mango is still at most one per view, so R84 narrows kit §2.5 rather than contradicting it. THE TITLE COMPONENT, named rather than assumed: on a COLLECTION screen it is `CollectionHeading` (`web/components/records/collection-heading.tsx` and `web-portal/components/collection-heading.tsx` — same name, same shape on both front doors: the `<h1>`/`<h2>` display-m heading plus its own `action` prop, "the screen's own door", deliberately not the toolbar's); on a bespoke RECORD DETAIL screen it is `RecordScreen`'s own `actions` prop (`web/components/records/record-chrome.tsx`, B1: "at most one primary and one secondary button" share the title's own row); on the five recipe-driven details that draw the OTHER path (`team.detail`, `members.detail`, `invites.detail`, `brand.detail`, `purposes.detail`), it is the kit's own `RecordDetail`/`RecordChrome` (`shared/web/screen-engine/screen-renderer.tsx`). THE CHECK reads the source: a static AST census over every `.tsx` file in `web/`, `web-portal/` and `shared/web/` finds every JSX `<Button>` whose `variant` is the mango one — `variant="default"` (the kit's own name for the brand fill: "`.kw-btn--primary` — mango fill, charcoal label. The one brand fill"), stated OR OMITTED, since `default` is `Button`'s own default variant — and walks its ancestors, including through a JSX attribute's own initializer (so a Button handed through `action={…}`/`actions={…}` still counts as inside): nested under `CollectionHeading`, `RecordScreen`, `RecordDetail` or `RecordChrome`, it passes; anywhere else it is an offender unless its enclosing function is named in `MANGO_OUTSIDE_TITLE_OK` with the real reason, rot-checked both ways. A `variant` that is a COMPUTED expression (`action.variant`, `TRIGGER_TONE[tone]`, a ternary) is out of reach by construction, stated rather than exempted — the same posture R82 takes on a recipe-built column list — a rule that reads source cannot resolve what only runs at render. AMENDED 18 Sep 2026, client ruling verbatim, "edit button is never black (even when it's only one)": the edit PEN specifically is quiet everywhere, never `inverse` and never mango even where it is the one button a title draws alone — `web/test/pencil-button-census.test.ts` reads every `<Button>` drawing a `PencilSimple` glyph under `web/components/` for exactly that. *Earned by:* a sweep finding 27 mango buttons outside a title, three of them shared seams worth more than the other twenty-four combined — `FormShell`'s own `SubmitButton` (every one of the ~38 forms R4 routes through it; its own comment called it "the one mango control on the screen"), `screen-bits.tsx`'s `AddButton` (UI-RULEBOOK B14's own words: "the one mango control in a toolbar row", eleven call sites), and `collection-frame.tsx`'s `createActionButton`/`CollectionEmptyState` ("Add the first", composition 27.21's own carve-out) — then twenty individual dialogs, sheets, list rows and forms across both front doors, every one switched to `variant="inverse"`, the kit's own black (charcoal fill, off-beige label — already this app's convention for "the black chip is always the ID"). Two are named in `MANGO_OUTSIDE_TITLE_OK` rather than fixed: `AgentPanel` and `AskTheAssistant` (`web/components/assistant/`), the assistant strip, owned by a different lane and left untouched here on purpose. | `mango-in-title-only`, `pencil-button-census` (`web/test/mango-title-only.test.ts`, `web/test/pencil-button-census.test.ts`) | enforced |
| R86 | ui | **In any collection, the one coloured chip is the record's status.** The client's ruling, 17 Sep 2026, verbatim: "I have changed my mind regarding chips. In a database where there are different columns, the one that gets the chip with the color is always the status. This means that for tickets, we need to find icons for the ticket type and assign colors to the status." A list row, a board card or a record's own head chip row may colour exactly ONE categorical field — its STATUS (`shared/status-tones.ts`, `shared/app-stages.ts`, D17) — and every other categorical field draws an ICON or plain text, never a colour. Priority on tasks is the one already-ruled exception (K19a) and is named rather than silently allowed. Tickets are the worked example: `ticketTypeColour` (`web/lib/type-colours.ts`) drew a coloured dot for a ticket's TYPE on the list row, the board card, `TicketChips`, both type pickers and the portal's own row since 2026-09-06 — retired everywhere a CHIP reads it, replaced by `ticketTypeIconName` (`shared/ticket-types.ts`), the identical closed-map pattern `storyTypeIconName` already stands for story type (K26): Issue → `Bug`, Question → `Question`, Extra → `PlusCircle`, Feedback → `ChatCircleText`. `ticketTypeColour` is NOT deleted — the tickets dashboard's own chart series is the one reader left, an aggregate view's series colour being a different domain from a record's own chip. CHECKED as a source census over `shape.tsx` and the ticket collection/detail components (plus `tasks-screen.tsx`, home of the named exception): every `Badge`/`Swatch` colour is resolved through a few local `const` hops to what it actually names, passing only when that names status, stage or waiting, or the file+function is named in `COLOURED_CHIP_OK`. | `status-owns-the-chip` (`web/test/status-owns-the-chip.test.ts`) | enforced |
| R85 | ui | **Every rail destination is named in one word.** The client's ruling, 17 Sep 2026, verbatim: "Make it a rule that in the navigation bar, we only have one-word names. For example, 'Knowledge Base': reduce it to 'Knowledge'. We need an alternative for work logs. Propose me multiple." A DESTINATION is a link a person can click to land somewhere — every `NAV` entry that carries a real `group` (not `"none"`) and is not `inRail: false`, and every `TEAM_SECTIONS` row with `placement: "sidebar"` (`web/lib/pages.ts`) — the same two lists `app-shell.tsx`'s own `universal`/`sidebarPages` read to draw the rail, so the check asks the SAME source the rail does. Its `title`, in English, must be exactly one word: no space, no hyphen. The rail's THREE GROUP HEADINGS (`NAV_GROUP_LABELS`: "My work", "Build", "Accounts") title a SECTION, never a place a click lands, and the client's ruling and open question were both about a destination — so all three sat in `RAIL_LABEL_WORDS_OK`, reason "groups are headings, not destinations; awaiting her word", rather than being silently measured against a rule that was never asked about them. ANSWERED THE SAME DAY: the client's ruling, 17 Sep 2026, verbatim: "No, the rail group heading can have two words." The three group headings are exempt from the one-word count by ruling now, not merely awaiting one. RED THE DAY THIS LAW WAS WRITTEN: "Knowledge base" (the `knowledge` sidebar entry) and "Work logs" (the `time` entry) both carried two words. "Knowledge base" → "Knowledge" everywhere it is a user-facing label (R6/R34 — the glossary term, every `t("Knowledge base")` call site, the translations); the route (`/knowledge`) and every identifier are unchanged. "Work logs" had no client pick at first — she asked to be shown alternatives rather than have one chosen silently for her, and "Hours" shipped as the recommendation while she decided — then she named it, the same day, verbatim: "The word for work logs is logs." The `time` entry now ships "Logs". "Work logs" itself is untouched everywhere else it is said (the glossary term, and every record's own tab), because only the RAIL destination is under this law. | `rail-labels-one-word` (`web/test/rail-labels-one-word.test.ts`) | enforced |
| R87 | ui | **A title fits one line on a MacBook Air, and every title-shaped field is capped there.** The client's ruling, 18 Sep 2026, verbatim: "for all titles (main, details, all) i would like to limit the lnght to what would fit in 1 line in a laptiop. this menas a max charactes for titles in the forms (not sutting it) wdyt? and ow many cahracters would taht be? consider text size regualr and the monitor size of a macbook" — and her pick, shown a side-by-side of three enforcement options: "for title lenght. set this limit considering macbook air, enforce with e3." MEASURED, not guessed: the artifact "Title Length" canvas-measured how many characters of regular-weight text fit on one line, at MacBook Air width (1440×900), in the app's three one-line title steps — the record heading (64), the collection heading (65), the list title cell (57) — and the narrowest, rounded down for a margin, is `TITLE_MAX_CHARS = 50` (`shared/types.ts`), one constant for all three rather than three separate ceilings, because one title moves between all three renderers. "ENFORCE WITH E3" names the THIRD of three options offered: a hard cap in every form (the input's own `maxLength`, so the 51st character never types) PLUS an ellipsis fallback for a record that already exceeded it before the ceiling existed. TWO SEAMS, ONE CONSTANT. Every title FIELD CONFIG — ticket, story, task, meeting, wave, sprint, app, account, and a knowledge source's title, an input's (a to-do's) title — sets `validation.maxLength` to `TITLE_MAX_CHARS` and shows a live "N / 50" counter through the kit `Field`'s own `count`/`countMax` (a number, not a sentence, so it is not caught by R81's ban on hints); the matching WRITE DOOR refuses a longer one through the SAME constant, positionally (R20's own discipline, applied to this one field on each door). Deliberately NOT counted as titles: a dropdown VALUE (`selectable-form-dialog.tsx`'s `optionField`, a "Choice," never a record), a ROLE's name, a PROCESS's or a STEP's name, and a PERSON'S name (`contact-link-dialog.tsx`'s `ContactCreateDialog`, out of the owned form-dialog glob in any case) — a person's name is not a title. And every ONE-LINE TITLE RENDERER — the record head and every recipe-driven detail (`clampRecordHeading`, `shared/web/record-heading.tsx`, now a single-line `truncate` rather than its earlier `line-clamp-2`), the collection heading (`CollectionHeading`), and the list view's own title cell (`RecordTable`'s first column) — truncates with an ellipsis and carries the full string as its `title` attribute, so a record whose title predates the ceiling is never rejected, only ever shown short. AMENDED 18 Sep 2026 (I1, her pick verbatim: "l1"): the fifty-character cap binds what a person TYPES, never a title that arrives already written elsewhere — a knowledge upload's `title` falling back to its own `fileName`, or a title carried in from a Google import (Drive, Gmail, Calendar, Chat) — which is kept WHOLE on write and only ever shortened where a one-line renderer already truncates any other title. AMENDED AGAIN 21 Sep 2026, her verbatim words: "imported knowledge titles are clamped on import at 50, imported ticket and story titles stay whole (I1)." This narrows I1 rather than replacing it: the whole-on-write protection now belongs to a ticket's and a story's own title alone (the `ticket` and `story` kinds in `INGEST_KINDS`, `workers/content/src/lib/knowledge-ingest.ts`). Every OTHER title that lands in the knowledge base's `title` column from outside the typed form: a knowledge upload's file-name fallback, every other mirrored kind (account, contact, app, process, sprint, meeting, todo, task, person, dropdown, portal_login), a Google import (Drive, Gmail, Calendar, Chat), and the glossary's own seeded words, now CLAMPS to `TITLE_MAX_CHARS` on the way in through one shared helper, `clampTitle` (`shared/clamp-title.ts`): the first 49 characters plus a single ellipsis character (U+2026), a trailing space trimmed before it, and the cut moved back to a nearby space (within the last 12 characters) rather than landing mid-word where one exists. The typed form is unmoved: a person-typed title over the cap is still refused, in words, at the same doors. | `title-length` (`web/test/title-length.test.ts`), `knowledge-import-title` (`workers/content/test/knowledge-import-title.test.ts`), `knowledge-mirror-title-clamp` (`workers/content/test/knowledge-mirror-title-clamp.test.ts`), `clamp-title` (`workers/content/test/clamp-title.test.ts`) | enforced |
| R89 | ui | **A record screen's footer sits at the screen's own bottom edge, with the panel gap above it, not merely last inside its own card.** The client's ruling, 18 Sep 2026, verbatim, over the shipped ticket page: "On ticket detail, the footer should be at the very bottom. The position is still fucking wrong. Fix it once and for all." D21 (`footer-is-last`) already proved a `<CardFooter>`/`<ReplyComposer>` is the LAST rendered child of its own enclosing card — DOM order only, nothing about where that card sits on the screen. It was not enough: measured live on staging (T0001, headless Playwright) the conversation card's own footer closed 414px past the visible screen body at 1440×900, because `TicketDetailBody`'s conversation card was sized to the SUM of the three side panels' own content height (an earlier, different ruling), never to the screen's actual available height. **AMENDED, 18 Sep 2026 EVENING — the first construction did not hold, re-proven live against a real ticket with a real conversation.** It claimed `app-shell.tsx`'s own R29 page container was already enough (`min-h-full`, "no edit to that file was needed") — false: `min-height` is only a floor a `height:auto` block GROWS PAST once real content exceeds it, so nothing downstream ever got a real budget; the page container measured 1312px against a 785px pane. Fixed, three places, proved by injecting candidate CSS live before touching source (`${SCRATCH}/footer-fix-proof.json`): (1) `app-shell.tsx`'s page container is `h-full` now (a DEFINITE height), not `min-h-full` — safe for every other screen (nothing here sets `overflow`, proved pixel-identical before/after on the tickets dashboard and an account detail page); (2) `RecordChrome` (the ticket's own head, a sibling of `TicketDetailBody` under `panelVisible={false}`) was ALSO claiming `flex-1 min-h-0`, splitting the column 50/50 instead of taking only its own content height — fixed with `record-chrome.tsx`'s own `HEAD_ONLY` (`flex-none`), read off `panelVisible`; (3) the conversation card's `min-h-[420px]` floor outlived the breakpoint it was written for once the grid had a real, sometimes-sub-420px height to give it — released with `lg:min-h-0`, same pattern as `tickets-dashboard.tsx`'s own stacked floors. `TicketDetailBody`'s own root (`data-slot="ticket-detail-body"`) is that flex column's own `flex-grow` item (`lg:flex-1 lg:min-h-0`), its right column ONE scrollable cell (`flex flex-col gap-6 overflow-y-auto`), and the conversation cell's own `lg:h-full lg:min-h-0` now fills a real, definite remaining height — `CardFooter` sitting flush at the screen body's own bottom inset, at 1440×900/1280×800/1024×768, the side column scrolling independently once taller than the row. The composer's own half — Aurora's screenshot 4, verbatim: "This composer should have a background color that makes it easy to identify, and also it should be full width of its own container" — the `<form>` itself always carried `w-full`; the bug was its PARENT (the div `reply-composer.tsx` hands `CardFooter` as `composer`), shrink-to-fit inside `CardFooter`'s own flex row with no width claim of its own, so the form's `w-full` was 100% of a 271px box. Fixed with `w-full` on that wrapper too, alongside `bg-card`, the same class the kit's own `Input` paints every ordinary text field with. **AMENDED AGAIN, 19 Sep 2026 — the LG fix above never touched below `lg`, and Aurora, verbatim, over the same page: "No, this is still wrong. The footer is currently under the stages and above the content. … I cannot believe you're so stupid and you cannot fix this."** Below `lg` the old grid collapsed to one column and stacked the conversation card (thread AND composer together) FIRST, the three side panels SECOND — so the composer sat right under the stage ladder, with "the content" rendering after it, exactly her sentence; confirmed live at 760×900 (rail collapsed) before this fix: the conversation card closed 434px past the visible screen body, the side panels starting AFTER it. A fact this re-fix settles for good: `lg:` here is a plain viewport media query with no `container-type` upstream of it, so opening the assistant panel (which narrows the CONTENT column, not the viewport) never crosses it — proven live at 1440×900 with the assistant open and at 1024×768, both already-passing LG states, untouched. **THE LAW NOW READS: at every width the composer is the last thing on the screen and sits on its bottom edge.** A first CSS-only attempt (a single tree, `order-*` walking the two columns back to their LG positions below `lg`) broke on the first live proof against a real ticket: the head alone measured ~74% of the available budget at 760×900, and two SEPARATE `flex-1 min-h-0` regions (the side panels and the conversation) crushed the conversation to a real, live, on-screen height of zero the instant there was no surplus to grow into. Fixed instead with a real breakpoint DECISION, not a resized CSS tree: `TicketDetailBody` (`ticket-detail-body.tsx`) now picks its own tree with `useIsAtLeastLg()`, a `useSyncExternalStore`/`matchMedia` hook in the same house shape `web/lib/use-is-phone.ts` already banks for "a phone needs a different tree, not a resized one." At `lg` the tree is byte-identical to the first landing above. Below `lg` it is `flex flex-col`, filling the SAME `flex-1 min-h-0` budget the LG grid already claimed; ONE scrolling region (never two competing ones) holds the side panels FIRST and the conversation's bare THREAD (no footer inside it) second; and the composer — the kit's own `<CardFooter>`, the identical element `TicketConversationPanel` uses at `lg`, never duplicated — is this flex column's own `flex-none` LAST child, carrying its own `bg-surface-panel` fill since it no longer stands inside a `Card`. D21 holds either way: the `<CardFooter>` is the last real child of whichever tree renders. **AMENDED A FOURTH TIME, 19 Sep 2026 (Round 22b) — "bottom edge" names the card, not a gap near it.** Aurora, over a screenshot at 1784×981 (rail collapsed): "look at screenshot! that's the footer not being on the very bottom! fix this at once." Measured live before this fix (`${SCRATCH}/r22-footer-dump-out.json`): the conversation card's own `CardFooter` closed at y=941 while `[data-slot="screen-shell-body"]` — the real scroller, the one box a footer's bottom edge actually answers to — closed at y=965, a further 24px of nothing under an already-correct 32px card inset (`CardFooter`'s own `lg:pb-[var(--space-7)]`). Every earlier landing measured the wrong reference: "the screen body's own bottom" was read as wherever the flex chain's own `h-full` box happened to stop, never against the vendored pane it sits inside — `h-full` floors that div to `screen-shell-body`'s CONTENT-box height, real and definite as the earlier fix intended, but a content box by definition excludes the pane's own `padding-bottom` (`DENSITY_BODY`, screen-shell.tsx, `py-[var(--space-5)] lg:py-[var(--space-6)]`, this shell's default density) — 20/24px reserved as blank margin below every screen's content, correct everywhere else, and exactly the "page inset" this law now forbids under a footer that is supposed to be flush. **THE LAW NOW READS, PRECISELY: at every width the conversation card's own bottom edge coincides with the screen body's own bottom edge — no page inset under it — and the composer sits at the card's own inner inset from that edge, nothing more.** `shared/ui/` cannot be hand-edited to drop that padding for one screen (R39), so `app-shell.tsx`'s own page container grows INTO it instead, only where a ticket body is actually present: `has-[[data-slot=ticket-detail-body]]:h-[calc(100%+var(--space-5))]` / `lg:has-[[data-slot=ticket-detail-body]]:h-[calc(100%+var(--space-6))]` — the pane's real, full border-box height, to the pixel, proved live before touching source (`${SCRATCH}/r22-footer-inject-out.json`): container bottom 965.00 against `screen-shell-body` bottom 965.00 at 1784×981, 884.00/884.00 at 1440×900, `/accounts` and the tickets dashboard pixel-identical with and without the rule. The below-`lg` pinned `<CardFooter>` needed no separate fix: `data-slot="ticket-detail-body"` marks BOTH of `TicketDetailBody`'s own trees, so the same rule reaches it and its own `p-4` inset is now the only thing between it and the pane's real bottom edge. **AMENDED A FIFTH TIME, Round 23, 19 Sep 2026 — Aurora, over a screenshot at 1991×842 with the assistant panel OPEN, a width/height/assistant-state combination none of the four earlier amendments' own proof widths had measured together: "THE PROBLEM IS WHERE THE FOOTER IS!!! SHOULD BE AT THE VERY BOTTOM!"** Every earlier amendment bounded the CONVERSATION CARD's own height and trusted a long `flex-1 min-h-0` chain to resolve correctly on every future width/height/assistant-state combination — the exact shape that kept re-breaking on the next untested one. Round 23 stops chasing the chain: the composer's own `<CardFooter>` is pulled OUTSIDE the scrolling region entirely, ONE instance in ONE DOM position, `flex-none`, common to both the `lg` and below-`lg` trees, and carries `position: sticky` besides — a negative, padding-compensated `bottom` offset (`bottom-[calc(-1*var(--space-5))] lg:bottom-[calc(-1*var(--space-6))]`, never a plain `bottom-0`, which measured a live 24px gap) so its own "stuck" threshold coincides with the pane's real border-box bottom rather than stopping short of it. Proved live at 1991×842, 1440×842 and 1784×981 with the assistant open, including 40 injected thread messages and 18 injected side-panel rows. **THE LAW NOW READS, FINAL FORM (round 23): the composer is pinned at the bottom of the screen at every width and height; everything else scrolls above it.** **AMENDED A SIXTH TIME, ROUND 24, 19 Sep 2026 — THE LAW HAD PINNED THE WRONG ELEMENT.** Aurora, over her own screenshot, verbatim: "the black section, the footer, should be at the very bottom / why is the write text space full width?? rewind here / THE FUKING FOOTERRR!" Every round from the first landing through round 23 read "the footer" as the reply composer. She meant THE BLACK BAND: the kit's own CH27.8 ink footer, the dark card holding LATEST ACTIVITY (the recent rows + "Add a note") and RECORD (Created by / Last edited by) — which had been rendering correctly last inside ITS OWN small region, but in the WRONG PLACE ENTIRELY, right after the stage ladder and before the ticket's own scrolling body, because `RecordScreen`'s one `<RecordDetail>` call draws band/hero/panel/footer in one fixed order and the ticket's body is a SIBLING rendered after it — no prop on that call could ever move it past a sibling. **THE LAW NOW READS, FINAL FORM: the ticket's dark band (Latest activity + Record) is the page's footer — the last element, pinned at the bottom of the screen at every width and height; the conversation and side cards scroll above it; the reply composer sits at the conversation card's own foot, inside the card, at every width.** Fixed by a SECOND call to the kit's own composition rather than a moved prop: `record-chrome.tsx` exports `RecordFooterBand`, `<RecordDetail>` called again with nothing but the footer's own props (no title, no hero, no tabs, `panelVisible={false}`) so its own render draws only the footer card; `RecordScreen`'s own copy is switched off (`footerVisible={false}`) so it never draws twice. `TicketDetailBody` renders `RecordFooterBand` as its own `flex-none` LAST child; `TicketConversationPanel` — retired in round 23 — is UN-retired, exactly its round-22 shape (`git show d1167183`): the composer is `Card`'s own `CardFooter`, `w-full` of the CARD, never the page — "why is the write text space full width??" was exactly this, the composer's `w-full` had been 100% of the page's own pinned band, not the card it used to sit inside. PROVED BY CONSTRUCTION, NOT `sticky` ALONE: live injection this round (40 thread bubbles + 6 activity rows, 1991×842 and 1440×842 with the assistant open) found a sticky band riding on an UNBOUNDED scrolling region rides DOWN with it the instant that region's content outgrows the viewport (`ticketBody.clientHeight` measured 7px, the thread's own `[role="log"]` never actually capping, `scrollHeight === clientHeight`) — fixed by binding every link in the chain (`h-full`/`flex-1 min-h-0` on the grid/stack, the conversation cell, the Card itself, and `min-h-0 overflow-y-auto` on the Card's own `CardContent`, the ONE true scroller) so the region is bounded FIRST and the band's position falls out of that construction; `sticky` with round 23's own negative, padding-compensated offset stays only as belt-and-braces. Proved live at 1800×978, 1991×842 (assistant open), 1440×900, 1280×800 and 760×900 (rail collapsed), plus the 40-bubble/6-row stress at 1991×842 and 1440×842: the band's bottom sits flush with the pane's own bottom edge (0px) at every width `md:` (768px) and above, the thread scrolls inside the card and the page itself never scrolls. Below `md` (760×900), the app's own PRE-EXISTING, unrelated `pb-24 md:pb-0` mobile bottom-chrome reservation on `app-shell.tsx`'s page container (confirmed identical on the untouched round-23 composer before this round) leaves a 96px gap between the band and the pane's raw border box — the band still sits flush with the page's own real CONTENT edge; this is not a regression this round introduced. **AMENDED A SEVENTH TIME, ROUND 26, 19 Sep 2026 — the panel gap above the band.** Aurora, reading the round-24 page back, verbatim: "ok, now the footer is at the bottom, but there's a law about how much margin there must be above!!! add the space!!" The band's own flush bottom edge (round 24) was already correct and stays exactly 0px; what was missing was the ordinary space ABOVE it — the same `gap-6` (24px at the 16px root, `documents/UI-RULEBOOK.md`'s own S1/N7, "between panels on a screen: gap-6") every other pair of stacked panels on this screen already carries. `TicketDetailBody`'s own root (`data-slot="ticket-detail-body"`) — a flex column whose only two children are the scrolling region and the band — now carries `gap-6` itself, so the gap sits in NORMAL FLOW between the two children, by TOKEN rather than a hand-numbered offset, and never touches the band's own `sticky` flush-bottom mechanics. **THE LAW NOW ALSO READS: the band's own top edge carries the standard panel gap (`gap-6`, 24px) above it, exactly the gap between any other two stacked panels on the screen, while its bottom edge stays flush at 0px.** **AMENDED AN EIGHTH TIME, ROUND 27, 19 Sep 2026 — the side column never scrolls.** Aurora, over the live ticket page, verbatim: "there should be no scrolling to see all right column items — expand the height!" and "scroll only on conversation when taller than right column." Round 24's own `lg` grid (`h-full min-h-0` on the grid AND the side column, `overflow-y-auto` on the side column) forced the row to fill the whole scrolling region regardless of content, so a short ticket showed the side column scrolling inside its own little box while the conversation card sat mostly empty beside it — backwards: the side column's three cards are what a person needs to see in full every time, the conversation is what varies wildly. Rebuilt by construction: the grid and the side column both drop their own `h-full`/`min-h-0` (and, on the side column, `overflow-y-auto`) — content-sized now, like any ordinary block, so the row resolves to the side column's own natural height (an explicit-height grid with one `auto` row would otherwise let `align-content`'s own default `stretch` hand that row the FULL container height regardless of content, exactly what forced the old internal scrollbar); the conversation cell drops `h-full` for `relative min-h-0` — no height class of its own, sized only by the grid's own `items-stretch`; and `TicketConversationPanel`'s own `Card` takes a new `fill="absolute"` at `lg` (`position: absolute; inset: 0`) — taken out of flow so the cell contributes zero intrinsic height back to the grid's own auto-track sizing, the one piece that keeps a long thread from dragging the row (and the side column with it) taller. Proved first by live DOM/style injection against T0001 (`${SCRATCH}/row-proof.json`, `${SCRATCH}/row-*.png`) at 1800×978, 1991×842 assistant open, 1440×900 and 1280×800: the side column's own `scrollHeight === clientHeight` (never scrolls) and its bottom exactly matching its last card's bottom at all four; the conversation card's height matching the side column's own height exactly (0px diff) at all four; a 40-bubble stress injection at 1991×842 leaving the row height unchanged (810.89px before and after) with the thread still scrolling inside the card; three injected extra side cards at 1440×900 (side column taller than the screen) making the one scrolling region above the band scroll the whole page while the band stayed flush (0px) with its own round-26 panel gap unchanged (24px) above it. **THE LAW NOW ALSO READS: at lg the row is as tall as the side column, which never scrolls; the conversation matches it and scrolls inside only when taller.** **AMENDED A NINTH TIME, ROUND 28, 19–20 Sep 2026 — one page scroll, no inner scrollbar.** Aurora's dark-theme screenshot showed a SECOND scrollbar inside the ticket page, above the band: this file's own "one scrolling region" wrapper (`flex-1 min-h-0 overflow-y-auto`, rounds 24–27) was a real `overflow:auto` box bounded to whatever the flex chain above it resolved to, so once the grid/stack inside it needed more room than that, the box scrolled its OWN content instead of letting the page grow — every earlier round protected the band's position from exactly this shape and never noticed it had built one at the region's. Her ruling, restated as the standing R91 (`no-nested-scroll`): "never need to scroll to see al content!!! (only exception chat compnents)." Fixed by REMOVING the bound, not moving it: (1) `TicketDetailBody`'s own root drops `min-h-0`, keeping `flex-1` — flexbox's own "automatic minimum size" (the initial value of `min-height` is `auto`, a content-based floor for a flex item along the main axis whenever nothing overrides it to `0`) fills the leftover space after the head when content is short and grows PAST it when content is taller, with no percentage arithmetic and no fight with the head sibling (a literal `min-h-full`, tried first by live injection, FAILED the short-page proof by ~196px — the head's own height — because `min-height:100%` on a flex item is blind to a sibling and floors the root to the WHOLE container on top of the head); (2) the scrolling-region wrapper drops `flex-1`/`min-h-0`/`overflow-y-auto` entirely, content-sized like the side column already was; (3) the band drops `sticky` and the round-23 negative, padding-compensated `bottom` offset for `margin-top:auto` — the ordinary "footer at the bottom of a short page" flex trick; (4) `app-shell.tsx`'s own `has-[[data-slot=ticket-detail-body]]:h-[calc(100%+…)]` growth rule (round 22b) is RETIRED — it existed only to let the sticky band reach past the pane's own reserved bottom padding, and nothing here is sticky any more. Proved first by live DOM/style injection against T0001 on staging, twice (`${SCRATCH}/onescroll-proof.json`, the failed `min-h-full` candidate; `${SCRATCH}/onescroll-proof2.json`, this construction), at 1800×978, 1991×842 assistant open, 1440×900, 1280×800 and 760×900: after injection, the census of every scrollHeight-exceeds-clientHeight element inside the ticket body came back EMPTY at all five widths (before injection it always found the old region wrapper, diffs of 360–1270px); the page's own scroller (`screenBodyScrolls`) carried the load at every width with the thread quiet at rest; the band stayed the root's last child, flush with the screen body's own bottom (within ±0.6px); the conversation card matched the side column's own height exactly at `lg` (F, 760×900, is below `lg` and stacked, unchanged from round 27). 40 injected thread bubbles at 1991×842 (assistant open) left the root's own height unchanged with the ONE remaining scroller being the thread's own `card-content` — R91's named exception, nothing else. Removing the three side cards and blanking the thread at 1440×900 (a genuinely short ticket) landed the band's own bottom EXACTLY at the screen body's own bottom (the `min-h-full` candidate had missed this by ~196px). And against an account record's own detail page, untouched by this construction, the last card's own bottom sat within ~1px of the screen body's own bottom at every width — the ticket page's band now ends exactly where every other record screen's last card already does. | `footer-on-the-edge` (`web/test/footer-on-the-edge.test.ts`) | enforced |
| R88 | ui | **Empty-state single door.** When a section is empty, its empty state's "Add the first" is the ONE way to add, never a second header-drawn add button beside it — and the section's own HEADER (title row) draws nothing either, because the empty state's own heading stands in for it. The client's ruling, 18 Sep 2026, reading the deployed Work logs card back over her own earlier one (R50/R84): "we already said on empty state, we only have the first, not the top-right plus button. This is a law. Reinforce it everywhere. And then also remove the work log header when it's empty." Her screenshot: the ticket page's Work logs card, empty, drew a "Work logs" title with a black top-right `+` AND, in the body, `CollectionEmptyState`'s own "No time logged against this yet. […] Add the first" — two doors on one zero-row collection. R50/R84 already close the BUTTON wherever a title row sits inside a `<ToolbarRow>`; the gap R88 closes is the title row that sits OUTSIDE one — `AddButton`'s own `empty` prop is optional for exactly that shape, and two call sites (`help-detail.tsx`'s Related stories and Work logs panels) had used the escape hatch to argue the opposite, a hardcoded `empty={false}` reasoned in `EMPTY_TOOLBAR_EXEMPT` as "this button should stay reachable at zero rows." That reasoning is retired. `EmptyGatedPanel` (`web/components/deep-link/screen-bits.tsx`) is the one shared shell the law lives in: `empty` true drops the WHOLE header (title, count and action together) and draws only `children`, expected to be the panel's own `CollectionEmptyState` — `children`'s position in the returned tree never moves as `empty` flips, so a child that owns its own "add" dialog is never remounted by the header appearing or disappearing. | `empty-state-single-door` (`web/test/empty-state-single-door.test.ts`) | enforced |
| R90 | ui | **Faces in choices: any choice over people, contacts, accounts or apps shows the same face the lists show.** Aurora, verbatim, about the new Raised-by `Select` on the ticket form and page: "every time there is an avatar, I want to also see it in the choice component, so I also want to see the avatars here." The photograph where a record has one, initials on the record's own tone where it does not, in the OPTIONS and in the TRIGGER's own chosen value. The kit grew the slot first (kwapso-design v1.2.127): `SelectItem` already carried an `image` prop, but it rendered a bare `<img>` with no fallback path, and nothing at all drew a mark in the trigger's own chosen value, because Radix clones `ItemText`'s children into the trigger and an `<img>` cloned there would have been a second, unasked-for drawing in the 44 field. `face={{ src, name, tone, shape }}` on both `SelectItem` and `SelectTrigger`, rendered through one shared `SelectFaceMark` helper over the kit's real `Avatar`/`AvatarImage`/`AvatarFallback` primitive, closes both gaps; the trigger's face is handed in by the call site (it already knows which option is selected — it is what supplies `SelectValue`'s own placeholder), keyed by the face's own identity so a stale `Avatar` load-status left over from a prior selection's photograph can never survive into a new no-photo one. Wired in this app: `help-form-dialog.tsx`'s and `help-stakeholders.tsx`'s "Raised by" contact Selects (options and trigger), and `work-logs-panel.tsx`'s "Logged by" staff filter (options and trigger; initials only — the door behind it carries no `imageUrl`, an open, named gap rather than a silent workaround). SCOPED TO `<Select>` on purpose, and not R35 widened: R35 (`records-carry-their-face`) already holds `RecordPicker`/`PickerOption` to this account structurally, through the type, a completely different mechanism from a prop the kit's `Select` only grew at v1.2.127. A static census, `faces-in-choices` (`web/test/faces-in-choices.test.ts`), holds every `<Select>` to account: detected as a choice over a record by its own options array's field names (`personName`/`contactId`/`memberId`/`accountId`/`avatar`/`photo`/`initials`, read off the block itself or off a pre-built options array's `const` definition elsewhere in the file), every `<SelectItem>` inside an identity-bearing block must carry `face=`. `FACES_IN_CHOICES_EXEMPT` is the reasoned, rot-checked way out, keyed by the source array's own expression rather than a line number — empty on the day this law shipped. | `faces-in-choices` (`web/test/faces-in-choices.test.ts`) | enforced |
| R91 | ui | **No nested scroll: one page scroll only.** Aurora, verbatim, 19 Sep 2026 ~13:30–13:40: "never need to scroll to see all content!!! (only exception chat compnents)" — stated as a standing law to be enforced everywhere. One page scroll only; no scroll area inside a page's content. When a section or component fits its own container, nothing scrolls. When it does not, the section itself becomes a bounded scrolling region with an explicit `overflow-y: auto` / `overflow-x: auto`, never the page scrolling one region and an inner section scrolling another. Asked directly whether the rail and the assistant pane are exceptions, her answer, verbatim: "exceptions like chat,". EXCEPTIONS: chat components (the ticket thread's `CardContent` and `AgentChat`'s own thread, scrolling inside their own card by L31/L33's own shape); the rail's own list of sections (pinned height, scrolls on overflow); the assistant pane (one aside scrolling region); true overlays (dialogs, sheets, popovers, select listboxes, command menus — a modal or a dropdown may scroll internally and holds no relation to the page's own scroll). NOT COVERED: horizontal scroll on tables and boards (`overflow-x-auto`). R91 is a VERTICAL law only; horizontal was never in question. **AMENDED, 21 Sep 2026: the five pending scrollers and the tables/boards question, both settled.** Five sites (the assistant's History tab, the paused-turn confirm list, the tickets dashboard's chart column, the import wizard's rejected-rows preview, and the meeting transcript preview) were shown to her on a decisions page, one recommendation each, keep or flatten; her ruling, verbatim: "confirm", she accepts every recommendation. KEPT, moved from pending to sanctioned in `NO_NESTED_SCROLL_EXEMPT`: the History tab list (`agent-history-tab.tsx`), the dashboard chart column (`tickets-dashboard.tsx`), the import preview (`import-screen.tsx`), and the meeting transcript preview (`meeting-detail.tsx`), each a bounded widget/preview/overlay-shaped region, not the page itself. FLATTENED: the paused-turn confirm list (`agent-panel.tsx`) no longer scrolls on its own; the page scrolls, and a list too long to show at once is capped by a "Show more" door (R88 style) rather than a second moving region beside the thread's own sanctioned scroll. Separately, asked whether a table or a kanban board is a fifth sanctioned shape of its own, her ruling, verbatim: "like everything else", it is not. A table or a board obeys R91 exactly as every other component does, no carve-out: a vertical scroll region inside one is an ordinary offender, checked by a second, narrower census (`findTableBoardOffenders`, same test file) against `TABLE_BOARD_SCROLL_EXEMPT` (`shared/rules/registry.ts`), empty as of this ruling because no table or board in the app currently scrolls on its own. Horizontal is untouched by this ruling. CHECKED as a source census, `no-nested-scroll` (`web/test/no-nested-scroll.test.ts`), walking `web/components` and `shared/web` for every vertical-scroll class or style (`overflow-y-auto`/`overflow-auto`/`overflow-y-scroll`/`overflow-scroll`, and a `max-h-*` paired with an `overflow` class) — every match must be named in `NO_NESTED_SCROLL_EXEMPT` with a reason, keyed by the source expression rather than a line number, rot-checked both ways so the list can only shrink, plus the table/board census above against `TABLE_BOARD_SCROLL_EXEMPT`. | `no-nested-scroll` (`web/test/no-nested-scroll.test.ts`) | enforced |
| R92 | ui | **Main excludes secondary: when a main person is chosen, they disappear from the secondary picker over the same pool.** Aurora, verbatim, 20 Sep 2026: "Generally, always when selecting main/secondary people (staff, contacts, etc.): when I select the main, this person should not be available as secondary. E.g. when I select 'Raised by,' this person should disappear from the 'Keep in the loop' options. Make this law." Her own examples are all two independent pickers over one shared pool: raised by / keep in the loop, assignee / reviewer, account manager / members, owner / stakeholders. `withoutMain()` (`shared/web/without-main.ts`) is the one seam a secondary picker narrows its options through. Scoped away from `app-form-dialog.tsx`'s "Main stakeholder" on purpose — a different shape, not an exception: a field literally named `main<Something>Id` is chosen FROM the secondary set (the main is a member of it, on purpose), never excluded from it, and the census's own field vocabulary leaves the bare `main` prefix out for exactly that reason. CHECKED as a source census, `main-excludes-secondary` (`web/test/main-excludes-secondary.test.ts`): within one file, every single-value main picker (`raisedByContactId`/`assigneeId`/`accountManagerId`/`leadUserId`/`ownerUserId` and prefixed siblings) paired with a `mode="multi"` secondary picker (`loopContactIds`/`stakeholderContactIds`/`reviewerIds`/`memberIds`) drawing from the SAME source array must show an exclusion (`withoutMain(` or an equivalent `.filter()`), or be named in `MAIN_EXCLUDES_SECONDARY_EXEMPT`, keyed by `${file}#${mainField}+${secondaryField}`. | `main-excludes-secondary` (`web/test/main-excludes-secondary.test.ts`) | enforced |
| R93 | ui | **Visual accompanies text: every avatar, icon or colour rides beside its text, everywhere — filters, views, select components — the only exception being an avatar or logo inside a chip.** Aurora, verbatim, 20 Sep 2026: "When selecting a module, also show the module's icon. Make this law: always, if there's a visual (avatar, icon or color), it should always accompany the text everywhere (filters, views, select components…), the only exception being avatars/logos in chips." Extends R90 (`faces-in-choices`) rather than duplicating it: R90 already holds every `<Select>` choosing over a PERSON to its own face; R93 is the sibling for an icon or a colour on an entity that is not a person. Scoped to modules on the day this law shipped — every module array is `AppModule[]` (`shared/types.ts`), carries a real `icon` field (`shared/module-icons.ts`), and the module's own gallery card already draws it. Status and ticket/story-type colour already have their own standing censuses (`status-owns-the-chip.test.ts`, `status-stage-picker-dots.test.ts`) and are read as already satisfying this law. CHECKED as a source census, `visual-accompanies-text` (`web/test/visual-accompanies-text.test.ts`): every `<Select>`/`<RecordPicker>` whose options map a module-named source array must draw the module's icon (`icon=` on every `<SelectItem>`, or `icon:` in a `<RecordPicker>`'s option literal), or be named in `VISUAL_ACCOMPANIES_TEXT_EXEMPT`, keyed by `${file}#${sourceArrayExpression}`. Wired the day this law shipped: the portal's module `<Select>` (`raise-ticket-dialog.tsx`). | `visual-accompanies-text` (`web/test/visual-accompanies-text.test.ts`) | enforced |
| R94 | ui | **Chip order: id, status, type, main parent, secondary parent — always this order, everywhere a record draws a row of chips.** Aurora, verbatim, 20 Sep 2026: "On story detail, the chips in order: id, status, type, app (underlined), sprint (id, underlined). This must always be the order, everywhere, for other things too: 1 id, 2 status, 3 (if) type, 4 main parent, 5 secondary parent." One seam, `orderChips()` (`shared/web/chip-order.ts`): every chip is tagged with which of the five kinds it is, and the seam sorts by that tag rather than the order a caller wrote the JSX in — a kind simply absent from a record is left out, never padded. Wired the day this law shipped: `shared/web/ticket-chips.tsx`'s `TicketChips` (already correct, now structurally guaranteed) and `web/components/accounts/contact-panels.tsx`'s "tickets raised for this contact" row — a real, live defect this law's own census caught, type drawn before status, fixed by routing the pair through `orderChips`. CHECKED as a source census, `chip-order` (`web/test/chip-order.test.ts`), plus `orderChips()`'s own unit tests proving the resolved order: any file building a chip row carrying both an id chip (`<RecordRef`) and a status chip (`variant="status"`/`ticketStatusCell(`) within one reading window must route the construction through `orderChips(`, or be named in `CHIP_ORDER_EXEMPT`, keyed by `${file}#${idChipValueExpression}`. | `chip-order` (`web/test/chip-order.test.ts`) | enforced |
| R95 | ui | **No em dash, anywhere a person reads.** Aurora, verbatim, 20 Sep 2026: "no em dahses - ABOSLUTLEY NOWHERE. In the ui, in the e-mai,s, in the glossary. They are strictly forbidden. make it law." Three surfaces, three censuses, no exemptions table. THE UI: every entry of `shared/i18n-strings.json` and `shared/i18n-seed.ts` (every language a seed entry names), every JSX text node, JSX child expression, `label`-shaped property, `field(...)` label, `toast.*(...)` argument and `t(...)` argument reachable from `web/` or `web-portal/` (`appFiles()`, the same import-closure derivation R28 already uses for "what a person reads"), plus `shared/web/` censused a second time, directly off disk, for the file R28's own UNWALKED_OK gap could drop from that closure. THE E-MAILS: `shared/workers/email-template.ts` and every SEND SITE, derived rather than hand-listed — any `workers/*/src` file that calls `sendBrandedEmail(`/`brandedEmail(`. THE GLOSSARY: `shared/glossary.ts`'s own `term`/`def` fields. U+2013 (en dash) is held to the same standard as U+2014 (em dash) in prose. Code comments, `documents/*.md` prose and test names carry no obligation here; every scan strips comments first (`stripComments`, `shared/rules/strip-comments.mjs`), and the two AST-walked censuses read only the seven positions `visitStrings` reports rather than a file's whole text, so a non-UI string sitting in a file the front doors happen to import (`shared/workers/limits.ts`'s own scale-registry prose, found live the day this law shipped) cannot be mistaken for something a person reads. Two real hits outside the census's own reach were fixed by hand the same day: every ACTIVITY LOG `description` a door writes, and every GuardError/`fail()` message an HTTP door returns. CHECKED, `web/test/no-em-dash.test.ts`: six censuses, each with its own tripwire. | `no-em-dash` (`web/test/no-em-dash.test.ts`) | enforced |
| R96 | ui | **The id chip is black.** Aurora, verbatim: "id pill must always be black! f..e in backlgoits not black", said over the Backlog list, whose standalone ID column drew the record's own reference (`T0001`, `B0001`, `P0001`, `W0001`, ...) as plain text, no chip at all. `shared/web/record-ref.tsx` (`RecordRef`) is the ONE shared id chip register (`<Badge variant="inverse" size="pill" className="shrink-0 tabular-nums">`, the kit's own ink-fill token pair, never a literal colour), and `one-black-chip.test.ts` already forbids anything else building that tone from scratch. What it could not see is the mirror mistake: a record's own reference drawn as a chip in the wrong tone, or not as a chip at all. Two shapes, one law: (1) a bare `{x.ref}` inside a `<Badge>` that is not `variant="inverse"`, which `storyLead()` and `ReviewsQueue()` (`web/components/work/stories-screen.tsx`) both did, and `story-detail.tsx`'s own chip row copied in turn (fixed the same session); (2) a standalone `ID` column (`field("ref", "ID")`, Aurora's own 20 Sep 2026 ruling adding it to Planned/Backlog) whose cell `RecordTable`'s generic renderer draws as bare text. CHECKED, `web/test/id-chip-is-black.test.ts`, a source census over both front doors plus `shared/web/`: every bare reference inside a non-inverse `<Badge>`, and every standalone `field("ref", "ID")` column, must route through `<RecordRef>`, or be named in `ID_CHIP_EXEMPT`, keyed by `{file, contains}`, the offending line's own text, never a line number. `stories-screen.tsx` and `work-panels.tsx` are owned by another lane and were not edited here; their still-open findings are the exemption table's own entries, reported rather than silently fixed. | `id-chip-is-black` (`web/test/id-chip-is-black.test.ts`) | enforced |
| R97 | ui | **A count never gets its own card.** Aurora, verbatim, 21 Sep 2026: "While it is a rule that when it's a count, unless explicitly said, it doesn't deserve its own card. Just by rule, same as related tickets or related stories or stakeholders: just a count next to the title." A number that counts things never earns a card, a tile or a stat box of its own; it sits beside its panel's own title, in the count register the app already builds for exactly the case she named, `web/components/tickets/ticket-detail-body.tsx`'s `TicketSidePanel` (`{title} {count}` on the heading's own line, "Stakeholders 4") and `web/components/records/collection-heading.tsx`'s own badge one level up. Two shapes: (1) the kit's own `<StatGrid>` primitive, a number-and-label card by construction, three call sites today (`pulse.tsx`'s dashboard, `agent-blocks.tsx`'s assistant metric blocks, `work-logs-panel.tsx`'s hours strip); (2) a hand-rolled metrics grid, the same shape by hand, `story-detail.tsx`'s own "Metrics" panel. No explicit exception is named today. CHECKED, `web/test/counts-beside-titles.test.ts`, over `web/components` and `web-portal/components`: every `<StatGrid` call site, and every file repeating the app's own KPI-tile value styling (`font-mono text-sm font-semibold`, two or more times in one file) twice or more, must be named in `COUNT_REGISTER_EXEMPT`. The three dashboard-shaped `<StatGrid>` sites are named there today, reason "pending her word"; the story page's own Metrics panel is a known offender owned by another lane's own brief, reported rather than fixed here. | `counts-beside-titles` (`web/test/counts-beside-titles.test.ts`) | enforced |
| R98 | ui | **Every button is the kit's own height.** Aurora, verbatim, 21 Sep 2026, validating the fix to the Knowledge Sync button (`size="sm"` sitting beside toolbar siblings at the kit's own default 40px, K56, `documents/UI-RULEBOOK.md`): "Validated. This is a rule for all buttons, so make sure that I don't find any others like this." Every button in a toolbar, a page head, a card header or a form foot uses the kit `Button`'s default size or `size="icon"` (the same 40px height); `size="sm"` (32px) and any custom height/padding class on a `Button` are forbidden there, outside a named exemption. CHECKED, `web/test/button-sizes.test.ts`, a source census over both front doors plus `shared/web/`: every `<Button` (or a future `<IconButton`) carrying `size="sm"` or a custom `h-`/`py-`/`px-` class, matched past a nested `{…}` expression so an `onClick` arrow's own `=>` never closes the tag early. Twenty-three call sites across seventeen files sat in a toolbar, a page head, a card header or a form foot the day this law shipped and were fixed the same session. What remains in `BUTTON_SIZE_EXEMPT`, one reasoned line per site, sits outside the four named surfaces (an inline error-state retry, a chat confirm row, a composer's own send row, a standalone picker/utility control) or is a dense per-item action inside a repeated list/card, most marked "pending her word"; five sites sit in `web/components/knowledge/`, out of this lane's own brief, reported rather than fixed. | `button-sizes` (`web/test/button-sizes.test.ts`) | enforced |
| R99 | arch | **No record closes while its own clock is still running.** Aurora, verbatim, 21 Sep 2026: "cannot mark anything as closed (task, story, ticket, whatever) if there's an active time log running." ONE shared door-side refusal, `refuseWhileTimerRuns(cfg, guard, { table, id })` (`workers/content/src/lib/work-logs.ts`): a `work_logs` row with `target_table`/`target_id` matching and `ended_at IS NULL` throws `GuardError(409, "timer_running", "Stop the timer first.")`, ANY running timer on the record refusing it, not only the caller's own — two people can be on one piece of work, and it is not closed while either of them is still on the clock, the same reasoning `stories.ts`'s own review-step refusal (`refuseUnreviewable`) already states beside it. Wired into three doors: a story's Done (`setStoryStatus`, the `status === "done"` branch, beside `refuseUndocumented`), a ticket's resolve (`setStatus`'s `resolved` branch, the door `/help/resolve` alone reaches) and a ticket's archive (`setTicketArchived`, only when `archived === true`; taking one back out never needs its own clock stopped). The UI MIRRORS the same fact rather than deciding it twice: the story page's Done button/folded action and the ticket page's Close button/folded action (both read `runningTimersKey(teamId)`, the SAME cache key `useRecordTimerAction` already reads for the Start/Stop control, R56) and the ticket page's Archive menu item are disabled with the reason "Stop the timer first." while a timer on that record is running. CHECKED, `web/test/no-close-while-timer-runs.test.ts`, a source census over `workers/content/src/lib/*.ts` — the only worker whose tables `WORK_LOG_TARGETS` ever names, so no other worker can hold a close door this law reaches: a CANDIDATE is an exported function whose body (comments stripped) carries BOTH a double-quoted JS comparison against `"done"`/`"resolved"`/`"closed"`/`"completed"` (a SQL literal is always single-quoted here, `'done'`, which is what keeps a read-side filter from ever matching) and an UPDATE that actually SETS a closing-shaped column (`status`, `completed_at`, `archived_at`, `resolved`) — the second clause is what tells `tasks.ts`'s `updateTask` (refuses to EDIT an already-done task, never itself closes one) apart from `setTaskDone` beside it. A candidate with no `refuseWhileTimerRuns(` call fails the build unless it is a reasoned, dated, ROT-CHECKED line in the check's own `CLOSE_DOOR_EXEMPT`, keyed by `{file, fn}` (a function's own name, never a line number, which rots on the next edit above it): `tasks.ts`'s `setTaskDone` is the one KNOWN PENDING site, dated 21 Sep 2026, owned by the tasks lane — it already carries this exact check INLINE (its own comment asks for the swap the moment the helper landed) and the row names the exact replacement call, `if (done) await refuseWhileTimerRuns(cfg, guard, { table: "tasks", id })`; `help.ts`'s `bulkSetStatusByFilter` is exempt because its own `resolved` branch is dead code from every route (`refuseDirectResolve` guards the one caller before this function is ever reached); `stories.ts`'s `setSprintComplete` is exempt because a SPRINT is not a `WORK_LOG_TARGETS` table, so no timer can ever run against one. A third case, over `web/`: content tests in `workers/content/test/story-build-notes.test.ts` and `workers/content/test/ticket-work-engine.test.ts` drive the real doors (a running timer refused, a stopped one allowed, on both the story and the ticket), and `web/test/story-detail.test.tsx` + `web/test/ticket-close-moved-to-top.test.tsx` drive the two screens the same way. | `no-close-while-timer-runs` (`web/test/no-close-while-timer-runs.test.ts`) | enforced |
| R100 | ui | **Everywhere (not only tickets), the gear settings button aligns to the middle horizontal of the title.** Aurora, verbatim, 21 Sep 2026, reviewing the deployed tickets module (UI-RULEBOOK.md L43): "EVERYWHERE (not only tickets) align the gear settinsvvutton to middle horozotnal of title." Read at L43 as a new rule for all modules, not the no-containers experiment. Every screen head that draws an action (a module settings gear, an edit pencil, or any other head-mounted control) beside its own title is a flex row with `items-center`, never `items-start`/`items-end`/`items-baseline`, so the action sits on the middle of the title's own line box. A head with a stacked title-plus-subtitle centres the action against the title element alone — its own row split out from the subtitle underneath, never one row whose `items-center` would centre against the whole two-line block. Wired the day this law shipped: `web/components/records/collection-heading.tsx` (`items-start` → `items-center`, every `<CollectionHeading action={…}>` caller inherits the fix), `web/components/screens/kwapso-screen.tsx` (title row split from its subtitle, `items-center`, gear and pencil beside `team.name` alone), and `web-portal/components/collection-heading.tsx` (`items-baseline` → `items-center`, found by the same sweep). CHECKED, `web/test/head-actions-centred.test.ts`, a source census over `web/components`, `web-portal/components` and `shared/web`: every `<div>`/`<section>` whose `className` carries `flex` (a row, never `flex-col`) and both a title marker (`<h1`, `<Title`, `<Headline`, a bare `{heading}`/`{title}` expression) and an action marker (`ModuleSettingsGear`, a bare `{action}`/`{actions}` expression, `headActions`) in one JSX subtree, must also carry `items-center`, or be named in `HEAD_ACTIONS_CENTRED_EXEMPT`, keyed by `{file, contains}`, rot-checked both ways. | `head-actions-centred` (`web/test/head-actions-centred.test.ts`) | enforced |
| R101 | ui | **Every toolbar's search field starts at the content edge, the same seam on every screen; no toolbar host adds its own horizontal inset.** Aurora, verbatim, 22 Sep 2026, over the Triage/Ready pair of screenshots: "Look at the search bar in the toolbar. It has different distances from the left. Make sure that you make this exactly the same everywhere, by the way. The correct one on the screenshots is the one on status ready." Ready's own toolbar (`<PagedFind>`) paints no fill, no radius and carries no inset of its own on its track, so the search field's left edge is the plain frame's own edge; `<ToolbarRow>` (Triage and seventeen other screens) and `<WaveFinder>` both still carried the old painted-pill shape from before `CollectionCard`'s default flipped to plain (rulebook L43, 21 Sep 2026), `ps-4`/`py-1.5 pe-1.5` on the track the actual culprit. CHECKED, `web/test/toolbar-search-edge.test.tsx`, over the app's three bespoke collection-toolbar rows (found by their own shared `data-slot="toolbar-row-track"`/`data-slot="toolbar-row-column"` slot names): the track carries no horizontal inset utility of its own and the column paints no fill and no radius of its own, or the finding is named in `TOOLBAR_SEARCH_EDGE_EXEMPT`, keyed by `{file, expression}`, rot-checked both ways, plus a disk census proving exactly the three known files still wear the shared slot names with none of them still carrying the literal old inset anywhere in source. | `toolbar-search-edge` (`web/test/toolbar-search-edge.test.tsx`) | enforced |
| R102 | ui | **A record's own reference column is as narrow as its chip, and its header is the record's own noun, never "ID".** Read at L43 alongside R96 and R101, from her list of minimal fixes, item 3, 22 Sep 2026 ("the id column narrow and named after the record"). Every table column whose cells render a record's own reference through `<RecordRef>` (R96) sits at `w-px` under the table's own auto layout, never a text column's usual fractional share, and its header is the record's own noun (Ticket, Story, Task, Wave, Phase, App, Account, Contact, Input, Meeting), never the bare word "ID". CHECKED, `web/test/id-column-noun.test.ts`, a source census over `web/components`, `shared/web/screen-engine` and `web/lib/screens.ts`: every column config whose cell renders `<RecordRef` must carry `w-px` and a header from the noun list, or be named in `ID_COLUMN_NOUN_EXEMPT`, keyed by `{file, expression}`, rot-checked both ways. | `id-column-noun` (`web/test/id-column-noun.test.ts`) | enforced |
| R103 | ui | **An empty or filtered-empty register draws no card and no paper fill: text on the page, 24px of air above it, its single door (R88) beside it.** Read at L43, her round of answers on the minimal fixes, 22 Sep 2026, item 2, over the empty-collection screenshot: "ok, but need a bit more spacing over it (like it was with the card)." Neither of R62's two zeros (`CollectionEmptyState` on the agency door, `PortalEmpty` on the portal) may sit inside a painted `<Card>` (the kit's own `default` variant, unless `variant="plain"`), inside any element carrying a literal `bg-surface-panel`/`bg-card` fill, or inside an `<EmptyGatedPanel surface="boxed">` branch: the shell's own default is `"plain"`, so painting it is always a deliberate override. The 24px top inset both registers already carry (`--space-6`) is the one air this law asks for. CHECKED, `web/test/empty-register-on-the-page.test.ts`, a census over `web/components`, `web-portal/components` and `shared/web/screen-engine`: every `<CollectionEmptyState>`/`<PortalEmpty>` mount, its ancestor chain read for a non-plain Card or a literal fill class, and every `<EmptyGatedPanel surface="boxed">` read directly, or named in `EMPTY_REGISTER_EXEMPT`, keyed by `{file, expression}`. A parallel lane owns the last offenders under `web/components`/`web-portal/components`; this census reports what it finds there rather than exempting a file mid-edit. | `empty-register-on-the-page` (`web/test/empty-register-on-the-page.test.ts`) | enforced |
| R104 | ui | **A person or a group of people inside a record section renders as PersonCard chips under an eyebrow label, never in a tile or a card.** Read at L43, her round of validated rulings on the round-48/round-50 batch, 22 Sep 2026, following the 21 Sep 2026 correction over the live tickets pages: "stakeholders raised by design like in the loop (chip like)" and "same with assigned to (chiplike)." Raised by, Assigned to, On the loop and a system's stakeholders all draw the same shape: a plain grey uppercase eyebrow naming the relationship, then `PersonCard orientation="horizontal" size="choice"` directly on the page, no card around it, the RECORD-SECTION reading of a person, never the GALLERY reading (a collection of people-as-records, its own per-record `<Card>`, R65). CHECKED, `web/test/people-as-chips.test.ts`, a census over `web/components/tickets`, `work`, `apps` and `accounts` (where this app's contacts live): every `<PersonCard>` whose ancestor chain reaches a `<Card>` of any variant is a finding, unless its whole file is named in `PEOPLE_AS_CHIPS_EXEMPT` with the reason "a per-record card in a grid". | `people-as-chips` (`web/test/people-as-chips.test.ts`) | enforced |
| R105 | ui | **Inside a sheet, every fact label shares the eyebrow register, text-micro, uppercase, muted, above its own content, and the sheet's own sections carry no background.** Her answer, 22 Sep 2026, item 6, reading the merged task-sheet section back: "there are 3 stiles of titles here adn that does not make sense: unify!! assigend to, details and deadline the three look different! ... also the assigned to person make it a chip, like in stories and put the title above." `task-sheet.tsx`'s own merged Assigned to/Details/Deadline section is the shape this law generalises: one shared class list, `text-micro text-muted-foreground uppercase`, on every fact label, sitting above its own content, and no `bg-*` fill anywhere in the sheet's own source. CHECKED, `web/test/sheet-fact-labels.test.ts`, a census over every file under `web/components` that draws a literal `<SheetContent`: every eyebrow-shaped label (`uppercase` + `text-muted-foreground`) must share the identical class list with every other one in that same file, `task-sheet.tsx`'s own three asserted against the canonical string directly, or named in `SHEET_FACT_LABEL_EXEMPT`, keyed by `{file, expression}`. | `sheet-fact-labels` (`web/test/sheet-fact-labels.test.ts`) | enforced |
| R106 | ui | **Every record page fills the shell's footer slot exactly once and no body renders the band inline; a sheet renders it as a stripe, the sheet body's own last child outside the scroller; one column stacks Record above Latest activity.** The general shape behind D21/L31 (`footer-on-the-edge`), kit v1.2.155: `ScreenFooterSlot` (`web/components/shell/footer-slot.tsx`) portals the ink footer band into `ScreenShell`'s own footer slot, outside the body's padded stack, flush with the pane's bottom edge; a record page (`record-chrome.tsx`, `ticket-detail-body.tsx`, `knowledge-detail.tsx`, `story-detail.tsx`) calls it exactly once. A sheet has no slot of its own: `task-sheet.tsx` draws `RecordFooterBand` with its `stripe` prop instead, the sheet body's own last child outside its scrolling region (Aurora, 22 Sep 2026: "the footer ... has to be at the very bottom, and also make it a stripe, not a container"). On a one-column footer the record's own facts stack above Latest activity, the kit's own fixed order. CHECKED: `web/test/footer-on-the-edge.test.ts` and `web/test/task-sheet.test.tsx` are both registered as this law's checks. `RECORD_FOOTER_SLOT_EXEMPT` names `shared/web/screen-engine/screen-renderer.tsx`, the recipe-driven detail path, shared with the portal, which has no footer slot. Decision pending. | `footer-band-home` (`web/test/footer-on-the-edge.test.ts`, `web/test/task-sheet.test.tsx`) | enforced |
| R107 | ui | **The Effort section's metric tiles span the section in a three-column grid with the kit's 16px gap, their left edge on the title's edge, each tile a Card default with the kit's content inset, and the log rows below carry no fill, separated by the kit Separator.** Her item 5, the round of answers on the minimal fixes, 22 Sep 2026, read as the kit lane's own item over the Effort card's own alignment and margin: "look screenshot. its not correct, the contact is touching the border." `EffortCard` (`web/components/work/effort-card.tsx`): the three stat tiles sit in a `sm:grid-cols-3 gap-[var(--space-4)]` grid, flush with the section title's own left edge; each tile keeps the kit's own `Card` default (one of `PAPER_ON_PURPOSE`'s five named exceptions, a metric standing for itself, not a grouping section); the work-log rows below are a plain list, no fill, no rounded/divide-y border utility, separated by exactly one kit `<Separator>` between adjacent rows, never above the first or below the last. CHECKED: `web/test/effort-card.test.tsx`'s own existing cases (the three-column gap-4 grid, the tiles' own Card default, the log rows' no-fill plain list, the one-Separator-between-rows shape) are registered as this law's check. | `effort-tiles` (`web/test/effort-card.test.tsx`) | enforced |
| R108 | ui | **A record section's own title shares one style, everywhere: the same small-caps eyebrow the task sheet's own fact labels already carry, never a separate "section title" register.** Her ruling, 22 Sep 2026, over a screenshot of a ticket record: "look at screenshot. i want that we have 1 single deign for titles. make it like in tasks 'assignd to, details, deadline' so evetything in tickets/stories that are titles (assigned to, pahse and wave, effort, stakeholders, related tickets, related stories...) make the chnage here and everywhee else. what we are changing is the sytle of the title of a section. implement and write the rule." Every record section's TITLE (Assigned to, Category, Phase and wave, Effort, Stakeholders, Related tickets, Related stories and every sibling) now draws R105's own canonical class, `text-micro text-muted-foreground uppercase`, never the old `text-sm font-medium`. Two shared hosts carry every ticket and story call site at once: `TicketSidePanel` (`web/components/tickets/ticket-detail-body.tsx`) and its twin `EmptyGatedPanel` (`web/components/deep-link/screen-bits.tsx`); three hand-rolled siblings were fixed the same way (`meeting-detail.tsx`, `client-org-panel.tsx`, `stakeholders-panel.tsx`). The heading stays a real heading element carrying its own `id` wherever it already had one, only the STYLE moved. Her screenshot also showed the consequence: the Assigned to section was titled "Assigned to" and then repeated "ASSIGNED TO" a second time as `AssignedToCard`'s own inner eyebrow (`help-stakeholders.tsx`): a label agreeing with its own heading. That inner chip is dropped for the ticket's own assignee now; "From the app" survives, because it says something the title does not. A section with several different facts under one title (Stakeholders' own "Raised by"/"On the loop", the merged Phase/Wave facts) keeps both, because those words differ from the section title. CHECKED, `web/test/section-title-one-style.test.ts`: a tripwire over `TicketSidePanel`/`EmptyGatedPanel`'s own title lines, plus a census over `web/components` and `web-portal/components` for a literal `<h2`/`<h3`/`<h4` carrying both `text-sm` and `font-medium` (the old shape), or named in `SECTION_TITLE_EXEMPT`, keyed by `{file, expression}`, rot-checked both ways. | `section-title-one-style` (`web/test/section-title-one-style.test.ts`) | enforced |

## How to add a new law

1. Add a row to `RULES_REGISTRY` in `shared/rules/registry.ts` with a real
   `checkId`.
2. Write the check (a per-worker test, or a case in `web/test/rules.test.ts`).
3. Add the matching row to the table above.

The `registry-integrity` check verifies steps 1 and 3 stay in sync. A rule with
no working check is not a law, delete it or write the check.

## Dimensions

Laws are cross-cutting: **arch** (architecture/data), **ui** (interface),
**workflow** (how we build), **ai** (the assistant). The agent/MCP tool surface is
already held to the doors it forwards to (`workers/mcp/test/catalog.test.ts` +
`workers/data-ops/test/trace-parity.test.ts`); a natural next row is a UI law once a
new interface pattern stabilises.
